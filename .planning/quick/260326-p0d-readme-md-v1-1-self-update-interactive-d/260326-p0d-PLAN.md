---
phase: quick
plan: 260326-p0d
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: []

must_haves:
  truths:
    - "README.md reflects v1.1 features: self-update, interactive discovery, wave execution, model/lang commands"
    - "New commands table includes all v1.1 commands (/mysd:update, /mysd:discuss, /mysd:fix, /mysd:note, /mysd:model, /mysd:lang, /mysd:ff, /mysd:ffe)"
    - "Architecture section mentions wave parallel execution and worktree engine"
    - "Configuration section reflects new options (model_profile, wave execution mode)"
  artifacts:
    - path: "README.md"
      provides: "Updated project README for v1.1"
      min_lines: 180
  key_links: []
---

<objective>
Update README.md to reflect v1.1 features — covering self-update, interactive discovery, wave execution, model/lang management, and new commands added in Phases 5-10.

Purpose: The current README only covers v1.0 features. Users and contributors need accurate documentation of v1.1 capabilities.
Output: Updated README.md with comprehensive v1.1 feature documentation.
</objective>

<execution_context>
@D:/work_data/project/go/mysd/.claude/get-shit-done/workflows/execute-plan.md
@D:/work_data/project/go/mysd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with v1.1 features</name>
  <files>README.md</files>
  <action>
Update README.md to document all v1.1 features. Preserve the existing structure and tone. Specific changes:

**1. Update "Why mysd?" section — add v1.1 highlights:**
- Wave parallel execution — tasks with dependency analysis run in parallel via git worktrees
- Interactive discovery — 4-dimension research (Codebase/Domain/Architecture/Pitfalls) with advisor agents
- Self-update — `mysd update` checks GitHub Releases, downloads platform binary, syncs plugin files
- Deferred notes — scope guardrail that captures out-of-scope ideas for future changes

**2. Update "Quick Start" section:**
- Add `/mysd:discuss` between propose and spec (optional interactive exploration)
- Replace `/mysd:execute` with `/mysd:apply` (Phase 8 renamed execute to apply at SKILL.md layer)
- Add note about wave parallel mode: tasks with dependencies auto-group into waves

**3. Update "Commands" table — add ALL new v1.1 commands:**

| Command | Description |
|---------|-------------|
| `/mysd:discuss` | Interactive exploration with 4-dimension research and advisor agents |
| `/mysd:fix` | Fix failed tasks in worktree isolation with optional research mode |
| `/mysd:update` | Check and update mysd binary + sync plugin files |
| `/mysd:note` | Manage deferred notes (scope guardrail for out-of-scope ideas) |
| `/mysd:model` | View/set model profile (quality/balanced/budget) |
| `/mysd:lang` | Set response language and OpenSpec locale |

Also update existing command descriptions if needed:
- `/mysd:plan` — now includes plan-checker MUST coverage verification
- `/mysd:execute` → `/mysd:apply` — now supports wave parallel execution mode
- `/mysd:ff` — update description: "Fast-forward: propose through plan (with auto research)"
- `/mysd:ffe` — update description: "Fast-forward: propose through execute (with auto research, auto execution)"

**4. Update "How It Works" lifecycle:**
- Add "Discuss" as optional step between Propose and Spec
- Mention wave parallel execution in the Execute step
- Mention plan-checker in the Plan step

**5. Add "Wave Parallel Execution" subsection under "Key Architecture":**
- Tasks with `depends` fields are topologically sorted into waves
- Same-wave tasks with no file overlap run in parallel git worktrees
- Auto merge with AI conflict resolution (3 retries), failed worktrees preserved
- Mode selection: sequential (safe) vs wave (parallel) — auto mode skips prompt

**6. Add "Self-Update" section (after Configuration or before Tech Stack):**
```
## Self-Update

mysd can update itself from GitHub Releases:

/mysd:update           # Check for updates and install interactively
mysd update --check    # Check only (JSON output, no install)
mysd update --force    # Update without confirmation

Updates include:
- Binary replacement with SHA256 checksum verification
- Automatic rollback on failure
- Plugin file sync (commands + agents) via manifest diff
```

**7. Update "Configuration" section:**
- Add `response_language` field
- Note that `execution_mode` now supports `wave` for parallel execution
- Note `model_profile` controls AI model selection across all agent roles

**8. Fix Tech Stack version:**
- Change "Go 1.25+" to "Go 1.23+" (matches CLAUDE.md)

**9. Do NOT change:**
- Installation section (unchanged)
- OpenSpec Compatibility section (unchanged)
- License section (unchanged)
  </action>
  <verify>
    <automated>grep -c "mysd:update\|mysd:discuss\|mysd:fix\|mysd:note\|mysd:model\|mysd:lang\|wave\|self-update\|worktree\|parallel" README.md</automated>
  </verify>
  <done>README.md accurately documents all v1.1 features including: self-update command, interactive discovery with 4-dimension research, wave parallel execution, model/lang management commands, deferred notes, plan-checker, and fix command. All command descriptions are current. No v1.0-only stale information remains.</done>
</task>

</tasks>

<verification>
- README.md contains all v1.1 commands in the Commands table
- Wave parallel execution is documented
- Self-update flow is documented
- Interactive discovery is mentioned
- Go version shows 1.23+ (not 1.25+)
- No broken markdown formatting
</verification>

<success_criteria>
- README.md has 180+ lines covering v1.1 features
- All 6 new commands appear in the Commands table
- Self-update section exists with CLI examples
- Wave parallel execution is explained
- Interactive discovery is mentioned in workflow
</success_criteria>

<output>
After completion, create `.planning/quick/260326-p0d-readme-md-v1-1-self-update-interactive-d/260326-p0d-SUMMARY.md`
</output>
