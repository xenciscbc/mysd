---
plan: "11-03"
title: "Executor failure sidecar + fix alignment + .gitignore"
phase: 11
wave: 1
depends_on: []
files_modified:
  - .claude/agents/mysd-executor.md
  - .claude/commands/mysd-fix.md
  - .gitignore
autonomous: true
requirements:
  - D-06
  - D-07
  - D-08
  - D-09
must_haves:
  truths:
    - "Executor agent writes sidecar file on task failure with error context"
    - "Fix agent reads sidecar file for diagnosis context"
    - ".sidecar/ directories are gitignored"
  artifacts:
    - path: ".claude/agents/mysd-executor.md"
      provides: "On-failure sidecar writing logic"
      contains: ".sidecar/T"
    - path: ".claude/commands/mysd-fix.md"
      provides: "Sidecar reading in Step 5B aligned to D-06 path format"
      contains: ".sidecar/T"
    - path: ".gitignore"
      provides: ".sidecar/ exclusion pattern"
      contains: ".sidecar/"
  key_links:
    - from: ".claude/agents/mysd-executor.md"
      to: ".specs/changes/{change_name}/.sidecar/"
      via: "Write tool creating T{id}-failure.md"
      pattern: "\\.sidecar/T.*-failure\\.md"
    - from: ".claude/commands/mysd-fix.md"
      to: ".specs/changes/{change_name}/.sidecar/"
      via: "Read tool loading T{id}-failure.md"
      pattern: "\\.sidecar/T.*-failure\\.md"
---

<objective>
Add failure sidecar writing to the executor agent so that when a task fails, diagnostic context is persisted for the fix agent. Align the fix SKILL.md to read from the correct sidecar path. Add .sidecar/ to .gitignore.

Purpose: Give /mysd:fix full failure context without requiring users to re-describe errors.
Output: Updated executor agent, fix SKILL.md, and .gitignore.
</objective>

<execution_context>
@D:/work_data/project/go/mysd/.claude/get-shit-done/workflows/execute-plan.md
@D:/work_data/project/go/mysd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-agent-doc/11-CONTEXT.md

<interfaces>
<!-- Current executor agent: 227 lines, no sidecar logic -->
<!-- Current fix SKILL.md: Step 5B reads "task sidecar" but path format not aligned to D-06 -->

From .claude/agents/mysd-executor.md:
- Step 3: Implement the Task
- Step 4: Mark Task Done (mysd task-update {id} done)
- No on-failure handling currently exists

From .claude/commands/mysd-fix.md:
- Step 3: Get Task Context — mentions "Failure reason from task sidecar (if available)"
- Step 5B: Implementation Failure Path — Step 1 "Read task sidecar: failure reason, AI attempts, test output"

Sidecar path per D-06: .specs/changes/{change_name}/.sidecar/T{id}-failure.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add on-failure sidecar writing to mysd-executor.md + update .gitignore</name>
  <files>.claude/agents/mysd-executor.md, .gitignore</files>
  <read_first>
    - .claude/agents/mysd-executor.md (full file — understand Step 3/4 flow to insert failure handling after Step 3 and before Step 4)
    - .gitignore (current content — append .sidecar/ pattern)
  </read_first>
  <action>
    1. Edit `.claude/agents/mysd-executor.md` — add an "On Failure" section between the Task Execution section and the Completion Summary section. Insert after Step 5 (Atomic Commit) and before Post-Execution Test Generation:

    ```markdown
    ---

    ## On Failure — Sidecar Context Writing (D-06, D-07)

    If ANY step in Task Execution fails (build error, test failure, implementation error, mid-execution abort):

    ### Step F1: Capture Failure Context

    Collect:
    - `error_output`: The build/test error output or error message from the failing step
    - `task_description`: The assigned_task name and description
    - `attempted_fix`: Any diagnostic or fix attempts made before giving up (if any)
    - `files_modified`: List of files that were created or modified before the failure

    ### Step F2: Write Sidecar File

    Create the sidecar directory and write the failure context:

    ```
    mkdir -p .specs/changes/{change_name}/.sidecar/
    ```

    Write to `.specs/changes/{change_name}/.sidecar/T{assigned_task.id}-failure.md`:

    ```markdown
    ---
    task_id: {assigned_task.id}
    task_name: {assigned_task.name}
    timestamp: {ISO 8601 timestamp}
    change_name: {change_name}
    ---

    ## Error Output

    ```
    {error_output — full build/test error, truncated to last 200 lines if very long}
    ```

    ## Task Description

    {assigned_task.description}

    ## Files Modified Before Failure

    {list of files touched before the error occurred}

    ## AI Diagnostic Attempts

    {summary of any fix attempts made, or "None — failed on first attempt"}
    ```

    ### Step F3: Mark Task Failed

    ```
    mysd task-update {assigned_task.id} failed
    ```

    After writing the sidecar and marking failed, output:
    "Task T{id} failed. Failure context saved to `.specs/changes/{change_name}/.sidecar/T{id}-failure.md`. Run `/mysd:fix T{id}` to diagnose and retry."

    **IMPORTANT:** Do NOT proceed to "Mark Task Done" (Step 4) or "Atomic Commit" (Step 5) when a failure occurs. The On Failure path is an alternative exit.
    ```

    2. Edit `.gitignore` — append:
    ```
    .sidecar/
    ```
  </action>
  <verify>
    <automated>grep -c "sidecar" "D:/work_data/project/go/mysd/.claude/agents/mysd-executor.md" &amp;&amp; grep -c ".sidecar/" "D:/work_data/project/go/mysd/.gitignore"</automated>
  </verify>
  <acceptance_criteria>
    - mysd-executor.md contains "## On Failure" section header
    - mysd-executor.md contains ".specs/changes/{change_name}/.sidecar/T{assigned_task.id}-failure.md" path
    - mysd-executor.md On Failure section contains "mkdir -p .specs/changes/{change_name}/.sidecar/"
    - mysd-executor.md On Failure section contains "mysd task-update {assigned_task.id} failed"
    - mysd-executor.md contains sidecar markdown template with task_id, task_name, timestamp, change_name frontmatter
    - mysd-executor.md contains "Error Output" and "AI Diagnostic Attempts" section headers in sidecar template
    - mysd-executor.md On Failure section states "Do NOT proceed to Mark Task Done"
    - .gitignore contains ".sidecar/"
  </acceptance_criteria>
  <done>Executor agent writes structured failure context to .sidecar/ on task failure. .sidecar/ is gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Align mysd-fix.md sidecar reading path to D-06 format</name>
  <files>.claude/commands/mysd-fix.md</files>
  <read_first>
    - .claude/commands/mysd-fix.md (full file — identify Steps 3, 4, and 5B that reference sidecar)
    - .claude/agents/mysd-executor.md (Task 1 output — confirm sidecar path format: .specs/changes/{change_name}/.sidecar/T{id}-failure.md)
  </read_first>
  <action>
    Edit `.claude/commands/mysd-fix.md` to align sidecar reading with D-06 path format:

    1. In Step 3 (Get Task Context), update the sidecar reading instruction to be explicit:
       Change the vague "Failure reason from task sidecar (if available)" to:
       ```markdown
       Check for failure sidecar file:
       ```
       Read .specs/changes/{change_name}/.sidecar/T{target_task.id}-failure.md
       ```
       If file exists: parse frontmatter (task_id, task_name, timestamp) and body sections (Error Output, Files Modified, AI Diagnostic Attempts). Store as `failure_context`.
       If file does not exist: set `failure_context` to null (backward compat per D-08 — degrade to no-context diagnosis).
       ```

    2. In Step 4 (Path Detection), update the implementation failure detection to use sidecar:
       Change: `Read task sidecar for failure details (build errors, test failures)`
       To:
       ```markdown
       If `failure_context` is not null:
         PATH = "implementation" (sidecar confirms implementation failure)
       If `failure_context` is null and task is failed:
         PATH = "implementation" (no sidecar — will diagnose without context)
       ```

    3. In Step 5B (Implementation Failure Path), Step 1 (Diagnose), make sidecar reading explicit:
       Change: `Read task sidecar: failure reason, AI attempts, test output`
       To:
       ```markdown
       1. **Diagnose (D-08, D-12):**
          If `failure_context` exists (from Step 3):
            Present to user:
            - Error output from sidecar
            - Files modified before failure
            - AI diagnostic attempts (if any)
            - Timestamp of failure

          If `failure_context` is null:
            Inform user: "No failure sidecar found — diagnosing from scratch."
            Read the task description and attempt to reproduce the error:
            ```
            go build ./...
            go test ./...
            ```
            Present any errors found.
       ```
  </action>
  <verify>
    <automated>grep -c ".sidecar/T" "D:/work_data/project/go/mysd/.claude/commands/mysd-fix.md"</automated>
  </verify>
  <acceptance_criteria>
    - mysd-fix.md Step 3 contains ".specs/changes/{change_name}/.sidecar/T{target_task.id}-failure.md" path
    - mysd-fix.md Step 3 contains "failure_context" variable assignment
    - mysd-fix.md Step 3 contains backward compat note ("If file does not exist: set failure_context to null")
    - mysd-fix.md Step 5B contains conditional on failure_context existence
    - mysd-fix.md Step 5B contains "No failure sidecar found" fallback message
    - mysd-fix.md does NOT contain vague "task sidecar" references without explicit path
  </acceptance_criteria>
  <done>Fix SKILL.md reads sidecar from exact D-06 path. Graceful fallback when sidecar missing.</done>
</task>

</tasks>

<verification>
1. mysd-executor.md contains On Failure section with sidecar writing
2. mysd-fix.md references exact sidecar path .specs/changes/{change_name}/.sidecar/T{id}-failure.md
3. .gitignore includes .sidecar/ pattern
4. Both files have valid YAML frontmatter (model, description, allowed-tools)
</verification>

<success_criteria>
- Executor writes structured sidecar on failure
- Fix reads sidecar for diagnosis context with backward-compat fallback
- .sidecar/ excluded from git
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-doc/11-03-SUMMARY.md`
</output>
