---
plan: "11-02"
title: "Workflow auto-chain — propose auto-spec + apply auto-verify"
phase: 11
wave: 1
depends_on: []
files_modified:
  - .claude/commands/mysd-propose.md
  - .claude/commands/mysd-apply.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-05
must_haves:
  truths:
    - "After propose Step 10, spec-writer is automatically invoked (auto-chain)"
    - "After apply Step 4, go build + go test + mysd-verifier runs automatically"
    - "propose --skip-spec flag bypasses auto-spec invocation"
    - "apply --auto skips verify confirmation prompt"
  artifacts:
    - path: ".claude/commands/mysd-propose.md"
      provides: "Step 11 auto-spec chain after proposal completion"
      contains: "Step 11"
    - path: ".claude/commands/mysd-apply.md"
      provides: "Step 5 auto-verify chain after task execution"
      contains: "Step 5"
  key_links:
    - from: ".claude/commands/mysd-propose.md"
      to: "mysd-spec-writer agent"
      via: "Task tool invocation in Step 11"
      pattern: "mysd-spec-writer"
    - from: ".claude/commands/mysd-apply.md"
      to: "mysd-verifier agent"
      via: "Task tool invocation in Step 5"
      pattern: "mysd-verifier"
---

<objective>
Add auto-chain steps to propose and apply SKILL.md files: propose auto-invokes spec-writer after completion (D-01), apply auto-invokes verify after execution (D-02).

Purpose: Reduce manual workflow steps — users no longer need to run /mysd:spec after propose or /mysd:verify after apply.
Output: Updated mysd-propose.md and mysd-apply.md with auto-chain steps.
</objective>

<execution_context>
@D:/work_data/project/go/mysd/.claude/get-shit-done/workflows/execute-plan.md
@D:/work_data/project/go/mysd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-agent-doc/11-CONTEXT.md

<interfaces>
<!-- Reference: discuss.md Step 10-11 already implements auto-spec chain — same pattern -->

From .claude/commands/mysd-discuss.md (reference for auto-spec pattern):
- Step 10: Confirm discussion results
- Step 11: Auto-invoke spec-writer via Task tool with change_name + capability_area + existing_spec_body + auto_mode

From .claude/agents/mysd-spec-writer.md:
- Input: change_name, capability_area, existing_spec_body, proposal, auto_mode

From .claude/agents/mysd-verifier.md:
- Input: change_name, must_items, should_items, may_items
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 11 auto-spec to mysd-propose.md</name>
  <files>.claude/commands/mysd-propose.md</files>
  <read_first>
    - .claude/commands/mysd-propose.md (current 10-step flow — Step 11 will be appended)
    - .claude/commands/mysd-discuss.md (reference: Steps 10-11 for auto-spec pattern — copy the Task tool invocation format exactly)
    - .claude/agents/mysd-spec-writer.md (verify expected input fields: change_name, capability_area, existing_spec_body, proposal, auto_mode)
  </read_first>
  <action>
    Edit `.claude/commands/mysd-propose.md`:

    1. In Step 1 (Parse Arguments), add `--skip-spec` flag parsing alongside `--auto`:
       ```
       Check `$ARGUMENTS` for `--auto` and `--skip-spec`. Remove them from the arguments list.
       Set `auto_mode` = true if `--auto` is present, false otherwise.
       Set `skip_spec` = true if `--skip-spec` is present, false otherwise.
       ```

    2. Update the `argument-hint` frontmatter to: `"[change-name|file|dir] [--auto] [--skip-spec]"`

    3. After Step 10 (Confirm), add Step 11:

       ```markdown
       ## Step 11: Auto-Invoke Spec Writer (D-01, D-04)

       If `skip_spec` is true: skip this step entirely. Show: "Spec generation skipped (--skip-spec). Run `/mysd:spec` manually when ready."

       Otherwise, automatically invoke the spec-writer agent to generate specs from the proposal.

       Read the proposal body:
       ```
       Read .specs/changes/{change_name}/proposal.md
       ```

       Read existing spec files (if any):
       ```
       ls .specs/changes/{change_name}/specs/
       ```

       For each capability area found in the proposal (or a single "core" area if not structured by capability):

       Use the Task tool to invoke `mysd-spec-writer`:
       ```
       Task: Generate specs for {change_name} — {capability_area}
       Agent: mysd-spec-writer
       Context: {
         "change_name": "{change_name}",
         "capability_area": "{capability_area}",
         "existing_spec_body": "{existing spec content if any, else null}",
         "proposal": "{proposal.md body}",
         "auto_mode": {auto_mode}
       }
       ```

       After spec-writer completes, show:
       1. Spec summary: number of MUST / SHOULD / MAY requirements generated
       2. Spec file paths created/updated
       3. Next steps:
          - `/mysd:plan` — Create execution plan from specs
          - `/mysd:design` — Add design decisions before planning
          - `/mysd:discuss` — Explore requirements interactively
          - `/mysd:spec` — Manually edit or regenerate specs
       ```

    4. Update Step 10 point 6 to remove the manual spec suggestion (since Step 11 handles it):
       Change: `Next step: "Run /mysd:spec to define detailed requirements, or /mysd:plan if specs are ready"`
       To: `Proceeding to spec generation...` (or remove point 6 entirely since Step 11 follows immediately)
  </action>
  <verify>
    <automated>grep -c "Step 11" "D:/work_data/project/go/mysd/.claude/commands/mysd-propose.md"</automated>
  </verify>
  <acceptance_criteria>
    - mysd-propose.md contains "## Step 11" section
    - mysd-propose.md Step 11 contains "mysd-spec-writer" Task tool invocation
    - mysd-propose.md Step 1 contains "--skip-spec" flag parsing
    - mysd-propose.md argument-hint contains "--skip-spec"
    - mysd-propose.md Step 11 contains "skip_spec" conditional check
    - mysd-propose.md Step 11 contains spec summary output (MUST / SHOULD / MAY count)
    - mysd-propose.md Step 11 contains next steps list with /mysd:plan, /mysd:design, /mysd:discuss, /mysd:spec
  </acceptance_criteria>
  <done>propose.md auto-chains to spec-writer after Step 10. --skip-spec flag bypasses the auto-chain. Spec summary and next step hints displayed after generation.</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 5 auto-verify to mysd-apply.md</name>
  <files>.claude/commands/mysd-apply.md</files>
  <read_first>
    - .claude/commands/mysd-apply.md (current 4-step flow — Step 5 will be appended after Step 4)
    - .claude/commands/mysd-verify.md (reference: verify SKILL.md flow — understand how verifier is invoked)
    - .claude/agents/mysd-verifier.md (verify expected input: change_name, must_items, should_items, may_items)
  </read_first>
  <action>
    Edit `.claude/commands/mysd-apply.md`:

    After Step 4 (Post-Execution), add Step 5:

    ```markdown
    ## Step 5: Auto-Verify (D-02, D-05)

    After all tasks complete, automatically run verification.

    ### Step 5a: Build and Test

    Run build check:
    ```
    go build ./...
    ```

    If build fails:
    - Display the build error output
    - Show: "Build failed. Skipping spec verification. Run `/mysd:fix` to address build errors."
    - STOP here — do not proceed to verifier agent.

    Run test check:
    ```
    go test ./...
    ```

    If tests fail:
    - Display the test failure output
    - Show: "Tests failed. Skipping spec verification. Run `/mysd:fix` to address test failures."
    - STOP here — do not proceed to verifier agent.

    ### Step 5b: Spec Verification

    If build and tests both pass, invoke the verifier agent.

    First, get fresh context for verification:
    ```
    mysd execute --context-only
    ```
    Parse JSON to get `must_items`, `should_items`, `may_items`.

    If `auto_mode` is true: proceed directly to verifier (skip confirmation per D-05).

    If `auto_mode` is false: ask user:
    ```
    Build and tests pass. Run spec verification now? [Y/n]
    ```
    If user declines: show "Skipped. Run `/mysd:verify` manually when ready." and end.

    Use the Task tool to invoke `mysd-verifier`:
    ```
    Task: Verify spec coverage for {change_name}
    Agent: mysd-verifier
    Context: {
      "change_name": "{change_name}",
      "must_items": [...],
      "should_items": [...],
      "may_items": [...]
    }
    ```

    After verifier completes, show:
    - Verification result summary
    - If all MUST items pass: "Ready to archive. Run `/mysd:archive`."
    - If MUST items fail: "Some MUST requirements not met. Run `/mysd:fix` or re-execute."
    ```

    Also update Step 4 to change the next step hint:
    - Remove: `Next step: "Run /mysd:verify to validate results"`
    - Change to: `Proceeding to auto-verify...`
  </action>
  <verify>
    <automated>grep -c "Step 5" "D:/work_data/project/go/mysd/.claude/commands/mysd-apply.md"</automated>
  </verify>
  <acceptance_criteria>
    - mysd-apply.md contains "## Step 5: Auto-Verify" section
    - mysd-apply.md Step 5 contains "go build ./..." command
    - mysd-apply.md Step 5 contains "go test ./..." command
    - mysd-apply.md Step 5 contains "mysd-verifier" Task tool invocation
    - mysd-apply.md Step 5 contains auto_mode conditional (skip confirmation when auto)
    - mysd-apply.md Step 5 contains build-fail early exit ("Skipping spec verification")
    - mysd-apply.md Step 4 no longer says "Run /mysd:verify"
  </acceptance_criteria>
  <done>apply.md auto-chains to verify after all tasks complete. Build/test failures stop before verifier. auto_mode skips confirmation.</done>
</task>

</tasks>

<verification>
1. mysd-propose.md has 11 steps (was 10)
2. mysd-apply.md has 5 steps (was 4)
3. Both files have valid YAML frontmatter
4. No broken markdown formatting
</verification>

<success_criteria>
- propose auto-chains to spec-writer after proposal completion
- apply auto-chains to verify (build + test + verifier) after task execution
- --skip-spec flag in propose bypasses auto-spec
- --auto in apply skips verify confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-doc/11-02-SUMMARY.md`
</output>
