---
plan: "11-04"
title: "Archive doc maintenance flow + ff/ffe inline additions"
phase: 11
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - .claude/commands/mysd-archive.md
  - .claude/commands/mysd-ff.md
  - .claude/commands/mysd-ffe.md
autonomous: true
requirements:
  - D-03
  - D-11
  - D-11b
  - D-13
  - D-14
  - D-17
  - D-18
must_haves:
  truths:
    - "archive reads docs_to_update via mysd execute --context-only"
    - "archive updates each file in docs_to_update with LLM after successful archive"
    - "archive shows file list confirmation before updating (skipped in --auto)"
    - "archive skips doc update when docs_to_update is empty"
    - "CHANGELOG.md gets prepend strategy, README.md gets full rewrite"
    - "ff inserts inline auto-verify between execute and archive"
    - "ff inserts inline docs_to_update after archive"
    - "ffe inserts inline auto-verify between execute and archive"
    - "ffe inserts inline docs_to_update after archive"
  artifacts:
    - path: ".claude/commands/mysd-archive.md"
      provides: "Doc maintenance flow after archive"
      contains: "docs_to_update"
    - path: ".claude/commands/mysd-ff.md"
      provides: "Inline auto-verify + docs update steps"
      contains: "docs_to_update"
    - path: ".claude/commands/mysd-ffe.md"
      provides: "Inline auto-verify + docs update steps"
      contains: "docs_to_update"
  key_links:
    - from: ".claude/commands/mysd-archive.md"
      to: "mysd execute --context-only"
      via: "Bash command at Step 0 to read docs_to_update"
      pattern: "mysd execute --context-only"
    - from: ".claude/commands/mysd-ff.md"
      to: "mysd execute --context-only"
      via: "Bash command to read docs_to_update after archive"
      pattern: "mysd execute --context-only"
---

<objective>
Rewrite archive SKILL.md to include doc maintenance flow (read docs_to_update config, update files with LLM after archive). Add inline auto-verify and docs_to_update steps to ff and ffe pipelines.

Purpose: Automate post-archive documentation updates. Ensure ff/ffe pipelines have the same verify + doc-update capabilities as the regular workflow.
Output: Updated archive.md, ff.md, ffe.md with doc maintenance and verify flows.
</objective>

<execution_context>
@D:/work_data/project/go/mysd/.claude/get-shit-done/workflows/execute-plan.md
@D:/work_data/project/go/mysd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-agent-doc/11-CONTEXT.md
@.planning/phases/11-agent-doc/11-01-SUMMARY.md
@.planning/phases/11-agent-doc/11-02-SUMMARY.md

<interfaces>
<!-- After Plan 01, mysd execute --context-only JSON includes docs_to_update field -->
<!-- After Plan 02, apply has Step 5 auto-verify pattern to reference -->

From internal/executor/context.go (after Plan 01):
```go
type ExecutionContext struct {
	// ... existing fields ...
	DocsToUpdate []string `json:"docs_to_update,omitempty"`
}
```

From mysd-apply.md Step 5 (after Plan 02):
- Step 5a: go build ./... + go test ./...
- Step 5b: mysd-verifier agent invocation

Current mysd-archive.md: 2 steps (Step 1: mysd archive, Step 2: Post-Archive Guidance)
Current mysd-ff.md: 5 steps (Step 1-5)
Current mysd-ffe.md: 6 steps (Step 1-6)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite mysd-archive.md with doc maintenance flow</name>
  <files>.claude/commands/mysd-archive.md</files>
  <read_first>
    - .claude/commands/mysd-archive.md (current 2-step flow — will be restructured to ~4 steps)
    - .claude/commands/mysd-apply.md (Step 1 --auto parsing pattern for reference)
    - .planning/phases/11-agent-doc/11-CONTEXT.md (D-11, D-11b, D-13, D-14, D-18 details)
  </read_first>
  <action>
    Rewrite `.claude/commands/mysd-archive.md` with this structure:

    **Frontmatter update:**
    - Add `Write` and `Edit` to allowed-tools (needed for doc updates)
    - Add `Task` to allowed-tools (not strictly needed but consistent)
    - Update argument-hint to: `"[--auto]"`

    **New step structure:**

    ```markdown
    ## Step 0: Parse Arguments and Read Config (D-18)

    Check `$ARGUMENTS` for `--auto`. Set `auto_mode` = true if `--auto` is present, false otherwise.

    Read docs_to_update config:
    ```
    mysd execute --context-only
    ```
    Parse JSON output. Extract `docs_to_update` array and `change_name`.
    If `docs_to_update` is null or empty: set `has_docs_to_update` = false.
    Otherwise: set `has_docs_to_update` = true, store the file paths list.

    ## Step 1: Archive the Change

    Run:
    ```
    mysd archive
    ```

    {Keep ALL existing error handling from current Step 1 — "must be verified", "not done", other errors}

    If archive succeeds, proceed to Step 2.

    ## Step 2: Doc Maintenance (D-11, D-11b, D-13, D-14)

    If `has_docs_to_update` is false:
      Skip this step. Go to Step 3.

    ### Step 2a: Confirm with User (D-13)

    If `auto_mode` is true: skip confirmation, proceed directly to Step 2b.

    If `auto_mode` is false:
    ```
    The following files are configured for post-archive update:
    {list each file path, one per line}

    Press Enter to proceed, or type 'n' to skip doc updates.
    ```
    If user types 'n': skip to Step 3.

    ### Step 2b: Read Context for Updates (D-11b)

    For the archived change, read:
    - `.specs/archive/{change_name}/proposal.md` — what and why
    - `.specs/archive/{change_name}/tasks.md` — what was done
    - `.specs/archive/{change_name}/specs/` — all spec files (MUST/SHOULD/MAY requirements)

    Combine into `update_context`.

    ### Step 2c: Update Each File (D-11, D-11b)

    For each file path in `docs_to_update`:

    1. Read the current file content (if file exists; if not, treat as new file)

    2. Determine update strategy based on filename:
       - If filename is `CHANGELOG.md` (case-insensitive):
         **Prepend strategy** — Generate ONLY the new changelog entry for this change.
         The entry should include: date, change name, summary of what was done (from proposal + tasks).
         Use Edit tool to prepend the new entry at the top of the file, below any existing header.
         Do NOT rewrite or modify existing changelog entries.

       - If filename is `README.md` (case-insensitive):
         **Full rewrite strategy** — Read the entire current README.md content.
         Rewrite the full file incorporating information from the archived change.
         Preserve the overall structure and tone. Update sections affected by the change.

       - For any other file:
         **Auto-detect strategy** — Read the file content. Based on the filename and content structure,
         determine the best approach (usually full rewrite). Apply the update.

    3. After updating, show: "Updated: {file_path}"

    ### Step 2d: Summary

    Show: "Updated {N} documentation file(s) based on archived change `{change_name}`."

    ## Step 3: Post-Archive Guidance

    {Keep existing post-archive guidance from current Step 2}
    ```
  </action>
  <verify>
    <automated>grep -c "docs_to_update" "D:/work_data/project/go/mysd/.claude/commands/mysd-archive.md"</automated>
  </verify>
  <acceptance_criteria>
    - mysd-archive.md contains "## Step 0" with "mysd execute --context-only" command
    - mysd-archive.md contains "docs_to_update" extraction from JSON
    - mysd-archive.md contains "## Step 2: Doc Maintenance" section
    - mysd-archive.md Step 2a contains user confirmation flow with Enter/n options
    - mysd-archive.md Step 2a contains auto_mode skip
    - mysd-archive.md Step 2c contains "CHANGELOG.md" with "Prepend strategy"
    - mysd-archive.md Step 2c contains "README.md" with "Full rewrite strategy"
    - mysd-archive.md Step 2c contains "Auto-detect strategy" for other files
    - mysd-archive.md Step 2b reads proposal.md + tasks.md + specs/ as context
    - mysd-archive.md allowed-tools includes Write and Edit
    - mysd-archive.md argument-hint contains "--auto"
    - mysd-archive.md still contains all original error handling for archive failures
  </acceptance_criteria>
  <done>Archive SKILL.md reads docs_to_update config, confirms with user, updates each file with appropriate strategy after successful archive.</done>
</task>

<task type="auto">
  <name>Task 2: Add inline auto-verify + docs_to_update to ff.md and ffe.md</name>
  <files>.claude/commands/mysd-ff.md, .claude/commands/mysd-ffe.md</files>
  <read_first>
    - .claude/commands/mysd-ff.md (current 5-step flow — insert auto-verify between Step 3 execute and Step 4 archive, add docs after archive)
    - .claude/commands/mysd-ffe.md (current 6-step flow — same pattern)
    - .claude/commands/mysd-apply.md (Step 5 auto-verify pattern to replicate inline)
    - .claude/commands/mysd-archive.md (Task 1 output — Step 2 doc maintenance pattern to replicate inline)
  </read_first>
  <action>
    **Edit `.claude/commands/mysd-ff.md`:**

    Current structure: Step 1 (Get Change) → Step 2 (Plan) → Step 3 (Apply) → Step 4 (Archive) → Step 5 (Confirm)

    New structure: Step 1 → Step 2 → Step 3 → **Step 4 (Auto-Verify)** → Step 5 (Archive) → **Step 6 (Docs Update)** → Step 7 (Confirm)

    Insert after Step 3 (Apply Phase), before the archive step:

    ```markdown
    ## Step 4: Inline Auto-Verify (D-17a)

    Run build and test (auto_mode=true, no confirmation):
    ```
    go build ./...
    ```
    If build fails: display error, show "Build failed. Archive skipped. Run `/mysd:fix` to fix." STOP.

    ```
    go test ./...
    ```
    If tests fail: display error, show "Tests failed. Archive skipped. Run `/mysd:fix` to fix." STOP.

    If both pass, invoke verifier:
    ```
    mysd execute --context-only
    ```
    Parse JSON for must_items, should_items, may_items.

    Use Task tool to invoke `mysd-verifier`:
    ```
    Task: Verify spec coverage for {change_name} (ff auto-verify)
    Agent: mysd-verifier
    Context: {
      "change_name": "{change_name}",
      "must_items": [...],
      "should_items": [...],
      "may_items": [...]
    }
    ```

    If MUST items fail: display results, show "Verification failed. Archive skipped." STOP.
    If all pass: proceed to archive.
    ```

    Renumber old Step 4 (Archive) to Step 5.

    After Step 5 (Archive), add:

    ```markdown
    ## Step 6: Inline Docs Update (D-17b)

    Read docs_to_update config:
    ```
    mysd execute --context-only
    ```
    Parse JSON, extract `docs_to_update`.

    If `docs_to_update` is null or empty: skip to Step 7.

    For each file in `docs_to_update`:
    1. Read `.specs/archive/{change_name}/proposal.md`, `tasks.md`, and `specs/` as context
    2. Read current file content
    3. Apply update strategy:
       - `CHANGELOG.md`: prepend new entry only
       - `README.md`: full rewrite
       - Others: auto-detect from content
    4. Show: "Updated: {file_path}"

    No user confirmation (auto_mode=true always in ff).
    ```

    Renumber old Step 5 (Confirm) to Step 7. Update confirm text to mention verification and docs.

    ---

    **Edit `.claude/commands/mysd-ffe.md`:**

    Current structure: Step 1 → Step 2 (Research) → Step 3 (Plan) → Step 4 (Apply) → Step 5 (Archive) → Step 6 (Confirm)

    New structure: Step 1 → Step 2 → Step 3 → Step 4 → **Step 5 (Auto-Verify)** → Step 6 (Archive) → **Step 7 (Docs Update)** → Step 8 (Confirm)

    Apply the same auto-verify and docs-update patterns as ff.md above:
    - Insert Step 5 (Auto-Verify) after Step 4 (Apply), identical to ff Step 4
    - Renumber old Step 5 (Archive) to Step 6
    - Insert Step 7 (Docs Update) after Step 6, identical to ff Step 6
    - Renumber old Step 6 (Confirm) to Step 8
  </action>
  <verify>
    <automated>grep -c "Auto-Verify" "D:/work_data/project/go/mysd/.claude/commands/mysd-ff.md" &amp;&amp; grep -c "docs_to_update" "D:/work_data/project/go/mysd/.claude/commands/mysd-ff.md" &amp;&amp; grep -c "Auto-Verify" "D:/work_data/project/go/mysd/.claude/commands/mysd-ffe.md" &amp;&amp; grep -c "docs_to_update" "D:/work_data/project/go/mysd/.claude/commands/mysd-ffe.md"</automated>
  </verify>
  <acceptance_criteria>
    - mysd-ff.md contains "## Step 4: Inline Auto-Verify" with go build, go test, mysd-verifier
    - mysd-ff.md contains "## Step 6: Inline Docs Update" with docs_to_update logic
    - mysd-ff.md has 7 steps total (was 5)
    - mysd-ff.md Step 4 contains build-fail early exit ("Archive skipped")
    - mysd-ff.md Step 6 contains CHANGELOG.md prepend and README.md rewrite strategies
    - mysd-ff.md Step 6 reads proposal.md + tasks.md + specs/ as context
    - mysd-ffe.md contains "## Step 5: Inline Auto-Verify" with same pattern
    - mysd-ffe.md contains "## Step 7: Inline Docs Update" with same pattern
    - mysd-ffe.md has 8 steps total (was 6)
    - Both ff and ffe contain "mysd execute --context-only" for reading docs_to_update
    - Both ff and ffe contain "No user confirmation" note for docs update
  </acceptance_criteria>
  <done>ff and ffe pipelines include inline auto-verify (build+test+verifier) before archive and inline docs_to_update after archive. Both operate in auto_mode without confirmation prompts.</done>
</task>

</tasks>

<verification>
1. mysd-archive.md has Step 0 (config read) → Step 1 (archive) → Step 2 (doc maintenance) → Step 3 (guidance)
2. mysd-ff.md has 7 steps with auto-verify and docs update
3. mysd-ffe.md has 8 steps with auto-verify and docs update
4. All three files reference `mysd execute --context-only` for docs_to_update
5. All files have valid YAML frontmatter
</verification>

<success_criteria>
- Archive reads and acts on docs_to_update config
- ff/ffe include inline verify and doc update
- File-type-specific update strategies implemented (CHANGELOG prepend, README rewrite)
- Auto mode skips confirmations in all three files
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-doc/11-04-SUMMARY.md`
</output>
