---
plan: "11-05"
title: "Plugin sync + mysd-docs SKILL.md"
phase: 11
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04"]
files_modified:
  - .claude/commands/mysd-docs.md
  - plugin/commands/mysd-docs.md
  - plugin/commands/mysd-lang.md
  - plugin/commands/mysd-model.md
  - plugin/commands/mysd-propose.md
  - plugin/commands/mysd-apply.md
  - plugin/commands/mysd-archive.md
  - plugin/commands/mysd-ff.md
  - plugin/commands/mysd-ffe.md
  - plugin/commands/mysd-fix.md
  - plugin/agents/mysd-executor.md
autonomous: true
requirements:
  - D-15
  - D-16
must_haves:
  truths:
    - "/mysd:docs lists, adds, and removes docs_to_update entries"
    - "All mysd-*.md commands in plugin/commands/ match .claude/commands/"
    - "All mysd-*.md agents in plugin/agents/ match .claude/agents/"
    - "mysd-lang.md and mysd-model.md exist in plugin/commands/"
    - "mysd-docs.md exists in plugin/commands/"
  artifacts:
    - path: ".claude/commands/mysd-docs.md"
      provides: "Thin SKILL.md wrapper for mysd docs command"
      contains: "mysd docs"
    - path: "plugin/commands/mysd-docs.md"
      provides: "Distribution copy of mysd-docs SKILL.md"
      contains: "mysd docs"
    - path: "plugin/commands/mysd-lang.md"
      provides: "Distribution copy of mysd-lang SKILL.md (was missing)"
      contains: "mysd lang"
    - path: "plugin/commands/mysd-model.md"
      provides: "Distribution copy of mysd-model SKILL.md (was missing)"
      contains: "mysd model"
  key_links:
    - from: "plugin/commands/mysd-*.md"
      to: ".claude/commands/mysd-*.md"
      via: "file content identity"
      pattern: "identical content"
    - from: "plugin/agents/mysd-*.md"
      to: ".claude/agents/mysd-*.md"
      via: "file content identity (minus model line)"
      pattern: "identical content"
---

<objective>
Create the mysd-docs.md SKILL.md wrapper and sync all modified SKILL.md and agent files to plugin/ distribution directory. Close the Phase 9-04 plugin sync gap for missing mysd-lang.md and mysd-model.md.

Purpose: Ensure plugin distribution matches dev copies. Users installing mysd get all commands and agents.
Output: New mysd-docs.md SKILL.md + full plugin sync with diff verification.
</objective>

<execution_context>
@D:/work_data/project/go/mysd/.claude/get-shit-done/workflows/execute-plan.md
@D:/work_data/project/go/mysd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-agent-doc/11-CONTEXT.md

<interfaces>
<!-- Reference: mysd-note.md thin wrapper pattern -->

From .claude/commands/mysd-note.md:
```yaml
---
model: claude-sonnet-4-5
description: Manage deferred notes (ideas for future changes). Usage: /mysd:note [add {content} | delete {id}]
argument-hint: "[add {content} | delete {id}]"
allowed-tools:
  - Bash
  - Read
---
```
- Step 1: Parse arguments (no args = list, "add" = add mode, "delete" = delete mode)
- Step 2: Execute binary command
- Step 3: Context hint

Plugin sync pattern:
- .claude/commands/ is authoritative dev copy
- plugin/commands/ is distribution copy (identical content)
- .claude/agents/ is authoritative dev copy
- plugin/agents/ is distribution copy (identical content except model line may be absent in some)
- Only sync mysd-*.md files (not gsd-*.md, not CLAUDE.md, not subdirectories)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mysd-docs.md SKILL.md wrapper</name>
  <files>.claude/commands/mysd-docs.md</files>
  <read_first>
    - .claude/commands/mysd-note.md (reference pattern — thin wrapper with Bash+Read only, no Task tool)
    - cmd/docs.go (if exists from Plan 01 — verify subcommand names: list/add/remove)
  </read_first>
  <action>
    Create `.claude/commands/mysd-docs.md` following the mysd-note.md thin wrapper pattern:

    ```markdown
    ---
    model: claude-sonnet-4-5
    description: Manage docs_to_update list (files updated after archive). Usage: /mysd:docs [add <path> | remove <path>]
    argument-hint: "[add <path> | remove <path>]"
    allowed-tools:
      - Bash
      - Read
    ---

    # /mysd:docs -- Manage Documentation Update List

    You are the mysd docs assistant. Your job is to manage the docs_to_update configuration — the list of files that get automatically updated after each `/mysd:archive`.

    ## Step 1: Parse Arguments

    Check `$ARGUMENTS`:
    - No arguments → list mode
    - First word is "add" → add mode, second word is the file path
    - First word is "remove" → remove mode, second word is the file path

    ## Step 2: Execute Command

    **List mode:**
    Run: `mysd docs`
    Display the output. If no entries exist, show:
    "No files configured for post-archive updates. Use `mysd docs add <path>` to add files like README.md or CHANGELOG.md."

    **Add mode:**
    Run: `mysd docs add {path}`
    Show confirmation.

    **Remove mode:**
    Run: `mysd docs remove {path}`
    Show confirmation or error if path not found.

    ## Step 3: Context Hint

    After any operation, show:
    "Files in docs_to_update are automatically updated with change context after `/mysd:archive`. Manage the list with `/mysd:docs`."
    ```
  </action>
  <verify>
    <automated>grep -c "mysd docs" "D:/work_data/project/go/mysd/.claude/commands/mysd-docs.md"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/mysd-docs.md exists
    - File has valid YAML frontmatter with model, description, argument-hint, allowed-tools
    - allowed-tools contains only Bash and Read (thin wrapper, no Task tool)
    - Contains 3 steps: Parse Arguments, Execute Command, Context Hint
    - Step 2 contains `mysd docs`, `mysd docs add`, `mysd docs remove` commands
    - argument-hint is "[add <path> | remove <path>]"
  </acceptance_criteria>
  <done>mysd-docs.md SKILL.md wrapper created following thin wrapper pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Sync all modified files to plugin/ distribution</name>
  <files>plugin/commands/mysd-docs.md, plugin/commands/mysd-lang.md, plugin/commands/mysd-model.md, plugin/commands/mysd-propose.md, plugin/commands/mysd-apply.md, plugin/commands/mysd-archive.md, plugin/commands/mysd-ff.md, plugin/commands/mysd-ffe.md, plugin/commands/mysd-fix.md, plugin/agents/mysd-executor.md</files>
  <read_first>
    - .claude/commands/mysd-docs.md (Task 1 output — copy to plugin)
    - .claude/commands/mysd-lang.md (missing from plugin/commands/ — needs copy)
    - .claude/commands/mysd-model.md (missing from plugin/commands/ — needs copy)
    - .claude/commands/mysd-propose.md (modified in Plan 02 — needs sync)
    - .claude/commands/mysd-apply.md (modified in Plan 02 — needs sync)
    - .claude/commands/mysd-archive.md (modified in Plan 04 — needs sync)
    - .claude/commands/mysd-ff.md (modified in Plan 04 — needs sync)
    - .claude/commands/mysd-ffe.md (modified in Plan 04 — needs sync)
    - .claude/commands/mysd-fix.md (modified in Plan 03 — needs sync)
    - .claude/agents/mysd-executor.md (modified in Plan 03 — needs sync)
    - plugin/agents/mysd-designer.md (check existing diff — only model line difference is expected)
  </read_first>
  <action>
    Sync all modified mysd-*.md files from .claude/ to plugin/:

    **New files to create (copy from .claude/):**
    1. Copy `.claude/commands/mysd-docs.md` → `plugin/commands/mysd-docs.md`
    2. Copy `.claude/commands/mysd-lang.md` → `plugin/commands/mysd-lang.md`
    3. Copy `.claude/commands/mysd-model.md` → `plugin/commands/mysd-model.md`

    **Modified files to sync (copy from .claude/):**
    4. Copy `.claude/commands/mysd-propose.md` → `plugin/commands/mysd-propose.md`
    5. Copy `.claude/commands/mysd-apply.md` → `plugin/commands/mysd-apply.md`
    6. Copy `.claude/commands/mysd-archive.md` → `plugin/commands/mysd-archive.md`
    7. Copy `.claude/commands/mysd-ff.md` → `plugin/commands/mysd-ff.md`
    8. Copy `.claude/commands/mysd-ffe.md` → `plugin/commands/mysd-ffe.md`
    9. Copy `.claude/commands/mysd-fix.md` → `plugin/commands/mysd-fix.md`

    **Agent sync:**
    10. Copy `.claude/agents/mysd-executor.md` → `plugin/agents/mysd-executor.md`

    For each copy: read the source file, write to destination using Write tool (ensure exact content match).

    **Verification (D-16):** After all copies, run diff on each pair to confirm zero diff:
    ```bash
    diff .claude/commands/mysd-docs.md plugin/commands/mysd-docs.md
    diff .claude/commands/mysd-lang.md plugin/commands/mysd-lang.md
    diff .claude/commands/mysd-model.md plugin/commands/mysd-model.md
    diff .claude/commands/mysd-propose.md plugin/commands/mysd-propose.md
    diff .claude/commands/mysd-apply.md plugin/commands/mysd-apply.md
    diff .claude/commands/mysd-archive.md plugin/commands/mysd-archive.md
    diff .claude/commands/mysd-ff.md plugin/commands/mysd-ff.md
    diff .claude/commands/mysd-ffe.md plugin/commands/mysd-ffe.md
    diff .claude/commands/mysd-fix.md plugin/commands/mysd-fix.md
    diff .claude/agents/mysd-executor.md plugin/agents/mysd-executor.md
    ```
    ALL diffs must be empty (zero output).

    **Also verify no mysd-*.md files are missing from plugin/:**
    ```bash
    diff <(ls .claude/commands/ | grep "^mysd-" | sort) <(ls plugin/commands/ | grep "^mysd-" | sort)
    diff <(ls .claude/agents/ | grep "^mysd-" | sort) <(ls plugin/agents/ | grep "^mysd-" | sort)
    ```
    Command diff should be empty (all mysd commands present in both).
    Agent diff may show model line differences (expected) — content should otherwise match.
  </action>
  <verify>
    <automated>diff "D:/work_data/project/go/mysd/.claude/commands/mysd-docs.md" "D:/work_data/project/go/mysd/plugin/commands/mysd-docs.md" &amp;&amp; diff "D:/work_data/project/go/mysd/.claude/commands/mysd-lang.md" "D:/work_data/project/go/mysd/plugin/commands/mysd-lang.md" &amp;&amp; diff "D:/work_data/project/go/mysd/.claude/commands/mysd-model.md" "D:/work_data/project/go/mysd/plugin/commands/mysd-model.md" &amp;&amp; echo "All synced"</automated>
  </verify>
  <acceptance_criteria>
    - plugin/commands/mysd-docs.md exists and is identical to .claude/commands/mysd-docs.md
    - plugin/commands/mysd-lang.md exists and is identical to .claude/commands/mysd-lang.md
    - plugin/commands/mysd-model.md exists and is identical to .claude/commands/mysd-model.md
    - plugin/commands/mysd-propose.md is identical to .claude/commands/mysd-propose.md
    - plugin/commands/mysd-apply.md is identical to .claude/commands/mysd-apply.md
    - plugin/commands/mysd-archive.md is identical to .claude/commands/mysd-archive.md
    - plugin/commands/mysd-ff.md is identical to .claude/commands/mysd-ff.md
    - plugin/commands/mysd-ffe.md is identical to .claude/commands/mysd-ffe.md
    - plugin/commands/mysd-fix.md is identical to .claude/commands/mysd-fix.md
    - plugin/agents/mysd-executor.md is identical to .claude/agents/mysd-executor.md
    - `diff <(ls .claude/commands/ | grep "^mysd-" | sort) <(ls plugin/commands/ | grep "^mysd-" | sort)` produces empty output
  </acceptance_criteria>
  <done>All modified mysd-*.md files synced to plugin/. Previously missing mysd-lang.md, mysd-model.md, and new mysd-docs.md added. Diff verification passes.</done>
</task>

</tasks>

<verification>
1. mysd-docs.md exists in both .claude/commands/ and plugin/commands/
2. All mysd-*.md commands present in both .claude/commands/ and plugin/commands/ (zero diff on file listing)
3. All mysd-*.md agents present in both .claude/agents/ and plugin/agents/
4. Content diff on all synced files produces zero output
5. `go build ./...` still compiles (no Go changes but verify binary not broken)
</verification>

<success_criteria>
- mysd-docs SKILL.md created as thin wrapper
- All modified files synced to plugin/ distribution
- Missing mysd-lang.md and mysd-model.md added to plugin/commands/
- Diff verification confirms exact content match
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-doc/11-05-SUMMARY.md`
</output>
