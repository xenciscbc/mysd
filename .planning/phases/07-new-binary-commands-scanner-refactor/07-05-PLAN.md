---
phase: 07-new-binary-commands-scanner-refactor
plan: 05
type: execute
wave: 2
depends_on:
  - 07-02
  - 07-03
  - 07-04
files_modified:
  - .claude/commands/mysd-scan.md
  - .claude/commands/mysd-init.md
  - .claude/commands/mysd-model.md
  - .claude/commands/mysd-lang.md
  - .claude/agents/mysd-planner.md
autonomous: false
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
must_haves:
  truths:
    - "mysd-scan.md references new JSON fields (primary_language, files, modules) not old fields (packages, go_files)"
    - "mysd-init.md instructs running mysd init then mysd lang set for locale setup"
    - "mysd-model.md SKILL.md exists for /mysd:model command"
    - "mysd-lang.md SKILL.md exists for /mysd:lang command"
    - "mysd-planner.md includes skills recommendation logic per D-07"
    - "Plan completion flow shows task-skills table and asks Accept all? Y/n per D-08, D-09"
    - "ffe mode skips skills confirmation per D-10"
  artifacts:
    - path: ".claude/commands/mysd-scan.md"
      provides: "Updated SKILL.md for language-agnostic scan"
      contains: "primary_language"
    - path: ".claude/commands/mysd-init.md"
      provides: "Updated SKILL.md for scaffold-only init"
      contains: "scaffold"
    - path: ".claude/commands/mysd-model.md"
      provides: "New SKILL.md for model command"
      contains: "mysd model"
    - path: ".claude/commands/mysd-lang.md"
      provides: "New SKILL.md for lang command"
      contains: "mysd lang"
    - path: ".claude/agents/mysd-planner.md"
      provides: "Updated planner agent with skills recommendation"
      contains: "skills"
  key_links:
    - from: ".claude/commands/mysd-scan.md"
      to: "cmd/scan.go"
      via: "mysd scan --context-only invocation"
      pattern: "mysd scan --context-only"
    - from: ".claude/agents/mysd-planner.md"
      to: ".claude/commands/mysd-plan.md"
      via: "Skills recommendation integrated into plan workflow"
      pattern: "skills"
---

<objective>
Update SKILL.md files and agent definitions for new commands and scanner changes.

Purpose: The binary changes from Plans 02-04 need corresponding SKILL.md orchestrators and agent updates. New /mysd:model and /mysd:lang commands need SKILL.md files. The scan SKILL.md must reference the new JSON format. The planner agent must include skills recommendation logic (SKILL-01 through SKILL-04, D-07 through D-10).
Output: 2 new SKILL.md files (model, lang), 2 updated SKILL.md files (scan, init), 1 updated agent definition (planner).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-new-binary-commands-scanner-refactor/07-02-SUMMARY.md
@.planning/phases/07-new-binary-commands-scanner-refactor/07-03-SUMMARY.md
@.planning/phases/07-new-binary-commands-scanner-refactor/07-04-SUMMARY.md
@.claude/commands/mysd-scan.md
@.claude/commands/mysd-init.md
@.claude/agents/mysd-planner.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mysd-scan.md and mysd-init.md SKILL.md files</name>
  <files>.claude/commands/mysd-scan.md, .claude/commands/mysd-init.md</files>
  <read_first>
    - .claude/commands/mysd-scan.md (current scan SKILL.md to update)
    - .claude/commands/mysd-init.md (current init SKILL.md to update)
    - .claude/agents/mysd-scanner.md (scanner agent invoked by scan SKILL.md)
  </read_first>
  <action>
    **Update `.claude/commands/mysd-scan.md`:**

    Replace Step 1 JSON field documentation to match new ScanContext (per D-02):

    OLD fields to remove: `packages` (array of PackageInfo with `name`, `dir`, `go_files`, `test_files`, `has_spec`)

    NEW fields to document:
    ```
    - `root_dir`: The project root directory
    - `primary_language`: Detected language ("go", "nodejs", "python", "unknown")
    - `files`: Map of file extension to count, e.g. {".go": 42, ".ts": 10}
    - `modules`: Array of discovered modules, each with:
      - `name`: Module/package name
      - `dir`: Relative directory path
    - `existing_specs`: Array of spec names that already exist
    - `excluded_dirs`: Array of directories excluded from scan
    - `total_files`: Total files found
    - `config_exists`: Boolean — true if openspec/config.yaml already exists
    ```

    Update Step 2 presentation format:
    ```
    Scan complete. Found {total_files} files in {root_dir}.
    Primary language: {primary_language}

    File types:
      .go: 42 files
      .md: 10 files
      ...

    Modules to scan (no spec yet):
      - {module.name} ({module.dir})

    Already have specs (will be skipped):
      - {spec_name}
    ```

    Update Step 3: Change from "for EACH confirmed package" to "for EACH confirmed module". Update Task context to pass `module_name`, `module_dir` instead of `package_name`, `package_dir`, `go_files`.

    Add locale setup: If `config_exists` is false, after spec generation, prompt: "No openspec config found. Set language with /mysd:lang" (per D-06, FSCAN-04).

    **Update `.claude/commands/mysd-init.md`:**

    Complete rewrite (per D-05). New content:

    Frontmatter: model: claude-sonnet-4-5, description: "Initialize mysd project structure (scaffold + locale setup).", allowed-tools: [Bash, Read]

    Title: /mysd:init -- Initialize mysd Project

    Step 1: Run `mysd init` -- creates .claude/mysd.yaml + openspec/ + openspec/specs/

    Step 2: Ask user language preference ("What language should mysd use? e.g., zh-TW, en-US, ja-JP"), then run `mysd lang set {user_choice}` to atomically set both configs (per D-06, FSCAN-04)

    Step 3: Confirm with "Project initialized. Run /mysd:scan to discover your codebase and generate specs."
  </action>
  <verify>
    <automated>grep -c "primary_language" .claude/commands/mysd-scan.md</automated>
  </verify>
  <acceptance_criteria>
    - mysd-scan.md contains "primary_language" (new field reference)
    - mysd-scan.md does NOT contain "go_files" or "test_files" or "PackageInfo" (old field references removed)
    - mysd-scan.md contains "modules" and "config_exists"
    - mysd-init.md contains "mysd lang set" (locale setup delegation)
    - mysd-init.md does NOT contain "model_profile" or "execution_mode" config field explanations (old init behavior removed)
    - mysd-init.md contains "openspec/" in scaffold description
  </acceptance_criteria>
  <done>Scan SKILL.md references new JSON format, init SKILL.md delegates to scaffold + lang set</done>
</task>

<task type="auto">
  <name>Task 2: Create mysd-model.md and mysd-lang.md SKILL.md files</name>
  <files>.claude/commands/mysd-model.md, .claude/commands/mysd-lang.md</files>
  <read_first>
    - .claude/commands/mysd-init.md (SKILL.md format reference)
    - .claude/commands/mysd-scan.md (SKILL.md format reference)
  </read_first>
  <action>
    **Create `.claude/commands/mysd-model.md`:**

    Frontmatter: model: claude-sonnet-4-5, description: "Display or set the AI model profile for mysd agent roles.", allowed-tools: [Bash, Read]

    Title: /mysd:model -- Model Profile Management

    Step 1 (Show Current Profile): Run `mysd model`. Display output to user as-is. Shows Profile: {name} header + Role/Model table with all 10 agent roles.

    Step 2 (Offer to Change): If user wants to change, explain profiles -- quality (all sonnet, best quality), balanced (mixed, default), budget (core sonnet, others haiku). Then run `mysd model set {profile}`.

    Step 3 (Confirm): Run `mysd model` again to show updated profile.

    **Create `.claude/commands/mysd-lang.md`:**

    Frontmatter: model: claude-sonnet-4-5, description: "Set response language and OpenSpec locale for the project.", allowed-tools: [Bash, Read]

    Title: /mysd:lang -- Language Settings

    Step 1 (Show Current Settings): Run `mysd lang`. Displays response_language and locale.

    Step 2 (Set Language): Present options -- 1. zh-TW, 2. en-US, 3. ja-JP, 4. Custom BCP47 code. Ask "Select a language or enter a BCP47 code:". Then run `mysd lang set {locale}`. Atomically updates both configs.

    Step 3 (Confirm): Run `mysd lang` again to show updated settings.
  </action>
  <verify>
    <automated>test -f .claude/commands/mysd-model.md && test -f .claude/commands/mysd-lang.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/mysd-model.md exists and contains "mysd model" and "mysd model set"
    - .claude/commands/mysd-model.md frontmatter has `model: claude-sonnet-4-5` and `allowed-tools` with Bash, Read
    - .claude/commands/mysd-lang.md exists and contains "mysd lang" and "mysd lang set"
    - .claude/commands/mysd-lang.md contains "BCP47" reference
    - .claude/commands/mysd-lang.md frontmatter has `model: claude-sonnet-4-5` and `allowed-tools` with Bash, Read
  </acceptance_criteria>
  <done>New SKILL.md files created for /mysd:model and /mysd:lang commands</done>
</task>

<task type="auto">
  <name>Task 3: Update mysd-planner agent with skills recommendation and confirmation flow</name>
  <files>.claude/agents/mysd-planner.md</files>
  <read_first>
    - .claude/agents/mysd-planner.md (current planner agent to extend)
    - .claude/commands/mysd-plan.md (plan SKILL.md that invokes planner agent)
  </read_first>
  <action>
    **Update `.claude/agents/mysd-planner.md`** to add skills recommendation (per D-07, D-08, D-09, D-10):

    Add a new **Step 4.5: Recommend Skills** between current Step 4 (Write tasks.md) and Step 5 (Check Phase):

    Content: For each task, recommend appropriate `/mysd:*` skills based on task content. Heuristics: spec artifacts -> /mysd:propose or /mysd:spec; design/architecture -> /mysd:design; code implementation -> no skill; testing/verification -> /mysd:verify; scanning -> /mysd:scan; capturing -> /mysd:capture. Write skills into each task's `skills` field in tasks.md YAML. Empty array `[]` if no recommendation.

    Also add **Step 7.5: Skills Confirmation** after Step 7 (Transition State), before Step 8 (Confirm):

    Content: If `auto_mode` is false (not ffe mode), present task-skills mapping table to user, then ask "Accept all recommended skills? (Y/n)". Default accept (Enter = Y, per D-09). If n, present each task individually for adjustment ("T1 [/mysd:design] -- change to: (press Enter to keep)"). If `auto_mode` is true (ffe mode per D-10), skip confirmation entirely, use recommended skills as-is.
  </action>
  <verify>
    <automated>grep -c "skills" .claude/agents/mysd-planner.md</automated>
  </verify>
  <acceptance_criteria>
    - mysd-planner.md contains "Step 4.5: Recommend Skills" section
    - mysd-planner.md contains "Step 7.5: Skills Confirmation" section
    - mysd-planner.md contains "Accept all recommended skills? (Y/n)" prompt text
    - mysd-planner.md contains "auto_mode" check for ffe mode skip
    - mysd-planner.md contains `skills` field reference in YAML example
  </acceptance_criteria>
  <done>Planner agent recommends skills per task and supports interactive/batch confirmation with ffe skip</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify complete Phase 7 output</name>
  <files>N/A</files>
  <action>
    Human verifies the complete Phase 7 output:
    1. FilterBlockedTasks in executor (Plan 01)
    2. Language-agnostic scanner with scaffold-only mode (Plan 02)
    3. mysd model read/set commands (Plan 03)
    4. mysd lang read/set with atomic dual-config write (Plan 04)
    5. SKILL.md files for scan, init, model, lang + planner skills recommendation (Plan 05)

    Verification steps:
    1. Run `go test ./...` -- all tests should pass
    2. Run `go build -o mysd.exe ./...` -- binary compiles
    3. Run `./mysd.exe model` -- should display Profile: balanced + 10 role mappings
    4. Run `./mysd.exe scan --context-only` -- should output JSON with primary_language, files, modules fields
    5. Verify `.claude/commands/mysd-model.md` and `.claude/commands/mysd-lang.md` exist
    6. Verify `.claude/agents/mysd-planner.md` contains "Recommend Skills" section
  </action>
  <verify>
    <automated>go test ./... && go build ./...</automated>
  </verify>
  <done>All Phase 7 artifacts verified: tests pass, binary compiles, SKILL.md files complete</done>
</task>

</tasks>

<verification>
go test ./...
go build ./...
grep "primary_language" .claude/commands/mysd-scan.md
grep "skills" .claude/agents/mysd-planner.md
test -f .claude/commands/mysd-model.md
test -f .claude/commands/mysd-lang.md
</verification>

<success_criteria>
- All SKILL.md files reference correct binary commands and JSON formats
- mysd-scan.md uses new field names (primary_language, files, modules)
- mysd-init.md delegates to scaffold + lang set
- mysd-model.md and mysd-lang.md are new, complete SKILL.md files
- mysd-planner.md includes skills recommendation and confirmation flow
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-new-binary-commands-scanner-refactor/07-05-SUMMARY.md`
</output>
