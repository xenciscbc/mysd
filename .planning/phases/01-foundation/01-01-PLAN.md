---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - main.go
  - internal/spec/schema.go
  - internal/spec/parser.go
  - internal/spec/writer.go
  - internal/spec/delta.go
  - internal/spec/detector.go
  - internal/spec/schema_test.go
  - internal/spec/parser_test.go
  - internal/spec/writer_test.go
  - internal/spec/delta_test.go
  - internal/spec/detector_test.go
  - testdata/fixtures/openspec-project/changes/sample-change/.openspec.yaml
  - testdata/fixtures/openspec-project/changes/sample-change/proposal.md
  - testdata/fixtures/openspec-project/changes/sample-change/specs/user-auth/spec.md
  - testdata/fixtures/openspec-project/changes/sample-change/design.md
  - testdata/fixtures/openspec-project/changes/sample-change/tasks.md
  - testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/.openspec.yaml
  - testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/proposal.md
  - testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/specs/theme-support/spec.md
  - testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/design.md
  - testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/tasks.md
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04
  - SPEC-07
  - OPSX-01
  - OPSX-02
  - OPSX-03
  - OPSX-04
  - DIST-01
  - DIST-02

must_haves:
  truths:
    - "Go module initializes and compiles on Windows, macOS, Linux"
    - "Parser can read OpenSpec brownfield project (no frontmatter) and produce typed Go structs"
    - "Parser can read my-ssd format (with frontmatter) and produce typed Go structs"
    - "Writer can scaffold a complete change directory with proposal.md, specs/, design.md, tasks.md"
    - "RFC 2119 keywords (MUST/SHOULD/MAY) are correctly identified case-sensitively"
    - "Delta ops (ADDED/MODIFIED/REMOVED) are correctly identified from spec headings"
    - "Detector finds .specs/ or openspec/ directory automatically"
  artifacts:
    - path: "go.mod"
      provides: "Go module definition"
      contains: "module github.com/mysd"
    - path: "internal/spec/schema.go"
      provides: "All typed Go structs for spec domain"
      exports: ["Change", "Requirement", "Task", "ProposalDoc", "DesignDoc", "ChangeMeta", "RFC2119Keyword", "DeltaOp", "ItemStatus"]
    - path: "internal/spec/parser.go"
      provides: "Spec file parsing with frontmatter + RFC 2119 extraction"
      exports: ["ParseChange", "ParseRequirement", "extractKeyword"]
    - path: "internal/spec/writer.go"
      provides: "Scaffold new change directory structure"
      exports: ["Scaffold"]
    - path: "internal/spec/delta.go"
      provides: "Delta spec operation identification"
      exports: ["ParseDelta", "DetectDeltaOp"]
    - path: "internal/spec/detector.go"
      provides: "Auto-detect .specs/ vs openspec/ directory"
      exports: ["DetectSpecDir", "SpecDirFlavor"]
  key_links:
    - from: "internal/spec/parser.go"
      to: "internal/spec/schema.go"
      via: "imports and populates typed structs"
      pattern: "spec\\.Change|spec\\.Requirement"
    - from: "internal/spec/parser.go"
      to: "adrg/frontmatter"
      via: "frontmatter.Parse for YAML extraction"
      pattern: "frontmatter\\.Parse"
    - from: "internal/spec/writer.go"
      to: "internal/spec/schema.go"
      via: "uses Change struct to generate files"
      pattern: "spec\\.Change"
---

<objective>
Go module 初始化 + Spec 資料模型 + 完整的 spec parser/writer/delta/detector 實作

Purpose: 建立 my-ssd 的核心資料層 — 所有後續功能（state machine、CLI commands）都依賴這些 typed Go structs 和 parser。
Output: 可編譯的 Go module，包含 `internal/spec/` 下的完整 spec 處理能力，以及測試 fixtures。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Go module init + spec schema types + test fixtures</name>
  <files>go.mod, main.go, internal/spec/schema.go, internal/spec/schema_test.go, testdata/fixtures/openspec-project/changes/sample-change/.openspec.yaml, testdata/fixtures/openspec-project/changes/sample-change/proposal.md, testdata/fixtures/openspec-project/changes/sample-change/specs/user-auth/spec.md, testdata/fixtures/openspec-project/changes/sample-change/design.md, testdata/fixtures/openspec-project/changes/sample-change/tasks.md, testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/.openspec.yaml, testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/proposal.md, testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/specs/theme-support/spec.md, testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/design.md, testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/tasks.md</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: schema structs, Pattern 4: OpenSpec format)
  </read_first>
  <behavior>
    - RFC2119Keyword constants: Must == "MUST", Should == "SHOULD", May == "MAY"
    - DeltaOp constants: DeltaAdded == "ADDED", DeltaModified == "MODIFIED", DeltaRemoved == "REMOVED", DeltaNone == ""
    - ItemStatus constants: StatusPending == "pending", StatusInProgress == "in_progress", StatusDone == "done", StatusBlocked == "blocked"
    - Change struct has fields: Name, Dir, Proposal, Specs, Design, Tasks, Meta
    - Requirement struct has fields: ID, Text, Keyword, DeltaOp, Status
    - ProposalDoc has fields: SpecVersion, ChangeName, Status, Created, Updated, Body
    - ChangeMeta has fields: Schema, Created
    - SpecDirFlavor constants: FlavorMySD, FlavorOpenSpec, FlavorNone
  </behavior>
  <action>
    1. Initialize Go module:
       - `go mod init github.com/mysd` (per D-05 command style, module name matches binary name)
       - Create `main.go` with `package main` and `func main() {}` placeholder (will be wired in Plan 03)

    2. Create `internal/spec/schema.go` with all typed structs per RESEARCH.md Pattern 2:
       - `package spec`
       - Type `RFC2119Keyword string` with constants `Must = "MUST"`, `Should = "SHOULD"`, `May = "MAY"`
       - Type `DeltaOp string` with constants `DeltaAdded = "ADDED"`, `DeltaModified = "MODIFIED"`, `DeltaRemoved = "REMOVED"`, `DeltaNone = ""`
       - Type `ItemStatus string` with constants `StatusPending = "pending"`, `StatusInProgress = "in_progress"`, `StatusDone = "done"`, `StatusBlocked = "blocked"`
       - Type `SpecDirFlavor string` with constants `FlavorMySD = "mysd"`, `FlavorOpenSpec = "openspec"`, `FlavorNone = "none"`
       - Struct `ChangeMeta` with `Schema string yaml:"schema"`, `Created string yaml:"created"`
       - Struct `ProposalFrontmatter` with `SpecVersion string yaml:"spec-version"`, `ChangeName string yaml:"change"`, `Status string yaml:"status"`, `Created string yaml:"created"`, `Updated string yaml:"updated"`
       - Struct `SpecFrontmatter` with `SpecVersion string yaml:"spec-version"`, `Capability string yaml:"capability"`, `Delta DeltaOp yaml:"delta"`, `Status ItemStatus yaml:"status"`
       - Struct `TasksFrontmatter` with `SpecVersion string yaml:"spec-version"`, `Total int yaml:"total"`, `Completed int yaml:"completed"`
       - Struct `ProposalDoc` with `Frontmatter ProposalFrontmatter`, `Body string`
       - Struct `DesignDoc` with `Body string`
       - Struct `Requirement` with `ID string`, `Text string`, `Keyword RFC2119Keyword`, `DeltaOp DeltaOp`, `Status ItemStatus`
       - Struct `Task` with `ID int`, `Name string`, `Description string`, `Status ItemStatus`
       - Struct `Change` with `Name string`, `Dir string`, `Proposal ProposalDoc`, `Specs []Requirement`, `Design DesignDoc`, `Tasks []Task`, `Meta ChangeMeta`
       - Error variables: `ErrNoSpecDir = errors.New("no spec directory found")`, `ErrInvalidTransition = errors.New("invalid state transition")`

    3. Create `internal/spec/schema_test.go`:
       - Test that all RFC2119Keyword constants have expected string values
       - Test that all DeltaOp constants have expected string values
       - Test that all ItemStatus constants have expected string values

    4. Create test fixtures:
       a. `testdata/fixtures/openspec-project/` — brownfield OpenSpec format (NO frontmatter in specs):
          - `changes/sample-change/.openspec.yaml`: `schema: spec-driven\ncreated: 2026-01-20`
          - `changes/sample-change/proposal.md`: plain Markdown proposal with no frontmatter, containing "## Summary" and description
          - `changes/sample-change/specs/user-auth/spec.md`: BDD format with `## Requirement: User Authentication`, WHEN/THEN/AND blocks, containing `MUST` and `SHOULD` keywords
          - `changes/sample-change/design.md`: plain Markdown design doc
          - `changes/sample-change/tasks.md`: plain Markdown task list with `- [ ] Task 1`, `- [ ] Task 2`

       b. `testdata/fixtures/mysd-project/` — my-ssd native format (WITH frontmatter):
          - `.specs/changes/add-dark-mode/.openspec.yaml`: `schema: spec-driven\ncreated: 2026-03-23`
          - `.specs/changes/add-dark-mode/proposal.md`: with `---\nspec-version: "1"\nchange: add-dark-mode\nstatus: proposed\ncreated: 2026-03-23\nupdated: 2026-03-23\n---` frontmatter + body
          - `.specs/changes/add-dark-mode/specs/theme-support/spec.md`: with `---\nspec-version: "1"\ncapability: theme-support\ndelta: ADDED\nstatus: pending\n---` frontmatter + BDD body containing MUST, SHOULD, and MAY keywords
          - `.specs/changes/add-dark-mode/design.md`: plain Markdown
          - `.specs/changes/add-dark-mode/tasks.md`: with `---\nspec-version: "1"\ntotal: 3\ncompleted: 0\n---` frontmatter + task list

    5. Install dependencies:
       - `go get github.com/spf13/cobra@v1.10.2`
       - `go get gopkg.in/yaml.v3`
       - `go get github.com/adrg/frontmatter@v0.2.0`
       - `go get github.com/spf13/viper@v1`
       - `go get github.com/charmbracelet/lipgloss@v1`
       - `go get github.com/stretchr/testify@v1`
  </action>
  <verify>
    <automated>cd D:\work_data\project\go\mysd &amp;&amp; go test ./internal/spec/ -run TestSchema -v</automated>
  </verify>
  <acceptance_criteria>
    - go.mod contains `module github.com/mysd`
    - go.mod contains `github.com/spf13/cobra`
    - internal/spec/schema.go contains `type RFC2119Keyword string`
    - internal/spec/schema.go contains `type Change struct`
    - internal/spec/schema.go contains `type Requirement struct`
    - internal/spec/schema.go contains `ErrNoSpecDir`
    - internal/spec/schema.go contains `SpecVersion string`
    - testdata/fixtures/openspec-project/changes/sample-change/.openspec.yaml exists
    - testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/proposal.md contains `spec-version: "1"`
    - `go build ./...` exits 0
    - `go test ./internal/spec/ -run TestSchema` exits 0
  </acceptance_criteria>
  <done>Go module compiles, all schema types defined with correct constants, both OpenSpec brownfield and my-ssd native test fixtures exist</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spec parser + detector + delta + writer implementation</name>
  <files>internal/spec/parser.go, internal/spec/detector.go, internal/spec/delta.go, internal/spec/writer.go, internal/spec/parser_test.go, internal/spec/detector_test.go, internal/spec/delta_test.go, internal/spec/writer_test.go</files>
  <read_first>
    internal/spec/schema.go (types to use)
    testdata/fixtures/openspec-project/changes/sample-change/specs/user-auth/spec.md (brownfield format)
    testdata/fixtures/mysd-project/.specs/changes/add-dark-mode/specs/theme-support/spec.md (native format)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: detector, RFC 2119 regex, delta format)
  </read_first>
  <behavior>
    - DetectSpecDir("testdata/fixtures/mysd-project") returns (".specs", FlavorMySD, nil)
    - DetectSpecDir("testdata/fixtures/openspec-project") returns ("openspec", FlavorOpenSpec, nil)
    - DetectSpecDir("/nonexistent") returns ("", FlavorNone, ErrNoSpecDir)
    - extractKeyword("System MUST validate input") returns Must
    - extractKeyword("System SHOULD log events") returns Should
    - extractKeyword("System MAY cache results") returns May
    - extractKeyword("you must not do this") returns "" (lowercase = not RFC 2119)
    - extractKeyword("no keywords here") returns ""
    - ParseChange on brownfield fixture returns Change with Specs populated, no frontmatter panic
    - ParseChange on my-ssd fixture returns Change with frontmatter fields populated
    - DetectDeltaOp("## ADDED Requirements") returns DeltaAdded
    - DetectDeltaOp("## MODIFIED Requirements") returns DeltaModified
    - DetectDeltaOp("## REMOVED Requirements") returns DeltaRemoved
    - DetectDeltaOp("## Requirements") returns DeltaNone
    - Scaffold creates proposal.md, specs/, design.md, tasks.md with correct frontmatter
  </behavior>
  <action>
    1. Create `internal/spec/detector.go` (per D-15):
       - `func DetectSpecDir(root string) (dir string, flavor SpecDirFlavor, err error)`:
         Check `filepath.Join(root, ".specs")` first (FlavorMySD), then `filepath.Join(root, "openspec")` (FlavorOpenSpec), else return ErrNoSpecDir.
       - `func ListChanges(specDir string) ([]string, error)`: List subdirectories under `specDir/changes/`.

    2. Create `internal/spec/parser.go`:
       - Import `github.com/adrg/frontmatter` for YAML frontmatter extraction
       - `var reMust = regexp.MustCompile(`\bMUST\b|\bMUST NOT\b|\bREQUIRED\b|\bSHALL\b|\bSHALL NOT\b`)`
       - `var reShould = regexp.MustCompile(`\bSHOULD\b|\bSHOULD NOT\b|\bRECOMMENDED\b`)`
       - `var reMay = regexp.MustCompile(`\bMAY\b|\bOPTIONAL\b`)`
       - `var reRequirementHeading = regexp.MustCompile(`^#{1,3}\s+Requirement:?\s*(.*)$`)` (avoid hardcoded heading per Pitfall 1)
       - `func extractKeyword(line string) RFC2119Keyword`: Case-sensitive RFC 2119 matching (Pitfall 2)
       - `func ParseProposal(path string) (ProposalDoc, error)`: Read file, extract frontmatter via `frontmatter.Parse(r, &fm)`, return ProposalDoc with Body. If no frontmatter, return zero-value frontmatter + full body (brownfield graceful degradation).
       - `func ParseSpec(path string) ([]Requirement, error)`: Read spec.md, extract frontmatter (optional), scan body for `Requirement:` headings, extract RFC 2119 keywords from requirement text, populate Requirement structs.
       - `func ParseTasks(path string) ([]Task, TasksFrontmatter, error)`: Parse tasks.md, extract frontmatter, parse `- [ ] Task` / `- [x] Task` lines into Task structs.
       - `func ParseChangeMeta(path string) (ChangeMeta, error)`: Parse `.openspec.yaml` via yaml.v3.
       - `func ParseChange(changeDir string) (Change, error)`: Orchestrator function that calls ParseProposal, ParseSpec (for each spec dir), ParseTasks, ParseChangeMeta, and assembles a Change struct.

    3. Create `internal/spec/delta.go` (per OPSX-03, D-17):
       - `var reDeltaHeading = regexp.MustCompile(`^#{1,3}\s+(ADDED|MODIFIED|REMOVED)\b`)`
       - `func DetectDeltaOp(heading string) DeltaOp`: Extract delta operation from heading text.
       - `func ParseDelta(body string) (added []Requirement, modified []Requirement, removed []Requirement)`: Parse delta spec body into categorized requirements.

    4. Create `internal/spec/writer.go` (per SPEC-01, D-12):
       - `func Scaffold(name string, baseDir string) (Change, error)`:
         Create directory structure `{baseDir}/changes/{name}/` with:
         - `.openspec.yaml` with `schema: spec-driven`, `created: {today}`
         - `proposal.md` with frontmatter: `spec-version: "1"`, `change: {name}`, `status: proposed`, `created: {today}`, `updated: {today}` + template body with `## Summary`, `## Motivation`, `## Scope` headings
         - `specs/` empty directory (specs are added by `mysd spec` in Phase 2)
         - `design.md` with placeholder template body (`## Architecture`, `## Key Decisions`)
         - `tasks.md` with frontmatter: `spec-version: "1"`, `total: 0`, `completed: 0` + empty task list template
       - Use `text/template` stdlib for rendering templates.
       - All files use `os.MkdirAll` for directory creation with `0755` permissions.
       - All files written with `os.WriteFile` with `0644` permissions.

    5. Create test files:
       - `detector_test.go`: Test DetectSpecDir with both fixture directories + nonexistent dir
       - `parser_test.go`: Test ParseProposal (brownfield + native), ParseSpec (RFC 2119 extraction), ParseTasks, ParseChange on both fixtures. Include lowercase "must" negative test case per Pitfall 2.
       - `delta_test.go`: Test DetectDeltaOp with all three ops + no-op. Test ParseDelta with multi-section delta body.
       - `writer_test.go`: Test Scaffold creates correct directory structure and file contents in a temp dir. Verify frontmatter contains `spec-version: "1"`.
  </action>
  <verify>
    <automated>cd D:\work_data\project\go\mysd &amp;&amp; go test ./internal/spec/ -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/spec/detector.go contains `func DetectSpecDir(root string)`
    - internal/spec/parser.go contains `func ParseChange(changeDir string)`
    - internal/spec/parser.go contains `func extractKeyword(line string) RFC2119Keyword`
    - internal/spec/parser.go contains `\bMUST\b` (case-sensitive regex)
    - internal/spec/parser.go contains `frontmatter.Parse`
    - internal/spec/delta.go contains `func DetectDeltaOp(heading string) DeltaOp`
    - internal/spec/writer.go contains `func Scaffold(name string`
    - internal/spec/writer.go contains `spec-version`
    - `go test ./internal/spec/ -v -count=1` exits 0 with all tests passing
    - parser_test.go contains a test with lowercase "must" returning empty keyword
  </acceptance_criteria>
  <done>All spec parsing, detection, delta, and scaffolding functions implemented and passing tests. Parser handles both brownfield (no frontmatter) and native (with frontmatter) OpenSpec formats correctly.</done>
</task>

</tasks>

<verification>
- `go build ./...` compiles without errors
- `go test ./internal/spec/ -v -count=1` — all tests pass
- `go vet ./...` — no issues
- Brownfield fixture (no frontmatter) parsed without errors
- Native fixture (with frontmatter) parsed with all metadata populated
- RFC 2119 case-sensitive matching verified (uppercase detected, lowercase ignored)
</verification>

<success_criteria>
- Go module initializes and compiles cross-platform (DIST-01, DIST-02)
- Typed Go structs define complete spec domain model (SPEC-01, SPEC-02, SPEC-03, SPEC-04, SPEC-07)
- Parser reads both OpenSpec brownfield and my-ssd native formats (OPSX-01, OPSX-02, OPSX-04)
- Delta ops correctly identified (OPSX-03)
- Writer scaffolds complete change directory (SPEC-01)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
