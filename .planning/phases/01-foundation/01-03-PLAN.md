---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - main.go
  - cmd/root.go
  - cmd/propose.go
  - cmd/init_cmd.go
  - cmd/spec.go
  - cmd/design.go
  - cmd/plan.go
  - cmd/execute.go
  - cmd/verify.go
  - cmd/archive.go
  - cmd/status.go
  - cmd/root_test.go
  - cmd/propose_test.go
  - cmd/init_cmd_test.go
  - Makefile
autonomous: true
requirements:
  - SPEC-01
  - CONF-01
  - CONF-04
  - DIST-01
  - DIST-02
  - STAT-01

must_haves:
  truths:
    - "User can run `mysd propose my-feature` and get scaffolded .specs/changes/my-feature/ directory"
    - "User can run `mysd init` and get .claude/mysd.yaml created with defaults"
    - "User can run `mysd --help` and see all commands listed"
    - "All stub commands (spec, design, plan, execute, verify, archive, status) return 'not yet implemented' message"
    - "Flags override config file values (--execution-mode, --lang, etc.)"
    - "Binary compiles cross-platform (go build produces single binary)"
  artifacts:
    - path: "main.go"
      provides: "Binary entry point"
      contains: "cmd.Execute"
    - path: "cmd/root.go"
      provides: "Root command with persistent flags and viper config init"
      exports: ["Execute"]
    - path: "cmd/propose.go"
      provides: "mysd propose command wiring spec.Scaffold"
      contains: "spec.Scaffold"
    - path: "cmd/init_cmd.go"
      provides: "mysd init command creating .claude/mysd.yaml"
      contains: "mysd.yaml"
    - path: "Makefile"
      provides: "Build, test, lint targets"
      contains: "go build"
  key_links:
    - from: "cmd/propose.go"
      to: "internal/spec/writer.go"
      via: "calls spec.Scaffold to create change dir"
      pattern: "spec\\.Scaffold"
    - from: "cmd/propose.go"
      to: "internal/state/state.go"
      via: "calls state.Transition to set PhaseProposed"
      pattern: "state\\.Transition"
    - from: "cmd/propose.go"
      to: "internal/output/printer.go"
      via: "uses output.Printer for styled messages"
      pattern: "output\\.NewPrinter"
    - from: "cmd/root.go"
      to: "internal/config/config.go"
      via: "loads config on cobra.OnInitialize"
      pattern: "config\\.Load"
    - from: "cmd/init_cmd.go"
      to: "internal/config/defaults.go"
      via: "writes Defaults() to .claude/mysd.yaml"
      pattern: "config\\.Defaults"
    - from: "main.go"
      to: "cmd/root.go"
      via: "calls cmd.Execute()"
      pattern: "cmd\\.Execute"
---

<objective>
CLI skeleton（Cobra）+ mysd propose 和 mysd init 完整實作 + 所有其他 command stub

Purpose: 將 Plan 01 的 spec parser 和 Plan 02 的 state/config/output 組裝成可執行的 CLI binary。使用者第一次可以 `mysd propose` 建立 spec、`mysd init` 初始化設定。
Output: 可執行的 `mysd` binary，兩個完整指令 + 所有 Phase 2/3 command stubs。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cobra CLI skeleton with root command, persistent flags, and all command stubs</name>
  <files>main.go, cmd/root.go, cmd/spec.go, cmd/design.go, cmd/plan.go, cmd/execute.go, cmd/verify.go, cmd/archive.go, cmd/status.go, Makefile</files>
  <read_first>
    main.go (current placeholder)
    internal/config/config.go (Load function signature)
    internal/config/defaults.go (ProjectConfig struct and field names)
    internal/output/printer.go (NewPrinter signature)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: thin commands, Pattern 5: viper+cobra flags)
  </read_first>
  <action>
    1. Update `main.go`:
       ```go
       package main

       import "github.com/mysd/cmd"

       func main() {
           cmd.Execute()
       }
       ```

    2. Create `cmd/root.go` (per RESEARCH.md Pattern 5):
       - `package cmd`
       - `var rootCmd = &cobra.Command{Use: "mysd", Short: "Spec-Driven Development for AI programming"}`
       - `var cfgFile string`
       - `func Execute()`: Call `rootCmd.Execute()`, exit 1 on error.
       - `func init()`:
         - `cobra.OnInitialize(initConfig)`
         - Add PersistentFlags:
           `--config` (string, "config file, default .claude/mysd.yaml")
           `--execution-mode` (string, "execution mode: single or wave")
           `--agent-count` (int, "number of agents for wave mode")
           `--lang` (string, "response language override")
           `--doc-lang` (string, "document output language override")
           `--tdd` (bool, "enable TDD mode")
           `--atomic-commits` (bool, "enable atomic git commits per task")
         - Bind each flag via `viper.BindPFlag("execution_mode", rootCmd.PersistentFlags().Lookup("execution-mode"))` etc. (per CONF-04)
       - `func initConfig()`:
         - If cfgFile set, use `viper.SetConfigFile(cfgFile)`
         - Else: `viper.SetConfigName("mysd")`, `viper.SetConfigType("yaml")`, `viper.AddConfigPath(".claude")`, `viper.AddConfigPath(filepath.Join(home, ".claude"))`
         - `viper.AutomaticEnv()`
         - `viper.ReadInConfig()` — ignore ConfigFileNotFoundError (convention over config)

    3. Create stub commands (Phase 2/3 — just registration + "not yet implemented"):
       - `cmd/spec.go`: `mysd spec` — Short: "Define detailed requirements with RFC 2119 keywords"
       - `cmd/design.go`: `mysd design` — Short: "Capture technical decisions and architecture"
       - `cmd/plan.go`: `mysd plan` — Short: "Break design into executable task list"
       - `cmd/execute.go`: `mysd execute` — Short: "Run tasks with pre-execution alignment"
       - `cmd/verify.go`: `mysd verify` — Short: "Goal-backward verification of MUST items"
       - `cmd/archive.go`: `mysd archive` — Short: "Archive completed spec to history"
       - `cmd/status.go`: `mysd status` — Short: "Show current spec state and progress"
       Each stub: `RunE: func(cmd *cobra.Command, args []string) error { fmt.Fprintln(cmd.OutOrStdout(), "not yet implemented"); return nil }`
       Each added to rootCmd in its own `init()` function.

    4. Create `Makefile`:
       ```makefile
       BINARY=mysd
       VERSION?=dev

       .PHONY: build test lint clean

       build:
       	go build -ldflags "-X main.version=$(VERSION)" -o $(BINARY) .

       test:
       	go test ./... -v -count=1

       lint:
       	go vet ./...

       clean:
       	rm -f $(BINARY)
       ```
  </action>
  <verify>
    <automated>cd D:\work_data\project\go\mysd &amp;&amp; go build -o mysd.exe . &amp;&amp; ./mysd.exe --help</automated>
  </verify>
  <acceptance_criteria>
    - main.go contains `cmd.Execute()`
    - cmd/root.go contains `var rootCmd`
    - cmd/root.go contains `cobra.OnInitialize(initConfig)`
    - cmd/root.go contains `viper.BindPFlag`
    - cmd/root.go contains `--execution-mode`
    - cmd/root.go contains `--lang`
    - cmd/spec.go contains `not yet implemented`
    - cmd/verify.go contains `not yet implemented`
    - Makefile contains `go build`
    - `go build -o mysd.exe .` exits 0
    - `./mysd.exe --help` output contains "propose"
    - `./mysd.exe --help` output contains "init"
    - `./mysd.exe spec` output contains "not yet implemented"
  </acceptance_criteria>
  <done>CLI binary compiles and runs. `mysd --help` shows all commands. Stub commands return "not yet implemented". Persistent flags registered and bound to viper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement mysd propose and mysd init commands</name>
  <files>cmd/propose.go, cmd/init_cmd.go, cmd/root_test.go, cmd/propose_test.go, cmd/init_cmd_test.go</files>
  <read_first>
    cmd/root.go (rootCmd structure, initConfig)
    internal/spec/writer.go (Scaffold function signature)
    internal/spec/detector.go (DetectSpecDir signature)
    internal/state/state.go (LoadState, SaveState signatures)
    internal/state/transitions.go (Transition, PhaseProposed)
    internal/config/config.go (Load signature)
    internal/config/defaults.go (ProjectConfig struct, Defaults function)
    internal/output/printer.go (NewPrinter, Success, Error signatures)
  </read_first>
  <behavior>
    - `mysd propose test-feature` creates .specs/changes/test-feature/ with proposal.md, specs/, design.md, tasks.md
    - `mysd propose test-feature` prints success message with created path
    - `mysd propose` with no args returns error with usage hint
    - `mysd propose test-feature` transitions state to PhaseProposed and saves STATE.json
    - `mysd init` creates .claude/mysd.yaml with default config values
    - `mysd init` when .claude/mysd.yaml already exists prints warning and does NOT overwrite
    - `mysd init --force` overwrites existing config
  </behavior>
  <action>
    1. Create `cmd/propose.go` (per D-10, D-12, SPEC-01):
       - Register as subcommand of rootCmd in `init()`
       - `Use: "propose [name]"`, `Short: "Create a new spec from description"`, `Args: cobra.MinimumNArgs(1)`
       - `RunE: runPropose`
       - `func runPropose(cmd *cobra.Command, args []string) error`:
         a. Create printer: `p := output.NewPrinter(cmd.OutOrStdout())`
         b. Determine spec dir: try `spec.DetectSpecDir(".")`. If ErrNoSpecDir, default to `.specs` (new project).
         c. Call `change, err := spec.Scaffold(args[0], specDir)` to create change directory
         d. Load state: `ws, _ := state.LoadState(specDir)`. Call `state.Transition(&ws, state.PhaseProposed)`.
           Set `ws.ChangeName = args[0]`. Call `state.SaveState(specDir, ws)`.
         e. Print success: `p.Success(fmt.Sprintf("Created spec: %s", change.Dir))`
         f. Print next step hint: `p.Info("Next: mysd spec — define detailed requirements")`

    2. Create `cmd/init_cmd.go` (per CONF-01, D-04):
       NOTE: File named `init_cmd.go` to avoid collision with Go's `init()` function convention.
       - Register as subcommand named "init" of rootCmd in `init()`
       - `Use: "init"`, `Short: "Initialize project config (.claude/mysd.yaml)"`
       - Add local flag `--force` (bool, false, "overwrite existing config")
       - `RunE: runInit`
       - `func runInit(cmd *cobra.Command, args []string) error`:
         a. Create printer: `p := output.NewPrinter(cmd.OutOrStdout())`
         b. Target path: `filepath.Join(".", ".claude", "mysd.yaml")`
         c. Check if file exists. If exists and no `--force`: `p.Warning("Config already exists: .claude/mysd.yaml (use --force to overwrite)")`, return nil.
         d. Create `.claude/` directory with `os.MkdirAll`
         e. Get defaults: `cfg := config.Defaults()`
         f. Marshal to YAML: `yaml.Marshal(cfg)` using gopkg.in/yaml.v3
         g. Write file with descriptive comments header:
            ```yaml
            # my-ssd project configuration
            # See: https://github.com/mysd/docs/config
            execution_mode: single
            agent_count: 1
            atomic_commits: false
            tdd: false
            test_generation: false
            response_language: ""
            document_language: ""
            ```
         h. `p.Success("Created config: .claude/mysd.yaml")`

    3. Create test files:
       - `cmd/root_test.go`: Test Execute() doesn't panic. Test `--help` output contains "propose" and "init".
       - `cmd/propose_test.go`: Test propose with name arg creates directory structure in temp dir. Test propose with no args returns error. Test propose creates STATE.json with phase "proposed".
       - `cmd/init_cmd_test.go`: Test init creates .claude/mysd.yaml in temp dir. Test init with existing file (no --force) does not overwrite. Test init with --force overwrites.

       Test pattern: Use `rootCmd.SetArgs([]string{"propose", "test-feature"})` with `rootCmd.SetOut(&buf)` and temp directory as working dir. Reset rootCmd between tests.
  </action>
  <verify>
    <automated>cd D:\work_data\project\go\mysd &amp;&amp; go test ./cmd/ -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - cmd/propose.go contains `func runPropose`
    - cmd/propose.go contains `spec.Scaffold`
    - cmd/propose.go contains `state.Transition`
    - cmd/propose.go contains `state.SaveState`
    - cmd/propose.go contains `output.NewPrinter`
    - cmd/init_cmd.go contains `func runInit`
    - cmd/init_cmd.go contains `mysd.yaml`
    - cmd/init_cmd.go contains `config.Defaults`
    - cmd/init_cmd.go contains `--force`
    - cmd/propose_test.go contains `TestPropose`
    - cmd/init_cmd_test.go contains `TestInit`
    - `go test ./cmd/ -v -count=1` exits 0
    - `go test ./... -v -count=1` exits 0 (full project)
  </acceptance_criteria>
  <done>
    - `mysd propose test-feature` creates .specs/changes/test-feature/ with all spec artifacts and updates STATE.json
    - `mysd init` creates .claude/mysd.yaml with default values
    - `mysd init` on existing config warns without overwriting (--force to override)
    - All tests pass across all packages
  </done>
</task>

</tasks>

<verification>
- `go build -o mysd.exe .` compiles single binary (DIST-01)
- `go test ./... -v -count=1` — all tests pass across all packages
- `go vet ./...` — no issues
- `./mysd.exe propose test-feature` creates .specs/changes/test-feature/ with correct files
- `./mysd.exe init` creates .claude/mysd.yaml with defaults
- `./mysd.exe --help` lists all commands
- `./mysd.exe spec` prints "not yet implemented"
</verification>

<success_criteria>
- Single binary compiles and runs on current platform (DIST-01, DIST-02)
- `mysd propose` scaffolds complete spec artifacts (SPEC-01) and transitions state (STAT-01)
- `mysd init` creates config file (CONF-01) with all preference fields
- Persistent flags override config values (CONF-04)
- All stub commands registered for Phase 2/3
- Full test suite passes: `go test ./... -count=1`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
