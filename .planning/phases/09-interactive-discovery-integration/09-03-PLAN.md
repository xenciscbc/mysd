---
phase: 09-interactive-discovery-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/mysd-plan.md
  - .claude/commands/mysd-spec.md
  - .claude/commands/mysd-status.md
  - .claude/commands/mysd-note.md
autonomous: true
requirements:
  - DISC-02
  - DISC-03
  - DISC-09
must_haves:
  truths:
    - "mysd-plan.md Step 3 spawns exactly ONE mysd-researcher agent, not four"
    - "mysd-plan.md Step 3 no longer contains 'for each dimension' loop"
    - "mysd-spec.md has optional single researcher step before invoking spec-writer"
    - "mysd-status.md shows deferred notes count at the bottom"
    - "mysd-note.md SKILL.md exists and delegates to binary mysd note subcommand"
    - "plan stage researcher uses dimension 'architecture' for implementation focus"
    - "spec stage researcher uses dimension 'codebase' for implementation focus"
  artifacts:
    - path: ".claude/commands/mysd-plan.md"
      provides: "plan orchestrator with single researcher (D-04 fix)"
      contains: "ONE.*mysd-researcher"
    - path: ".claude/commands/mysd-spec.md"
      provides: "spec orchestrator with optional single researcher"
      contains: "mysd-researcher"
    - path: ".claude/commands/mysd-status.md"
      provides: "status with deferred notes count"
      contains: "mysd note"
    - path: ".claude/commands/mysd-note.md"
      provides: "thin SKILL.md wrapper for mysd note binary"
      contains: "mysd note"
  key_links:
    - from: ".claude/commands/mysd-plan.md"
      to: "mysd-researcher"
      via: "Single Task tool spawn (not 4)"
      pattern: "Agent: mysd-researcher"
    - from: ".claude/commands/mysd-note.md"
      to: "mysd note binary"
      via: "Bash invocation"
      pattern: "mysd note"
---

<objective>
Fix `/mysd:plan` single researcher (D-04 bug), add optional research to `/mysd:spec` (DISC-02), enhance `/mysd:status` deferred count display (D-09), and create `/mysd:note` SKILL.md (D-03).

Purpose: Four smaller SKILL.md changes that complete the remaining requirements: plan research fix, spec research addition, status enhancement, and note command creation.
Output: Updated plan/spec/status SKILL.md files + new note SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interactive-discovery-integration/09-CONTEXT.md
@.planning/phases/09-interactive-discovery-integration/09-RESEARCH.md

<interfaces>
<!-- Current SKILL.md files being modified -->

From .claude/commands/mysd-plan.md Step 3 (CURRENT — needs fix):
```markdown
## Step 3: Research Phase (if research_enabled)
Spawn 4 `mysd-researcher` agents in parallel using Task tool, one per dimension:
For each dimension in ["codebase", "domain", "architecture", "pitfalls"]:
```
This is the Phase 8 bug (D-04). Must be replaced with single researcher.

From .claude/commands/mysd-spec.md (CURRENT — needs research step):
```markdown
## Step 1: Get Execution Context
## Step 2: Invoke Spec Writer Agent
## Step 3: State Transition
## Step 4: Confirm
```
No research step exists. Need to insert between Step 1 and Step 2.

From .claude/commands/mysd-status.md (CURRENT — needs deferred count):
```markdown
## Step 3: Display Enhanced Dashboard
### Next Step Recommendation
```
Need to add deferred notes count section after progress summary.

From .claude/agents/mysd-researcher.md (spawn interface):
```
Context: {
  "change_name": "{change_name}",
  "dimension": "{dimension}",
  "topic": "{topic}",
  "spec_files": [{spec file paths}],
  "auto_mode": {auto_mode}
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mysd-plan.md single researcher + add spec.md research step</name>
  <files>.claude/commands/mysd-plan.md, .claude/commands/mysd-spec.md</files>
  <read_first>
    - .claude/commands/mysd-plan.md (FULL read — must see current Step 3 to replace)
    - .claude/commands/mysd-spec.md (FULL read — must see current steps to insert new one)
    - .claude/agents/mysd-researcher.md (spawn interface verification)
    - .planning/phases/09-interactive-discovery-integration/09-CONTEXT.md (D-04 decision)
    - .planning/phases/09-interactive-discovery-integration/09-RESEARCH.md (Pattern 4: Single Researcher)
  </read_first>
  <action>
    **mysd-plan.md — D-04 Bug Fix (DISC-03):**

    Replace Step 3 entirely. Remove the `for each dimension in [...]` loop and all 4 parallel researcher spawns. Replace with:

    ```markdown
    ## Step 3: Research Phase (if research_enabled)

    If `research_enabled` is true (from context JSON) or `--research` flag present:

      If `auto_mode` is false:
        Ask: "Would you like to run focused research on implementation details? [y/N]"
        If user declines: skip to Step 4.

      If `auto_mode` is true:
        Skip research entirely. Go to Step 4.

      Spawn ONE `mysd-researcher` agent (single, NOT parallel):

      Task: Research implementation details for {change_name}
      Agent: mysd-researcher
      Context: {
        "change_name": "{change_name}",
        "dimension": "architecture",
        "topic": "implementation of {change_name} — validate technical feasibility and supplement implementation details",
        "spec_files": [{all spec file paths from context + design.md path}],
        "auto_mode": {auto_mode}
      }

      Collect research output. This becomes additional input for the designer in Step 4.

      If `auto_mode` is false:
        Present research summary and ask: "Research complete. Proceed to design? (Y/n)"
    ```

    CRITICAL verification: After editing, the file must NOT contain:
    - `for each dimension` or `For each dimension`
    - `["codebase", "domain", "architecture", "pitfalls"]` as a loop target
    - More than ONE `Task:` block in Step 3

    Keep all other steps (1, 2, 4, 5, 6, 7) unchanged.

    Update frontmatter `description` to: "Plan orchestrator. Optional single-agent research, then design, then task planning. Usage: /mysd:plan [--research] [--check] [--auto]"

    ---

    **mysd-spec.md — DISC-02 (new research step):**

    Insert a new Step 2 between existing Step 1 (Get Execution Context) and Step 2 (Invoke Spec Writer). Renumber subsequent steps to 3, 4, 5.

    New Step 2:
    ```markdown
    ## Step 2: Optional Research (DISC-02, DISC-04)

    If `auto_mode` is true: skip research. Go to Step 3.

    If `auto_mode` is false:
      Ask: "Would you like to run focused research on how to implement this spec? [y/N]"

    If user chooses research:
      Spawn ONE `mysd-researcher` agent:

      Task: Research implementation approach for {change_name} spec
      Agent: mysd-researcher
      Context: {
        "change_name": "{change_name}",
        "dimension": "codebase",
        "topic": "how to implement the requirements in {change_name} — focus on existing codebase patterns and integration points",
        "spec_files": ["{proposal.md path}"],
        "auto_mode": false
      }

      Present research findings to user.
      These findings become additional context for the spec writer in Step 3.
    ```

    Renumber old Step 2 -> Step 3 (Invoke Spec Writer), old Step 3 -> Step 4 (State Transition), old Step 4 -> Step 5 (Confirm).

    In the renamed Step 3 (Invoke Spec Writer), add to the Context JSON:
    ```
    "research_findings": "{from Step 2, or empty if no research}"
    ```

    Update frontmatter `description` to: "Write detailed requirements with optional focused research. Invokes mysd-spec-writer agent. Usage: /mysd:spec [--auto]"

    Add `--auto` flag parsing to Step 1 (same pattern as propose/discuss):
    ```
    Check `$ARGUMENTS` for `--auto`. Set `auto_mode` = true if present.
    ```
  </action>
  <verify>
    <automated>grep -c "for each dimension\|For each dimension" .claude/commands/mysd-plan.md | grep -q "^0$" && grep -c "Agent: mysd-researcher" .claude/commands/mysd-plan.md | grep -q "^1$" && grep -c "Agent: mysd-researcher" .claude/commands/mysd-spec.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - mysd-plan.md Step 3 contains exactly ONE "Agent: mysd-researcher" spawn
    - mysd-plan.md does NOT contain "for each dimension" or "For each dimension"
    - mysd-plan.md does NOT contain '["codebase", "domain", "architecture", "pitfalls"]' as loop
    - mysd-plan.md Step 3 uses dimension "architecture" for the single researcher
    - mysd-plan.md Step 3 has DISC-04 opt-in prompt when auto_mode is false
    - mysd-spec.md contains "Agent: mysd-researcher" (new Step 2)
    - mysd-spec.md new researcher uses dimension "codebase"
    - mysd-spec.md has 5 steps total (was 4, added 1)
    - mysd-spec.md Step 1 parses --auto flag
  </acceptance_criteria>
  <done>mysd-plan.md uses single researcher (D-04 bug fixed). mysd-spec.md has optional single researcher for implementation approach (DISC-02). Both have DISC-04 opt-in prompts.</done>
</task>

<task type="auto">
  <name>Task 2: Create mysd-note.md SKILL.md + enhance mysd-status.md</name>
  <files>.claude/commands/mysd-note.md, .claude/commands/mysd-status.md</files>
  <read_first>
    - .claude/commands/mysd-status.md (current implementation — will add deferred count section)
    - .claude/commands/mysd-propose.md (SKILL.md pattern reference for thin wrapper)
    - .planning/phases/09-interactive-discovery-integration/09-CONTEXT.md (D-03, D-09)
    - .planning/phases/09-interactive-discovery-integration/09-RESEARCH.md (Pattern 5, Pattern 6)
  </read_first>
  <action>
    **Create `.claude/commands/mysd-note.md`** — NEW file (D-03):

    ```markdown
    ---
    model: claude-sonnet-4-5
    description: Manage deferred notes (ideas for future changes). Usage: /mysd:note [add {content} | delete {id}]
    allowed-tools:
      - Bash
      - Read
    ---

    # /mysd:note -- Manage Deferred Notes

    You are the mysd note assistant. Your job is to manage deferred notes — ideas captured during exploration that are outside the current change scope.

    ## Step 1: Parse Arguments

    Check `$ARGUMENTS`:
    - No arguments → list mode
    - First word is "add" → add mode, remaining words are the note content
    - First word is "delete" → delete mode, next word is the note ID

    ## Step 2: Execute Command

    **List mode:**
    Run: `mysd note`
    Display the output. If no notes exist, show: "No deferred notes. Ideas captured during /mysd:propose or /mysd:discuss exploration will appear here."

    **Add mode:**
    Run: `mysd note add "{content}"`
    Show confirmation with the assigned note ID.

    **Delete mode:**
    Run: `mysd note delete {id}`
    Show confirmation or error if ID not found.

    ## Step 3: Context Hint

    After any operation, show:
    "Deferred notes are loaded as context when you run /mysd:propose. Browse all notes with /mysd:note."
    ```

    ---

    **Update `.claude/commands/mysd-status.md`** — D-09 enhancement:

    Add a new section to Step 3 (Display Enhanced Dashboard), AFTER the "Next Step Recommendation" section:

    ```markdown
    ### Deferred Notes Count

    Run: `mysd note`
    Parse the output to count notes (each line starting with `[` is a note).

    If notes exist, show at the bottom:
    ```
    Deferred notes: {N} — run /mysd:note to browse
    ```

    If no notes exist, do not show this line.
    ```

    Also add `Bash` to allowed-tools if not already present (it already is).
  </action>
  <verify>
    <automated>test -f .claude/commands/mysd-note.md && grep -q "mysd note" .claude/commands/mysd-note.md && grep -q "Deferred notes:" .claude/commands/mysd-status.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/mysd-note.md exists as new file
    - mysd-note.md frontmatter has model, description, allowed-tools (Bash, Read — NO Task tool)
    - mysd-note.md has 3 steps: Parse Arguments, Execute Command, Context Hint
    - mysd-note.md handles list/add/delete modes
    - mysd-note.md does NOT use Task tool (thin wrapper, not orchestrator)
    - mysd-status.md contains "Deferred notes:" string
    - mysd-status.md contains "mysd note" or "/mysd:note" reference
    - mysd-status.md deferred count section is after Next Step Recommendation
  </acceptance_criteria>
  <done>/mysd:note SKILL.md created as thin binary wrapper. /mysd:status shows deferred notes count when notes exist (D-09).</done>
</task>

</tasks>

<verification>
- mysd-plan.md has exactly 1 researcher spawn in Step 3 (not 4)
- mysd-spec.md has new research step with single researcher
- mysd-status.md shows deferred count
- mysd-note.md exists and delegates to binary
- All 4 files have valid YAML frontmatter
</verification>

<success_criteria>
- D-04 bug fixed: plan uses single researcher with "architecture" dimension
- DISC-02 implemented: spec has optional single researcher with "codebase" dimension
- DISC-04 implemented: both plan and spec have opt-in research prompts
- D-09 implemented: status shows deferred notes count
- D-03 implemented: /mysd:note SKILL.md delegates to binary
</success_criteria>

<output>
After completion, create `.planning/phases/09-interactive-discovery-integration/09-03-SUMMARY.md`
</output>
