---
phase: 09-interactive-discovery-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/mysd-propose.md
  - .claude/commands/mysd-discuss.md
autonomous: true
requirements:
  - DISC-01
  - DISC-04
  - DISC-05
  - DISC-06
  - DISC-07
must_haves:
  truths:
    - "propose asks whether to enable 4-dimension research before invoking proposal writer"
    - "propose spawns 4 mysd-researcher agents in parallel when research is enabled"
    - "propose orchestrator identifies gray areas from researcher output and spawns advisors in parallel"
    - "propose dual-loop: each gray area has deep-dive, then user chooses continue or done"
    - "propose after all areas: user can discover new areas or finish exploration"
    - "discuss asks whether to enable 4-dimension research after topic identification"
    - "discuss orchestrator identifies gray areas and spawns advisors at orchestrator layer"
    - "discuss dual-loop identical to propose pattern"
    - "auto_mode skips research entirely in both propose and discuss"
    - "advisors are never spawned inside researcher agents — only at orchestrator layer"
  artifacts:
    - path: ".claude/commands/mysd-propose.md"
      provides: "propose orchestrator with discovery pipeline"
      contains: "Gray Area"
    - path: ".claude/commands/mysd-discuss.md"
      provides: "discuss orchestrator with discovery pipeline"
      contains: "Gray Area"
  key_links:
    - from: ".claude/commands/mysd-propose.md"
      to: "mysd-researcher"
      via: "Task tool spawn 4 parallel"
      pattern: "Agent: mysd-researcher"
    - from: ".claude/commands/mysd-propose.md"
      to: "mysd-advisor"
      via: "Task tool spawn per gray area"
      pattern: "Agent: mysd-advisor"
    - from: ".claude/commands/mysd-discuss.md"
      to: "mysd-researcher"
      via: "Task tool spawn 4 parallel"
      pattern: "Agent: mysd-researcher"
    - from: ".claude/commands/mysd-discuss.md"
      to: "mysd-advisor"
      via: "Task tool spawn per gray area"
      pattern: "Agent: mysd-advisor"
---

<objective>
Rewrite `/mysd:propose` and `/mysd:discuss` SKILL.md orchestrators to include the full interactive discovery pipeline: optional 4-dimension research, gray area identification, parallel advisor spawning, dual-loop exploration with user-driven termination, and scope guardrail.

Purpose: These are the two primary discovery commands (DISC-01, DISC-04-07). The pipeline is identical in structure for both, differing only in entry context (propose = new change, discuss = existing change topic).
Output: Updated `.claude/commands/mysd-propose.md` and `.claude/commands/mysd-discuss.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interactive-discovery-integration/09-CONTEXT.md
@.planning/phases/09-interactive-discovery-integration/09-RESEARCH.md

<interfaces>
<!-- Agent definitions the orchestrators will spawn — DO NOT modify these files -->

From .claude/agents/mysd-researcher.md (spawn interface):
```
Task: Research {dimension} for topic: {topic}
Agent: mysd-researcher
Context: {
  "change_name": "{change_name}",
  "dimension": "{dimension}",       // "codebase" | "domain" | "architecture" | "pitfalls"
  "topic": "{topic}",
  "spec_files": [{spec file paths}],
  "auto_mode": {auto_mode}
}
```

From .claude/agents/mysd-advisor.md (spawn interface):
```
Task: Analyze gray area: {gray_area_description}
Agent: mysd-advisor
Context: {
  "change_name": "{change_name}",
  "gray_area": "{gray_area_description}",
  "research_findings": "{combined research output}",
  "auto_mode": {auto_mode}
}
```

From .claude/agents/mysd-proposal-writer.md (spawn interface):
```
Task: Write proposal for {change_name}
Agent: mysd-proposal-writer
Context: {
  "change_name": "{change_name}",
  "conclusions": "{conclusions text}",
  "existing_proposal": null | "{body}",
  "auto_mode": {auto_mode}
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite mysd-propose.md with discovery pipeline</name>
  <files>.claude/commands/mysd-propose.md</files>
  <read_first>
    - .claude/commands/mysd-propose.md (current implementation — FULL read required)
    - .claude/agents/mysd-researcher.md (spawn interface — verify context fields)
    - .claude/agents/mysd-advisor.md (spawn interface — verify context fields)
    - .claude/agents/mysd-proposal-writer.md (spawn interface — verify context fields)
    - .planning/phases/09-interactive-discovery-integration/09-CONTEXT.md (D-01, D-02, D-06, D-07, D-08 decisions)
    - .planning/phases/09-interactive-discovery-integration/09-RESEARCH.md (Pattern 1: SKILL.md Discovery Pipeline, Pattern 3: Scope Guardrail)
  </read_first>
  <action>
    Rewrite `.claude/commands/mysd-propose.md` preserving existing frontmatter (model, description, allowed-tools including Task) and Steps 1-3 (parse args, source detection, scaffold). Insert new steps between scaffold and proposal writer, then renumber.

    The final step structure MUST be:

    **Step 1: Parse Arguments** — keep existing (parse --auto, extract source_arg)

    **Step 2: Source Detection** — keep existing (6-priority source detection chain)

    **Step 3: Scaffold the Change** — keep existing (run `mysd propose {change-name}`)

    **Step 4: Load Deferred Notes (D-02)** — NEW
    - Per D-02: propose ALWAYS loads deferred notes (cross-change context is valuable)
    - Run: `mysd note list`
    - If output shows notes, include them as `deferred_context` for research and proposal writing
    - If no notes, set `deferred_context` to empty string

    **Step 5: Optional Research (DISC-01, DISC-04, D-06)** — NEW
    - If `auto_mode` is true: skip research entirely (FAUTO-02). Go directly to Step 9.
    - If `auto_mode` is false: Ask user:
      ```
      Would you like to run 4-dimension research on this proposal?
      (Codebase / Domain / Architecture / Pitfalls) [y/N]
      ```
    - If user declines: go to Step 9.
    - If user accepts: proceed to Step 6.

    **Step 6: Parallel Research Spawning** — NEW
    - Spawn 4 `mysd-researcher` agents in parallel using Task tool:
      ```
      For each dimension in ["codebase", "domain", "architecture", "pitfalls"]:
        Task: Research {dimension} for proposal: {change_name}
        Agent: mysd-researcher
        Context: {
          "change_name": "{change_name}",
          "dimension": "{dimension}",
          "topic": "{source content or user description}",
          "spec_files": [".specs/changes/{change_name}/proposal.md"],
          "auto_mode": false
        }
      ```
    - Collect all 4 research outputs. Present organized summary by dimension.

    **Step 7: Gray Area Identification + Advisor Spawning (DISC-06)** — NEW
    - From the 4 research outputs, identify gray areas: ambiguous design decisions where multiple valid approaches exist, conflicting recommendations between dimensions, or areas needing user input.
    - For each gray area, spawn one `mysd-advisor` agent in parallel:
      ```
      Task: Analyze gray area: {gray_area_description}
      Agent: mysd-advisor
      Context: {
        "change_name": "{change_name}",
        "gray_area": "{gray_area_description}",
        "research_findings": "{all 4 researcher outputs combined}",
        "auto_mode": false
      }
      ```
    - CRITICAL: Advisors MUST be spawned at this orchestrator layer, NOT inside any researcher agent.
    - Collect all advisor comparison tables.

    **Step 8: Dual-Loop Exploration (DISC-05, DISC-07, D-01, D-07, D-08)** — NEW

    **Layer 1 — Per-Area Deep Dive:**
    For each gray area with its advisor analysis:
    1. Present the advisor's comparison table
    2. Facilitate discussion (DISC-05 dual-mode):
       - AI presents findings and asks clarifying questions (AI-led)
       - User can answer or ask their own questions (user-led)
       - This is natural conversation flow, no explicit mode switch needed
    3. **Scope Guardrail (DISC-08, D-08):** During discussion, if a suggestion expands beyond the current proposal scope:
       - Acknowledge the idea
       - State: "This is outside the current proposal scope."
       - Run: `mysd note add "{idea summary}"` to save to deferred notes
       - Continue exploration without incorporating it
       - Scope boundary is determined by reading proposal.md's In Scope / Out of Scope sections
    4. After the area discussion concludes, ask (D-01 — user-driven, no quota):
       ```
       This area is resolved. Would you like to:
       1. Continue to the next area
       2. Finish exploration
       ```
       If "finish": exit Layer 1, skip to Step 9.

    **Layer 2 — New Area Discovery:**
    After all identified gray areas are explored:
    ```
    All identified areas have been explored.
    Would you like to:
    1. Explore additional areas (describe what you'd like to investigate)
    2. Finish exploration and proceed to proposal writing
    ```
    If "explore": user describes new areas -> repeat gray area analysis (spawn advisor per new area) -> Layer 1 deep dive for each.
    If "finish": proceed to Step 9.

    **Step 9: Invoke Proposal Writer** — updated from old Step 4
    - Use Task tool with `mysd-proposal-writer`:
      ```
      Task: Write proposal for {change_name}
      Agent: mysd-proposal-writer
      Context: {
        "change_name": "{change_name}",
        "conclusions": "{research findings + exploration conclusions + source content}",
        "existing_proposal": "{current proposal.md body if exists, else null}",
        "deferred_context": "{deferred notes from Step 4}",
        "auto_mode": {auto_mode}
      }
      ```

    **Step 10: Confirm** — updated from old Step 5
    - Show: proposal file path, brief summary, whether research was performed, number of gray areas explored, number of ideas deferred
    - Next step: `/mysd:spec` or `/mysd:plan`

    Update the `description` in frontmatter to: "Create a new spec change with proposal scaffolding. Supports 4-dimension research, gray area exploration, and scope guardrail. Usage: /mysd:propose [change-name|file-path|dir-path] [--auto]"
  </action>
  <verify>
    <automated>grep -c "mysd-researcher" .claude/commands/mysd-propose.md | grep -q "[1-9]" && grep -c "mysd-advisor" .claude/commands/mysd-propose.md | grep -q "[1-9]" && grep -c "Gray Area\|gray area" .claude/commands/mysd-propose.md | grep -q "[1-9]" && grep -c "mysd note add" .claude/commands/mysd-propose.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - mysd-propose.md contains "Agent: mysd-researcher" (at least 1 occurrence)
    - mysd-propose.md contains "Agent: mysd-advisor" (at least 1 occurrence)
    - mysd-propose.md contains "codebase" and "domain" and "architecture" and "pitfalls" dimension names
    - mysd-propose.md contains "mysd note add" (scope guardrail)
    - mysd-propose.md contains "mysd note list" (deferred notes loading)
    - mysd-propose.md contains "auto_mode" (auto mode handling)
    - mysd-propose.md contains Step 8 with "Layer 1" and "Layer 2" (dual-loop)
    - mysd-propose.md contains scope guardrail instructions referencing "In Scope / Out of Scope"
    - mysd-propose.md does NOT contain Task tool inside researcher/advisor agent definitions
    - mysd-propose.md frontmatter still contains "Task" in allowed-tools
  </acceptance_criteria>
  <done>mysd-propose.md has full discovery pipeline: opt-in research, 4 parallel researchers, gray area identification, parallel advisor spawning at orchestrator layer, dual-loop exploration with user-driven termination, scope guardrail writing to deferred notes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite mysd-discuss.md with discovery pipeline</name>
  <files>.claude/commands/mysd-discuss.md</files>
  <read_first>
    - .claude/commands/mysd-discuss.md (current implementation — FULL read required)
    - .claude/commands/mysd-propose.md (Task 1 output — reuse identical discovery pipeline pattern)
    - .claude/agents/mysd-researcher.md (spawn interface)
    - .claude/agents/mysd-advisor.md (spawn interface)
    - .planning/phases/09-interactive-discovery-integration/09-CONTEXT.md (D-01, D-02, D-07, D-08)
  </read_first>
  <action>
    Rewrite `.claude/commands/mysd-discuss.md` preserving frontmatter and Steps 1-3 (parse args, source detection, topic identification). Replace Step 4 (Optional Research) and Step 5 (Discussion Loop) with the discovery pipeline. Keep Steps 6-8 (Spec Update, Re-plan, Confirm).

    The final step structure MUST be:

    **Step 1: Parse Arguments** — keep existing

    **Step 2: Source Detection (D-06)** — keep existing (6-priority detection)

    **Step 3: Topic Identification (D-01)** — keep existing

    **Step 4: Conditional Deferred Notes Loading (D-02)** — NEW
    - Per D-02: discuss checks for active WIP change before loading deferred notes
    - Run: `mysd status` and check if there is an active change in non-archived state
    - If active WIP change exists: do NOT load deferred notes (avoid polluting focused WIP discussion)
    - If NO active WIP change (or change is archived): Run `mysd note list` and include as context
    - Store result as `deferred_context`

    **Step 5: Optional Research (DISC-04, D-06)** — REPLACE old Step 4
    - If `auto_mode` is true: skip research entirely (FAUTO-02). Go directly to Step 9.
    - If `auto_mode` is false: Ask user:
      ```
      Would you like to run 4-dimension research on this topic?
      (Codebase / Domain / Architecture / Pitfalls) [y/N]
      ```
    - If user declines: go to Step 9 (discussion without research).
    - If user accepts: proceed to Step 6.

    **Step 6: Parallel Research Spawning** — NEW (identical pattern to propose)
    - Spawn 4 `mysd-researcher` agents in parallel:
      ```
      For each dimension in ["codebase", "domain", "architecture", "pitfalls"]:
        Task: Research {dimension} for topic: {topic}
        Agent: mysd-researcher
        Context: {
          "change_name": "{change_name}",
          "dimension": "{dimension}",
          "topic": "{topic}",
          "spec_files": [{spec file paths from Step 3}],
          "auto_mode": false
        }
      ```
    - Collect all 4 research outputs.

    **Step 7: Gray Area Identification + Advisor Spawning (DISC-06)** — NEW
    - Identify gray areas from research outputs (same logic as propose Step 7)
    - Spawn one `mysd-advisor` per gray area in parallel at orchestrator layer
    - CRITICAL: advisors spawned HERE, not inside researchers

    **Step 8: Dual-Loop Exploration (DISC-05, DISC-07, D-01, D-07, D-08)** — NEW
    - Identical to propose Step 8:
      - Layer 1: per-area deep dive with AI-led/user-led dual-mode
      - Scope guardrail: `mysd note add "{idea}"` for out-of-scope suggestions
      - After each area: binary choice (continue / done) — D-01 user-driven
      - Layer 2: after all areas, discover new areas or finish

    **Step 9: Discussion Loop** — REPLACE old Step 5
    - If research was performed: present key findings, highlight conclusions from exploration
    - If no research: discuss topic based on existing spec context
    - Facilitate discussion until user reaches conclusion
    - After each conclusion: ask incorporate / continue / done (same as current)
    - If auto_mode: automatically choose "Incorporate"

    **Step 10: Spec Update** — renumbered from old Step 6, keep existing logic
    (proposal-writer for proposal layer, spec-writer for specs layer, designer for design layer)

    **Step 11: Re-plan + Plan-Checker** — renumbered from old Step 7, keep existing logic

    **Step 12: Confirm** — renumbered from old Step 8, updated to show research/exploration summary

    Update frontmatter `description` to: "Ad-hoc discussion with optional 4-dimension research, gray area exploration, and scope guardrail. Updates specs and triggers re-plan. Usage: /mysd:discuss [topic|change-name|file-path|dir-path] [--auto]"
  </action>
  <verify>
    <automated>grep -c "mysd-researcher" .claude/commands/mysd-discuss.md | grep -q "[1-9]" && grep -c "mysd-advisor" .claude/commands/mysd-discuss.md | grep -q "[1-9]" && grep -c "Gray Area\|gray area" .claude/commands/mysd-discuss.md | grep -q "[1-9]" && grep -c "mysd note" .claude/commands/mysd-discuss.md | grep -q "[1-9]" && grep -c "Layer 1\|Layer 2" .claude/commands/mysd-discuss.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - mysd-discuss.md contains "Agent: mysd-researcher" (at least 1 occurrence)
    - mysd-discuss.md contains "Agent: mysd-advisor" (at least 1 occurrence)
    - mysd-discuss.md contains all 4 dimension names: "codebase", "domain", "architecture", "pitfalls"
    - mysd-discuss.md contains "mysd note add" (scope guardrail)
    - mysd-discuss.md contains "mysd note list" or "mysd status" (deferred notes conditional loading)
    - mysd-discuss.md contains "Layer 1" and "Layer 2" (dual-loop)
    - mysd-discuss.md contains D-02 logic: check active WIP before loading deferred notes
    - mysd-discuss.md Step 10/11 preserve existing spec update and re-plan logic
    - mysd-discuss.md frontmatter still has Task in allowed-tools
  </acceptance_criteria>
  <done>mysd-discuss.md has full discovery pipeline identical to propose pattern, with D-02 conditional deferred notes loading, scope guardrail, and preserved spec update + re-plan steps.</done>
</task>

</tasks>

<verification>
- Both files have valid YAML frontmatter with model, description, allowed-tools
- Both files reference mysd-researcher and mysd-advisor agents
- Neither file has Task tool references inside agent context blocks (agents don't spawn agents)
- Both files have auto_mode skip logic
- Both files have dual-loop with Layer 1 and Layer 2
- Both files have scope guardrail with `mysd note add`
</verification>

<success_criteria>
- propose and discuss SKILL.md files contain the full discovery pipeline
- 4-dimension research is opt-in with explicit user prompt (DISC-04)
- Gray area identification happens at orchestrator layer (DISC-06)
- Dual-loop has user-driven termination without numeric quotas (D-01, DISC-07)
- Scope guardrail writes to deferred notes via `mysd note add` (DISC-08)
- auto_mode skips research entirely (FAUTO-02)
- Deferred notes loading differs: propose always loads (D-02), discuss conditionally loads
</success_criteria>

<output>
After completion, create `.planning/phases/09-interactive-discovery-integration/09-02-SUMMARY.md`
</output>
