---
phase: 09-interactive-discovery-integration
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - plugin/commands/mysd-propose.md
  - plugin/commands/mysd-discuss.md
  - plugin/commands/mysd-plan.md
  - plugin/commands/mysd-spec.md
  - plugin/commands/mysd-status.md
  - plugin/commands/mysd-note.md
autonomous: false
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
  - DISC-06
  - DISC-07
  - DISC-08
  - DISC-09
must_haves:
  truths:
    - "All 6 modified/new SKILL.md files are synced from .claude/commands/ to plugin/commands/"
    - "plugin/commands/ copies are byte-identical to .claude/commands/ copies"
    - "go build compiles cleanly with note subcommand"
    - "go test passes full suite"
  artifacts:
    - path: "plugin/commands/mysd-propose.md"
      provides: "Distribution copy of propose SKILL.md"
    - path: "plugin/commands/mysd-discuss.md"
      provides: "Distribution copy of discuss SKILL.md"
    - path: "plugin/commands/mysd-plan.md"
      provides: "Distribution copy of plan SKILL.md"
    - path: "plugin/commands/mysd-spec.md"
      provides: "Distribution copy of spec SKILL.md"
    - path: "plugin/commands/mysd-status.md"
      provides: "Distribution copy of status SKILL.md"
    - path: "plugin/commands/mysd-note.md"
      provides: "Distribution copy of note SKILL.md (NEW)"
  key_links:
    - from: "plugin/commands/*.md"
      to: ".claude/commands/*.md"
      via: "Byte-identical copy"
      pattern: "identical"
---

<objective>
Sync all modified and new SKILL.md files to plugin/commands/ distribution directory, run full test suite, and verify Phase 9 completion with human checkpoint.

Purpose: Ensures the plugin distribution directory stays in sync with the authoritative .claude/commands/ copies (Phase 8 sync pattern). Final verification that all DISC-01 through DISC-09 requirements are addressed.
Output: Synced plugin/commands/ files, full test suite green, human verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interactive-discovery-integration/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync SKILL.md files to plugin/commands/ + full test suite</name>
  <files>plugin/commands/mysd-propose.md, plugin/commands/mysd-discuss.md, plugin/commands/mysd-plan.md, plugin/commands/mysd-spec.md, plugin/commands/mysd-status.md, plugin/commands/mysd-note.md</files>
  <read_first>
    - .claude/commands/mysd-propose.md (source — copy to plugin)
    - .claude/commands/mysd-discuss.md (source — copy to plugin)
    - .claude/commands/mysd-plan.md (source — copy to plugin)
    - .claude/commands/mysd-spec.md (source — copy to plugin)
    - .claude/commands/mysd-status.md (source — copy to plugin)
    - .claude/commands/mysd-note.md (source — copy to plugin, NEW file)
    - plugin/commands/mysd-propose.md (destination — verify current state before overwrite)
  </read_first>
  <action>
    Copy each of the 6 SKILL.md files from `.claude/commands/` to `plugin/commands/`:

    1. `cp .claude/commands/mysd-propose.md plugin/commands/mysd-propose.md`
    2. `cp .claude/commands/mysd-discuss.md plugin/commands/mysd-discuss.md`
    3. `cp .claude/commands/mysd-plan.md plugin/commands/mysd-plan.md`
    4. `cp .claude/commands/mysd-spec.md plugin/commands/mysd-spec.md`
    5. `cp .claude/commands/mysd-status.md plugin/commands/mysd-status.md`
    6. `cp .claude/commands/mysd-note.md plugin/commands/mysd-note.md` (new file)

    After copying, verify byte-identical:
    ```
    diff .claude/commands/mysd-propose.md plugin/commands/mysd-propose.md
    diff .claude/commands/mysd-discuss.md plugin/commands/mysd-discuss.md
    diff .claude/commands/mysd-plan.md plugin/commands/mysd-plan.md
    diff .claude/commands/mysd-spec.md plugin/commands/mysd-spec.md
    diff .claude/commands/mysd-status.md plugin/commands/mysd-status.md
    diff .claude/commands/mysd-note.md plugin/commands/mysd-note.md
    ```

    Then run full verification:
    ```
    go build ./...
    go test ./...
    ```

    Both must exit 0.
  </action>
  <verify>
    <automated>diff .claude/commands/mysd-propose.md plugin/commands/mysd-propose.md && diff .claude/commands/mysd-note.md plugin/commands/mysd-note.md && go build ./... && go test ./... && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - plugin/commands/mysd-note.md exists (new file)
    - All 6 diff commands show no differences
    - `go build ./...` exits 0
    - `go test ./...` exits 0
  </acceptance_criteria>
  <done>All 6 SKILL.md files synced to plugin/commands/. Binary compiles. Full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 9 deliverables</name>
  <files>plugin/commands/mysd-note.md, .claude/commands/mysd-propose.md, .claude/commands/mysd-plan.md</files>
  <action>
    Present the Phase 9 deliverables summary to the user and ask them to verify the key behaviors listed in how-to-verify.
  </action>
  <verify>User confirms "approved"</verify>
  <done>Human has verified Phase 9 implementation is correct.</done>
  <what-built>
    Phase 9 Interactive Discovery Integration — complete implementation:

    **Go binary (Plan 01):**
    - `internal/spec/deferred.go` — DeferredNote CRUD with auto-increment ID
    - `cmd/note.go` — `mysd note list/add/delete` subcommands
    - `cmd/status.go` — deferred notes count display

    **SKILL.md orchestrators (Plan 02 + 03):**
    - `mysd-propose.md` — 4-dimension research + gray areas + advisors + dual-loop + scope guardrail
    - `mysd-discuss.md` — same pipeline + conditional deferred notes loading (D-02)
    - `mysd-plan.md` — D-04 fix: single researcher instead of 4 parallel
    - `mysd-spec.md` — DISC-02: optional single researcher
    - `mysd-status.md` — D-09: deferred notes count
    - `mysd-note.md` — NEW: thin wrapper for binary

    **Plugin sync (Plan 04):**
    - All 6 files synced to plugin/commands/
  </what-built>
  <how-to-verify>
    1. Run `mysd note add "test idea for caching"` — should create .specs/deferred.json with note ID 1
    2. Run `mysd note` — should list the note with `[1] test idea for caching`
    3. Run `mysd note delete 1` — should confirm deletion
    4. Run `mysd note` — should show "No deferred notes"
    5. Review `.claude/commands/mysd-propose.md` — confirm Steps 5-8 have discovery pipeline
    6. Review `.claude/commands/mysd-plan.md` Step 3 — confirm single researcher, no "for each dimension"
    7. Review `.claude/commands/mysd-spec.md` — confirm new Step 2 with optional research
    8. Spot-check `plugin/commands/mysd-note.md` exists and matches `.claude/commands/mysd-note.md`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All plugin/commands/ files identical to .claude/commands/ counterparts
- `go build ./...` clean
- `go test ./...` all pass
- Human confirms binary note CRUD works
- Human confirms SKILL.md files have correct content
</verification>

<success_criteria>
- Phase 9 requirements DISC-01 through DISC-09 all addressed
- Plugin distribution in sync
- No regressions in existing functionality
- Human approval obtained
</success_criteria>

<output>
After completion, create `.planning/phases/09-interactive-discovery-integration/09-04-SUMMARY.md`
</output>
