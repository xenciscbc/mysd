---
phase: 03-verification-feedback-loop
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - .claude/commands/mysd-verify.md
  - .claude/commands/mysd-archive.md
  - .claude/commands/mysd-uat.md
  - .claude/agents/mysd-verifier.md
  - .claude/agents/mysd-uat-guide.md
autonomous: true
requirements:
  - VRFY-02
  - VRFY-03
  - WCMD-06
  - WCMD-07
  - WCMD-12
  - UAT-04

must_haves:
  truths:
    - "mysd-verifier agent reads ONLY spec files and filesystem evidence (not alignment.md or execution history)"
    - "mysd-verify SKILL.md orchestrates: context-only -> verifier agent -> write-results (same pattern as execute)"
    - "mysd-archive SKILL.md calls binary with appropriate flags"
    - "mysd-uat SKILL.md invokes uat-guide agent with UAT file content"
    - "mysd-uat-guide agent interactively walks user through UAT checklist items"
  artifacts:
    - path: ".claude/commands/mysd-verify.md"
      provides: "SKILL.md orchestrator for verification workflow"
      contains: "mysd verify --context-only"
    - path: ".claude/agents/mysd-verifier.md"
      provides: "Independent verifier agent definition"
      contains: "allowed-tools"
    - path: ".claude/commands/mysd-uat.md"
      provides: "SKILL.md for UAT interactive flow"
    - path: ".claude/agents/mysd-uat-guide.md"
      provides: "UAT interactive guide agent"
  key_links:
    - from: ".claude/commands/mysd-verify.md"
      to: ".claude/agents/mysd-verifier.md"
      via: "Task tool invocation"
      pattern: "Task.*mysd-verifier"
    - from: ".claude/commands/mysd-uat.md"
      to: ".claude/agents/mysd-uat-guide.md"
      via: "Task tool invocation"
      pattern: "Task.*mysd-uat-guide"
---

<objective>
Create the Claude Code plugin files: 3 SKILL.md orchestrators (verify, archive, uat) and 2 agent definitions (verifier, uat-guide). These follow the exact same pattern as the existing mysd-execute.md SKILL.md and mysd-executor.md agent.

Purpose: These plugin files are the user-facing interface in Claude Code. `/mysd:verify` triggers verification, `/mysd:archive` triggers archiving, `/mysd:uat` triggers interactive UAT. The verifier agent enforces independence from the executor (D-12).
Output: 5 plugin Markdown files in .claude/commands/ and .claude/agents/
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-verification-feedback-loop/03-CONTEXT.md
@.planning/phases/03-verification-feedback-loop/03-01-SUMMARY.md
@.planning/phases/03-verification-feedback-loop/03-02-SUMMARY.md

<interfaces>
<!-- Existing plugin files to replicate pattern from -->

From .claude/commands/mysd-execute.md (SKILL.md pattern):
Frontmatter has: model (claude-sonnet-4-5), description, allowed-tools (Bash, Read, Write, Edit, Task).
Body structure: Step 1 runs binary with --context-only, Step 2 invokes agent via Task tool, Step 3 runs post-processing binary command.

From .claude/agents/mysd-executor.md (agent pattern):
Frontmatter has: model (claude-sonnet-4-5), description, allowed-tools (Read, Write, Edit, Bash).
Body structure: Input section (describes context JSON), Mandatory section (alignment gate), Execution section.

From internal/verifier/report.go (VerifierReport JSON format):
Report JSON has fields: change_name, overall_pass, must_pass, results array (id, text, keyword, pass, evidence, suggestion), has_ui_items, ui_items array (id, text, test_steps).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL.md files (mysd-verify, mysd-archive, mysd-uat)</name>
  <files>.claude/commands/mysd-verify.md, .claude/commands/mysd-archive.md, .claude/commands/mysd-uat.md</files>
  <read_first>
    - .claude/commands/mysd-execute.md (full file — replicate SKILL.md pattern exactly)
    - .claude/commands/mysd-status.md (if exists — for archive pattern reference)
    - .planning/phases/03-verification-feedback-loop/03-CONTEXT.md (D-01 through D-17 decisions)
  </read_first>
  <action>
    1. **mysd-verify.md** — SKILL.md orchestrator (per D-14, Pattern 8 from research):
       - Frontmatter: model claude-sonnet-4-5, description "Goal-backward verification of all MUST items. Invokes independent verifier agent.", allowed-tools: Bash, Read, Write, Task
       - Step 1: Run `mysd verify --context-only`, parse JSON output. Fields: change_name, change_dir, specs_dir, must_items, should_items, may_items, tasks_summary. On error, guide user to `/mysd:execute` first.
       - Step 2: Use Task tool to invoke mysd-verifier agent with full context JSON. Agent writes report to `.specs/changes/{change_name}/verifier-report.json`.
       - Step 3: After verifier completes, run `mysd verify --write-results .specs/changes/{change_name}/verifier-report.json`. This writes verification.md, gap-report.md (if failures), verification-status.json, and transitions state.
       - Step 4: Report results to user. If all MUST pass: suggest `/mysd:archive`. If failures: suggest `/mysd:execute` to fix, then re-verify.

    2. **mysd-archive.md** — SKILL.md orchestrator:
       - Frontmatter: model claude-sonnet-4-5, description "Archive a verified spec change to .specs/archive/.", allowed-tools: Bash, Read
       - Step 1: Run `mysd archive`. If succeeds, inform user "Change archived to .specs/archive/{name}/".
       - If fails with "must be verified": guide to `/mysd:verify`.
       - If fails with "not done": guide to fix items, re-execute, re-verify.
       - Step 2: Post-archive, suggest starting new change with `/mysd:propose`.

    3. **mysd-uat.md** — SKILL.md orchestrator (per D-11, UAT-04):
       - Frontmatter: model claude-sonnet-4-5, description "Interactive User Acceptance Testing walkthrough.", allowed-tools: Bash, Read, Write, Task
       - Step 1: Run `mysd status` to get current change name. Check if `.mysd/uat/{change_name}-uat.md` exists. If not, inform user to run `/mysd:verify` first.
       - Step 2: Use Task tool to invoke mysd-uat-guide agent with UAT file content.
       - Step 3: After guide completes, results are saved in the UAT file.
  </action>
  <verify>
    <automated>grep -l "allowed-tools" .claude/commands/mysd-verify.md .claude/commands/mysd-archive.md .claude/commands/mysd-uat.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/mysd-verify.md contains `mysd verify --context-only` and `mysd verify --write-results`
    - .claude/commands/mysd-verify.md contains `Task` in allowed-tools (for invoking verifier agent)
    - .claude/commands/mysd-verify.md contains `mysd-verifier` agent reference
    - .claude/commands/mysd-archive.md contains `mysd archive` command
    - .claude/commands/mysd-uat.md contains `mysd-uat-guide` agent reference
    - .claude/commands/mysd-uat.md contains `Task` in allowed-tools
    - All three files have valid YAML frontmatter with `model:`, `description:`, `allowed-tools:`
  </acceptance_criteria>
  <done>Three SKILL.md files created following the same orchestrator pattern as mysd-execute.md</done>
</task>

<task type="auto">
  <name>Task 2: Create agent definitions (mysd-verifier, mysd-uat-guide)</name>
  <files>.claude/agents/mysd-verifier.md, .claude/agents/mysd-uat-guide.md</files>
  <read_first>
    - .claude/agents/mysd-executor.md (full file — replicate agent format exactly)
    - .planning/phases/03-verification-feedback-loop/03-CONTEXT.md (D-12 independence, D-13 multi-layer evidence, D-15 UI detection)
    - .planning/phases/03-verification-feedback-loop/03-RESEARCH.md (Pitfall 3 self-verification blindness)
  </read_first>
  <action>
    1. **mysd-verifier.md** — Independent verifier agent (per D-12, D-13, Pitfall 3):
       - Frontmatter: model claude-sonnet-4-5, description "Independent verifier agent. Evaluates spec MUST/SHOULD/MAY items against filesystem evidence only.", allowed-tools: Read, Bash, Write, Glob, Grep
       - Input section: Describe verification context JSON fields (change_name, change_dir, specs_dir, must_items, should_items, may_items, tasks_summary)
       - CRITICAL Evidence-Based Verification section: "You MUST find concrete evidence for EVERY item you mark as PASS. Finding no evidence means FAIL — do NOT assume completion." List 4 evidence types: (1) File existence via Read/Glob, (2) Code pattern via Grep, (3) Test execution via Bash, (4) Build check via Bash. Evidence format: "internal/foo/bar.go:42 — function validateInput found"
       - Phase 1: Read spec files in {change_dir}/specs/*/spec.md, proposal.md, design.md. Explicit prohibition: "DO NOT read {change_dir}/alignment.md (executor artifact — breaks independence D-12). DO NOT read execution logs or history."
       - Phase 2: Verify MUST items (Priority 1) — for each item, search codebase for evidence, run tests, record pass/fail with evidence string
       - Phase 3: Verify SHOULD items (Priority 2) — same process, failures are warnings not blockers
       - Phase 4: Note MAY items (Priority 3) — quick check, neither pass nor fail affects overall
       - Phase 5: UI detection (D-15) — review all MUST and SHOULD items, for items involving user-visible behavior create ui_items entries with test_steps. Set has_ui_items=true if any found.
       - Phase 6: Write report JSON to {change_dir}/verifier-report.json. Format: change_name, overall_pass (true only if must_pass), must_pass (true only if ALL MUST pass), results array (id, text, keyword, pass, evidence, suggestion), has_ui_items, ui_items array. Rules: every result MUST have non-empty evidence, every failed result MUST have non-empty suggestion.

    2. **mysd-uat-guide.md** — UAT interactive guide agent (per D-11):
       - Frontmatter: model claude-sonnet-4-5, description "Interactive UAT guide. Walks user through acceptance tests one by one.", allowed-tools: Read, Write
       - Input section: Receives UAT checklist file content
       - Process: For each item, present test description and steps, ask "Does this pass? (pass / fail / skip)", record response. For fails, ask "What went wrong?" and record notes.
       - After all items: Show summary (X/Y passed, Z failed, W skipped). Update UAT file: item statuses, notes, run_at timestamps, last_run, summary counts, append to run_history (UAT-05).
       - Important notes: Be encouraging, ask for specific details on failures, keep flow moving, allow early stop with progress saved.
  </action>
  <verify>
    <automated>grep -l "allowed-tools" .claude/agents/mysd-verifier.md .claude/agents/mysd-uat-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/mysd-verifier.md contains `model: claude-sonnet-4-5` in frontmatter
    - .claude/agents/mysd-verifier.md contains "DO NOT read" section mentioning alignment.md (D-12)
    - .claude/agents/mysd-verifier.md contains "Evidence-Based Verification" section (Pitfall 3)
    - .claude/agents/mysd-verifier.md contains evidence format example with file path + line number
    - .claude/agents/mysd-verifier.md contains verifier-report.json output format
    - .claude/agents/mysd-verifier.md contains ui_items detection logic (D-15)
    - .claude/agents/mysd-uat-guide.md contains "pass / fail / skip" interaction pattern
    - .claude/agents/mysd-uat-guide.md contains run_history update instruction (UAT-05)
    - Neither agent file contains reference to alignment.md or execution logs (except in prohibition)
  </acceptance_criteria>
  <done>Verifier agent enforces independence (D-12), requires evidence (Pitfall 3), detects UI items (D-15); UAT guide walks through items interactively with history tracking (UAT-05)</done>
</task>

</tasks>

<verification>
- All 5 plugin files have valid YAML frontmatter (check for model: and allowed-tools: fields)
- mysd-verifier.md references alignment.md ONLY in prohibition context
- mysd-verify.md references both --context-only and --write-results flags
</verification>

<success_criteria>
- Plugin files follow exact same pattern as Phase 2 execute SKILL.md and executor agent
- Verifier agent is independent (D-12), evidence-based (D-13), detects UI items (D-15)
- UAT guide supports interactive pass/fail/skip with history (UAT-04, UAT-05)
- All SKILL.md files use Task tool to invoke their respective agents
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-feedback-loop/03-04-SUMMARY.md`
</output>
