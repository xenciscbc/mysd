---
phase: 03-verification-feedback-loop
plan: 05
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - internal/verifier/integration_test.go
  - internal/uat/integration_test.go
  - cmd/integration_test.go
autonomous: true
requirements:
  - VRFY-01
  - VRFY-04
  - VRFY-05
  - SPEC-05
  - SPEC-06
  - WCMD-06
  - WCMD-07
  - UAT-02
  - UAT-03
  - UAT-05

must_haves:
  truths:
    - "Full verify pipeline works end-to-end: context-only JSON → simulated report → write-results → verification.md + gap-report.md + status.json + state transition"
    - "Full archive pipeline works: verified state + all MUST done → directory moved to archive → state = archived"
    - "Archive refuses when MUST items are not done"
    - "UAT file round-trip: write → read → update → read preserves history"
    - "All existing tests still pass (no regressions)"
  artifacts:
    - path: "internal/verifier/integration_test.go"
      provides: "End-to-end verification context → report → write-back test"
    - path: "cmd/integration_test.go"
      provides: "CLI-level integration tests for verify and archive commands"
  key_links:
    - from: "cmd/verify.go"
      to: "internal/verifier/"
      via: "Full pipeline: BuildVerificationContext → ParseVerifierReport → WriteVerificationReport → WriteGapReport"
      pattern: "verifier\\."
    - from: "cmd/archive.go"
      to: "internal/state/"
      via: "Full pipeline: LoadState → gate checks → moveDir → Transition → SaveState"
      pattern: "state\\."
---

<objective>
Write integration tests that exercise the full verification and archive pipelines end-to-end using fixture change directories. These tests validate that all components from Plans 01-04 work together correctly.

Purpose: Unit tests in Plans 01-03 test individual functions. These integration tests verify the complete data flow: spec parsing → context JSON → report processing → state transitions → file system mutations.
Output: Integration test files that validate the complete Phase 3 feedback loop
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-verification-feedback-loop/03-01-SUMMARY.md
@.planning/phases/03-verification-feedback-loop/03-02-SUMMARY.md
@.planning/phases/03-verification-feedback-loop/03-03-SUMMARY.md

<interfaces>
<!-- All Plan 01-03 exports needed for integration testing -->

From internal/verifier/context.go:
```go
func BuildVerificationContext(specsDir, changeName string) (VerificationContext, error)
func StableID(r spec.Requirement) string
```

From internal/verifier/report.go:
```go
func ParseVerifierReport(data []byte) (VerifierReport, error)
func WriteGapReport(changeDir string, report VerifierReport) error
func WriteVerificationReport(changeDir string, report VerifierReport) error
```

From internal/spec/updater.go:
```go
func ReadVerificationStatus(changeDir string) (VerificationStatus, error)
func WriteVerificationStatus(changeDir string, vs VerificationStatus) error
func UpdateItemStatus(changeDir string, reqID string, newStatus ItemStatus) error
```

From internal/uat/checklist.go:
```go
func NewUATChecklist(changeName string, items []UATItem) UATChecklist
func WriteUAT(filePath string, checklist UATChecklist) error
func ReadUAT(filePath string) (UATChecklist, error)
func UATFilePath(baseDir, changeName string) string
```

From internal/state/state.go:
```go
func LoadState(specsDir string) (WorkflowState, error)
func SaveState(specsDir string, ws WorkflowState) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verification pipeline integration test</name>
  <files>internal/verifier/integration_test.go</files>
  <read_first>
    - internal/verifier/context.go (BuildVerificationContext)
    - internal/verifier/report.go (ParseVerifierReport, WriteGapReport, WriteVerificationReport)
    - internal/spec/updater.go (ReadVerificationStatus, WriteVerificationStatus)
    - internal/spec/parser.go (ParseChange — to understand fixture structure needed)
    - internal/spec/writer.go (if exists — for fixture scaffolding pattern)
    - internal/executor/context_test.go (test fixture patterns from Phase 2)
  </read_first>
  <behavior>
    - Test: Full pipeline with all MUST pass: BuildVerificationContext → construct passing VerifierReport → WriteVerificationReport → WriteVerificationStatus → ReadVerificationStatus confirms all MUST = done
    - Test: Full pipeline with MUST failure: BuildVerificationContext → construct failing VerifierReport → WriteGapReport produces gap-report.md → ReadVerificationStatus confirms failed item = blocked
    - Test: StableID is consistent across two separate BuildVerificationContext calls on same fixture (ID stability)
    - Test: VerificationReport markdown contains MUST section before SHOULD section (D-02 ordering)
    - Test: GapReport only contains failed MUST items (not failed SHOULD)
  </behavior>
  <action>
    1. Create `internal/verifier/integration_test.go` with test helper `setupTestChange(t *testing.T) string` that creates a temp directory with:
       - `.openspec.yaml` with `schema: "1"`
       - `proposal.md` with basic frontmatter
       - `specs/capability-a/spec.md` with 3 MUST items, 2 SHOULD items, 1 MAY item (RFC 2119 keywords in body)
       - `design.md` with basic content
       - `tasks.md` with V2 frontmatter and 2 tasks (both status: done)

    2. **TestVerificationPipeline_AllPass**:
       - Call `BuildVerificationContext(specsDir, changeName)` — assert MustItems has 3 items, ShouldItems has 2, MayItems has 1
       - Construct a VerifierReport with all results passing
       - Call `WriteVerificationReport(changeDir, report)` — assert file exists, contains "## MUST Items"
       - Call `WriteVerificationStatus(changeDir, vs)` with all MUST = done
       - Call `ReadVerificationStatus(changeDir)` — assert all 3 MUST requirement IDs map to "done"

    3. **TestVerificationPipeline_MustFailure**:
       - Same setup, but construct report with 1 MUST failing
       - Call `WriteGapReport(changeDir, report)` — assert gap-report.md exists, contains failed item text
       - Assert gap-report.md does NOT contain passing MUST items
       - Call `WriteVerificationStatus` with mix of done/blocked
       - Assert ReadVerificationStatus shows 1 blocked

    4. **TestStableID_Consistency**:
       - Call BuildVerificationContext twice on same fixture
       - Assert MustItems[0].ID == MustItems[0].ID across both calls (deterministic)

    5. **TestVerificationReport_Ordering**:
       - Write report with mixed results
       - Read verification.md content
       - Assert strings.Index("## MUST Items") < strings.Index("## SHOULD Items") < strings.Index("## MAY Items")
  </action>
  <verify>
    <automated>go test ./internal/verifier/ -run TestVerificationPipeline -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/verifier/integration_test.go contains TestVerificationPipeline_AllPass, TestVerificationPipeline_MustFailure, TestStableID_Consistency, TestVerificationReport_Ordering
    - setupTestChange creates a valid change directory structure with spec files containing MUST/SHOULD/MAY keywords
    - Tests exercise the full chain: BuildVerificationContext → report construction → Write functions → Read functions
    - `go test ./internal/verifier/ -count=1` exits 0
  </acceptance_criteria>
  <done>End-to-end verification pipeline tested: context building, report processing, status write-back, gap report generation all work together correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Archive pipeline + UAT round-trip integration tests</name>
  <files>cmd/integration_test.go, internal/uat/integration_test.go</files>
  <read_first>
    - cmd/archive.go (runArchive logic, moveDir, gate checks)
    - cmd/verify.go (runVerifyContextOnly, runVerifyWriteResults)
    - internal/uat/checklist.go (WriteUAT, ReadUAT, UATFilePath)
    - internal/state/state.go (SaveState, LoadState)
    - internal/state/transitions.go (ValidTransitions)
    - cmd/execute_test.go (if exists — for cmd test patterns)
  </read_first>
  <behavior>
    - Test: Archive succeeds when state==verified and all MUST done — changeDir moved, STATE.json phase==archived, ARCHIVED-STATE.json exists in archive dir
    - Test: Archive fails with state==executed — returns error containing "must be verified"
    - Test: Archive fails with MUST item blocked — returns error containing "not done"
    - Test: Archive does not check UAT at all (UAT-02) — passes even with no UAT files
    - Test: UAT round-trip: NewUATChecklist → WriteUAT → ReadUAT → update results → WriteUAT → ReadUAT → run_history has 1 entry
    - Test: Full regression: `go test ./... -count=1` passes
  </behavior>
  <action>
    1. **cmd/integration_test.go**: Create archive integration tests using cobra command testing pattern (execute command via `archiveCmd.RunE(cmd, args)`):

       **TestArchiveIntegration_Success**:
       - Setup: create temp specsDir with changes/{name}/ containing valid spec files, STATE.json with phase=verified, verification-status.json with all MUST=done
       - Execute archive command
       - Assert: `specsDir/archive/{name}/` exists, `specsDir/changes/{name}/` does NOT exist, `specsDir/archive/{name}/ARCHIVED-STATE.json` exists, `specsDir/STATE.json` has phase=archived

       **TestArchiveIntegration_GateRejectsExecuted**:
       - Setup: STATE.json with phase=executed
       - Execute archive command
       - Assert: error contains "must be verified"

       **TestArchiveIntegration_GateRejectsMustNotDone**:
       - Setup: STATE.json with phase=verified, verification-status.json with 1 MUST=blocked
       - Execute archive command
       - Assert: error contains "not done"

       **TestArchiveIntegration_NoUATCheck** (UAT-02):
       - Setup: same as success case, but NO .mysd/uat/ directory at all
       - Execute archive with --yes flag (skip prompt)
       - Assert: succeeds (UAT absence does not block)

    2. **internal/uat/integration_test.go**: UAT round-trip test:

       **TestUATRoundTrip**:
       - Create checklist with NewUATChecklist("test-change", 3 items)
       - WriteUAT to temp path
       - ReadUAT — assert 3 items, all status "pending"
       - Modify items: set item 1 to pass, item 2 to fail with notes
       - WriteUAT again (should preserve history)
       - ReadUAT — assert RunHistory has 1 entry with original summary

       **TestUATRoundTrip_MultipleRuns** (UAT-05):
       - Write → update → write → update → write → read
       - Assert RunHistory has 2 entries (two completed runs before current)
       - Each entry has correct timestamp and summary

    3. After all integration tests pass, run `go test ./... -count=1` to confirm no regressions against Phase 1 and 2 tests.
  </action>
  <verify>
    <automated>go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - cmd/integration_test.go contains TestArchiveIntegration_Success, TestArchiveIntegration_GateRejectsExecuted, TestArchiveIntegration_GateRejectsMustNotDone, TestArchiveIntegration_NoUATCheck
    - internal/uat/integration_test.go contains TestUATRoundTrip, TestUATRoundTrip_MultipleRuns
    - Archive success test verifies: archive directory exists, change directory removed, ARCHIVED-STATE.json present, STATE.json phase=archived
    - UAT multi-run test verifies RunHistory length grows with each write
    - `go test ./... -count=1` exits 0 (full suite including Phase 1+2 tests)
  </acceptance_criteria>
  <done>Archive pipeline validates double gate enforcement and directory move; UAT round-trip validates history preservation across multiple sessions; full test suite passes with zero regressions</done>
</task>

</tasks>

<verification>
- `go test ./... -count=1` — FULL suite passes (Phase 1, 2, and 3 tests)
- `go vet ./...` — no vet errors
- `go build ./...` — builds cleanly
</verification>

<success_criteria>
- Integration tests prove the complete feedback loop: verify → write-back → archive
- Archive gate correctly blocks non-verified and incomplete changes
- UAT file format survives multiple read/write cycles with history
- Zero regressions against existing Phase 1 and Phase 2 test suites
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-feedback-loop/03-05-SUMMARY.md`
</output>
