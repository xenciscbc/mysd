---
phase: 03-verification-feedback-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/uat/checklist.go
  - internal/uat/checklist_test.go
autonomous: true
requirements:
  - UAT-01
  - UAT-03
  - UAT-04
  - UAT-05

must_haves:
  truths:
    - "UAT checklist can be created from a list of UI-related items"
    - "UAT checklist file persists in .mysd/uat/ directory across sessions"
    - "Existing UAT results are preserved when file is re-written (history append, not overwrite)"
    - "ReadUAT can read a previously written UAT file"
  artifacts:
    - path: "internal/uat/checklist.go"
      provides: "UAT checklist data model, read/write functions"
      exports: ["UATChecklist", "UATItem", "UATRunResult", "ReadUAT", "WriteUAT", "NewUATChecklist"]
    - path: "internal/uat/checklist_test.go"
      provides: "Tests for UAT CRUD and history preservation"
  key_links:
    - from: "internal/uat/checklist.go"
      to: ".mysd/uat/{change_name}-uat.md"
      via: "YAML frontmatter + markdown body file format"
      pattern: "yaml\\.Marshal|frontmatter\\.Parse"
---

<objective>
Build the UAT (User Acceptance Testing) checklist package: data model, read/write functions with YAML frontmatter, and history preservation logic.

Purpose: The UAT system is independent of the verifier — it reads/writes `.mysd/uat/` files. The verifier agent will call `WriteUAT` to generate checklists, and the `/mysd:uat` SKILL.md will call `ReadUAT` to load them for interactive sessions.
Output: `internal/uat/` package with checklist.go and tests
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-verification-feedback-loop/03-CONTEXT.md
@.planning/phases/03-verification-feedback-loop/03-RESEARCH.md

<interfaces>
<!-- Pattern reference for YAML frontmatter + markdown body format -->

From internal/spec/updater.go (file format pattern):
WriteTasks serializes YAML frontmatter + "--- delimiter" + body content.

From internal/spec/schema.go (ItemStatus reuse):
type ItemStatus string with constants: StatusPending="pending", StatusDone="done", StatusBlocked="blocked"

From 03-RESEARCH.md Pattern 7 (UAT file format):
UAT file uses YAML frontmatter with fields: spec-version, change, generated, last_run, summary (total/pass/fail/skip), results array (id, description, status, run_at). Body is markdown with checkbox items.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UAT data model and WriteUAT with directory creation</name>
  <files>internal/uat/checklist.go, internal/uat/checklist_test.go</files>
  <read_first>
    - internal/spec/updater.go (WriteTasks pattern for YAML frontmatter + markdown body)
    - internal/spec/schema.go (ItemStatus type for reuse consideration)
    - .planning/phases/03-verification-feedback-loop/03-RESEARCH.md (Pattern 7 UAT format, Pitfall 4 history preservation)
  </read_first>
  <behavior>
    - Test: NewUATChecklist creates a UATChecklist with change name, generated timestamp, and items list
    - Test: WriteUAT creates .mysd/uat/ directory if it does not exist
    - Test: WriteUAT writes {change_name}-uat.md with YAML frontmatter containing spec-version, change, generated, summary fields
    - Test: WriteUAT markdown body contains "## UAT Checklist: {change_name}" heading and "- [ ] {description}" checkboxes
    - Test: WriteUAT on existing file preserves previous results in run_history array (Pitfall 4)
    - Test: WriteUAT updates last_run and summary counts when results are present
  </behavior>
  <action>
    1. Create `internal/uat/checklist.go` with package `uat`.

    2. Define the data model:

       UATChecklist struct with fields:
       - SpecVersion string (yaml:"spec-version")
       - Change string (yaml:"change")
       - Generated string (yaml:"generated") — RFC3339 date
       - LastRun string (yaml:"last_run,omitempty") — RFC3339 timestamp
       - Summary UATSummary (yaml:"summary")
       - Results []UATItem (yaml:"results")
       - RunHistory []UATRunResult (yaml:"run_history,omitempty")

       UATSummary struct with fields: Total, Pass, Fail, Skip (all int)

       UATItem struct with fields:
       - ID string (yaml:"id") — "uat-1", "uat-2", ...
       - Description string (yaml:"description")
       - Status string (yaml:"status") — "pending" / "pass" / "fail" / "skip"
       - Notes string (yaml:"notes,omitempty")
       - RunAt string (yaml:"run_at,omitempty") — RFC3339 timestamp

       UATRunResult struct with fields: RunAt string, Summary UATSummary

    3. Implement `NewUATChecklist(changeName string, items []UATItem) UATChecklist` — sets SpecVersion to "1", Generated to `time.Now().Format(time.RFC3339)`, Summary.Total to len(items), all items default Status to "pending".

    4. Implement `UATFilePath(baseDir, changeName string) string` — returns `filepath.Join(baseDir, ".mysd", "uat", changeName+"-uat.md")`.

    5. Implement `WriteUAT(filePath string, checklist UATChecklist) error`:
       - `os.MkdirAll(filepath.Dir(filePath), 0755)` to ensure directory exists (UAT-03)
       - If file already exists, read it via ReadUAT, append current results as a UATRunResult to RunHistory (Pitfall 4 — history preservation, UAT-05)
       - Marshal frontmatter with yaml.Marshal, write YAML frontmatter delimiters around it
       - Write markdown body: "## UAT Checklist: {change}" heading, followed by generated-by note, followed by "### UI Acceptance Tests" heading, followed by "- [ ] {item.Description}" for each item

    6. Implement `ReadUAT(filePath string) (UATChecklist, error)`:
       - Use `adrg/frontmatter` to parse YAML frontmatter into UATChecklist struct
       - Return zero-value UATChecklist if file does not exist (not an error — UAT-04 allows repeated runs)

    Per D-10: UAT checklist generated during verify. Per D-11: read by mysd-uat-guide agent.
    Per UAT-05: run_history preserves historical results with timestamps.
  </action>
  <verify>
    <automated>go test ./internal/uat/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/uat/checklist.go exists and declares `package uat`
    - internal/uat/checklist.go exports UATChecklist, UATItem, UATSummary, UATRunResult, NewUATChecklist, WriteUAT, ReadUAT, UATFilePath
    - internal/uat/checklist_test.go has TestNewUATChecklist, TestWriteUAT, TestWriteUAT_PreservesHistory, TestReadUAT, TestReadUAT_FileNotExist
    - WriteUAT creates `.mysd/uat/` directory via os.MkdirAll
    - WriteUAT output file contains `spec-version: "1"` in frontmatter
    - WriteUAT on existing file appends to run_history (not overwrites)
    - ReadUAT on non-existent file returns zero-value UATChecklist (no error)
    - `go test ./internal/uat/... -count=1` exits 0
  </acceptance_criteria>
  <done>UAT checklist package reads and writes .mysd/uat/ files with YAML frontmatter, preserves run history across sessions, and handles missing files gracefully; all tests pass</done>
</task>

</tasks>

<verification>
- `go test ./internal/uat/... -count=1` — all tests pass
- `go vet ./internal/uat/...` — no vet errors
</verification>

<success_criteria>
- UAT file format matches Pattern 7 from research (YAML frontmatter + markdown checkboxes)
- History preservation prevents Pitfall 4 (overwrite vs append)
- .mysd/uat/ directory auto-created on first write
- ReadUAT gracefully handles missing files for first-time use
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-feedback-loop/03-02-SUMMARY.md`
</output>
