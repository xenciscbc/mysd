---
phase: 08-skill-md-orchestrators-agent-definitions
plan: 05
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - .claude/commands/mysd-fix.md
  - .claude/commands/mysd-ff.md
  - .claude/commands/mysd-ffe.md
  - plugin/commands/mysd-fix.md
  - plugin/commands/mysd-ff.md
  - plugin/commands/mysd-ffe.md
autonomous: false
requirements:
  - FCMD-02
  - FAUTO-03
  - FAUTO-04

must_haves:
  truths:
    - "/mysd:fix detects merge conflict vs implementation failure path automatically"
    - "/mysd:fix merge conflict path resolves conflicts, verifies build, merges, cleans up worktree"
    - "/mysd:fix implementation path optionally researches, respawns executor in worktree"
    - "/mysd:fix supports abandon path returning task to pending"
    - "/mysd:fix restores downstream skipped tasks transitively after successful fix"
    - "ff = plan (no research) + apply + archive with auto_mode hardcoded true"
    - "ffe = plan (with research) + apply + archive with auto_mode hardcoded true"
    - "ff/ffe do NOT invoke mysd-fast-forward agent (directly orchestrate pipeline)"
  artifacts:
    - path: ".claude/commands/mysd-fix.md"
      provides: "Fix orchestrator with dual-path detection"
      contains: "conflict"
    - path: ".claude/commands/mysd-ff.md"
      provides: "Fast-forward = plan + apply + archive"
      contains: "auto_mode"
    - path: ".claude/commands/mysd-ffe.md"
      provides: "Full fast-forward = research + plan + apply + archive"
      contains: "mysd-researcher"
    - path: "plugin/commands/mysd-fix.md"
      provides: "Fix orchestrator (plugin)"
      contains: "conflict"
    - path: "plugin/commands/mysd-ff.md"
      provides: "ff (plugin)"
      contains: "auto_mode"
    - path: "plugin/commands/mysd-ffe.md"
      provides: "ffe (plugin)"
      contains: "mysd-researcher"
  key_links:
    - from: ".claude/commands/mysd-fix.md"
      to: ".claude/agents/mysd-executor.md"
      via: "Task tool spawn for re-execution"
      pattern: "Agent: mysd-executor"
    - from: ".claude/commands/mysd-ff.md"
      to: ".claude/commands/mysd-apply.md"
      via: "Pipeline stage (plan -> apply -> archive)"
      pattern: "apply"
    - from: ".claude/commands/mysd-ffe.md"
      to: ".claude/agents/mysd-researcher.md"
      via: "Task tool spawn x4"
      pattern: "Agent: mysd-researcher"
---

<objective>
Create /mysd:fix orchestrator and rewrite /mysd:ff and /mysd:ffe for the new pipeline model.

Purpose: FCMD-02 requires a fix command with dual-path detection (merge conflict vs implementation failure). FAUTO-03/04 require ff/ffe to use auto mode and skip research (ff) or include research (ffe), both using the direct pipeline model instead of the mysd-fast-forward agent.

Output: 3 SKILL.md files (fix, ff, ffe) in both .claude/commands/ and plugin/commands/
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-skill-md-orchestrators-agent-definitions/08-CONTEXT.md
@.planning/phases/08-skill-md-orchestrators-agent-definitions/08-RESEARCH.md
@.planning/phases/08-skill-md-orchestrators-agent-definitions/08-03-SUMMARY.md

<interfaces>
<!-- Fix binary commands available (D-13, no new binary needed): -->
- `mysd execute --context-only` — outputs JSON with failed task info, wave_groups
- `mysd task-update {id} {status}` — update task status (pending/in_progress/done/blocked)
- `mysd worktree remove {id} {branch}` — remove worktree
- `mysd worktree list` — list active worktrees

<!-- Fix dual-path logic (D-08, D-14): -->
Merge conflict: conflict markers in worktree files -> resolve -> build -> merge -> cleanup
Implementation: build/test failure, no conflicts -> diagnose -> optional research -> re-execute

<!-- ff/ffe new pipeline (D-24, D-25): -->
ff = plan (no research) + apply + archive (auto_mode: true)
ffe = plan (4-dim research) + apply + archive (auto_mode: true)

<!-- auto_mode in ff/ffe (D-19, D-20): -->
ff/ffe hardcode auto_mode: true, don't parse --auto
ff does NOT use research (FAUTO-04)
ffe DOES use research (for design technical basis)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /mysd:fix SKILL.md orchestrator</name>
  <files>
    .claude/commands/mysd-fix.md
    plugin/commands/mysd-fix.md
  </files>
  <read_first>
    .claude/commands/mysd-execute.md
    .claude/commands/mysd-apply.md
    .claude/agents/mysd-executor.md
    .claude/agents/mysd-researcher.md
    .planning/phases/08-skill-md-orchestrators-agent-definitions/08-CONTEXT.md
  </read_first>
  <action>
Create `.claude/commands/mysd-fix.md`:

**Frontmatter:**
```yaml
---
model: claude-sonnet-4-5
description: Fix a failed task. Auto-detects merge conflict vs implementation failure. Supports optional research for implementation issues. Usage: /mysd:fix [change-name] [T{id}]
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Task
---
```

**Body — /mysd:fix:**

```markdown
# /mysd:fix -- Fix Failed Task

You are the mysd fix orchestrator. Your job is to diagnose and fix a failed task, either resolving merge conflicts or re-executing after implementation diagnosis.

## Step 1: Parse Arguments (D-09)

Check `$ARGUMENTS`:
- Format 1: `T{id}` — fix task T{id} of current active change
- Format 2: `{change-name} T{id}` — fix task T{id} of specified change
- No arguments: proceed to Step 2 for task selection

## Step 2: Identify Target Task (D-10)

If no task ID provided:
  Run: `mysd execute --context-only`
  Parse JSON for tasks with status "failed" or "blocked".
  List them:
  ```
  Failed/blocked tasks:
    T2 (setup-auth) — failed: merge conflict
    T5 (add-validation) — blocked: depends on T2
  ```
  Ask user: "Which task would you like to fix?"

Set `target_task` and `change_name`.

## Step 3: Get Task Context

Run: `mysd execute --context-only`
Parse:
- Task details (id, name, description, status)
- Worktree info (if exists): path, branch
- Failure reason from task sidecar (if available)

Run: `mysd worktree list`
Check if worktree for T{id} exists.

## Step 4: Path Detection (D-08)

Auto-detect which fix path to use:

**Check for merge conflict:**
If worktree exists for this task:
  Read files in worktree path, search for conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`)
  If conflict markers found -> PATH = "merge_conflict"

**Check for implementation failure:**
If no conflict markers but task is failed:
  Read task sidecar for failure details (build errors, test failures)
  PATH = "implementation"

**Present detection to user (D-08 safety valve):**
"Detected: {merge conflict | implementation failure}. Proceed with {path}? (Y/n)"

## Step 5A: Merge Conflict Path (D-14)

1. **Navigate to worktree:**
   Read conflicted files in `{worktree_path}`

2. **Resolve conflicts:**
   Read each conflicted file. Remove conflict markers by choosing the correct resolution.
   Use Edit tool to fix each file.

3. **Complete merge:**
   ```
   cd {worktree_path}
   git add -A
   git commit -m "fix({change_name}): resolve merge conflict for T{id}"
   ```

4. **Verify build:**
   ```
   cd {worktree_path}
   go build ./...
   go test ./...
   ```
   If build/test fails: attempt to fix (up to 3 retries). If still failing, ask user.

5. **Merge to main branch:**
   ```
   git checkout {main_branch}
   git merge --no-ff {task_branch}
   ```

6. **Cleanup:**
   ```
   mysd worktree remove {id} {branch}
   git branch -D {branch}
   mysd task-update {id} done
   ```

7. **Restore downstream tasks (D-14):**
   Run `mysd execute --context-only` to get wave_groups
   Find all tasks that were skipped due to dependency on T{id} (transitively)
   For each skipped downstream task:
     `mysd task-update {downstream_id} pending`

## Step 5B: Implementation Failure Path (D-14)

1. **Diagnose (D-12):**
   Read task sidecar: failure reason, AI attempts, test output
   Analyze and present diagnosis to user

2. **Optional research (D-11):**
   Ask: "Would you like to research this issue? [y/N]"
   If yes:
     Task: Research implementation issue
     Agent: mysd-researcher
     Context: {
       "change_name": "{change_name}",
       "dimension": "codebase",
       "topic": "Fix: {failure reason summary}",
       "spec_files": [{relevant spec files}],
       "auto_mode": false
     }

3. **Cleanup old worktree/branch:**
   ```
   mysd worktree remove {id} {branch}
   git branch -D {branch}
   mysd task-update {id} pending
   ```

4. **Re-execute task:**
   Spawn executor in fresh worktree:
   Task: Re-execute task T{id} after fix
   Agent: mysd-executor
   Context: {
     "change_name": "{change_name}",
     "must_items": [...],
     "should_items": [...],
     "may_items": [...],
     "tasks": [...],
     "assigned_task": {target task with updated description},
     "tdd_mode": {from context},
     "atomic_commits": {from context},
     "worktree_path": ".worktrees/T{id}",
     "branch": "mysd/{change_name}/T{id}-{slug}",
     "isolation": "worktree",
     "auto_mode": false
   }

5. **Restore downstream tasks** (same as Step 5A-7)

## Step 5C: Abandon Path

If user chooses to abandon:
1. `mysd task-update {id} pending`
2. `mysd worktree remove {id} {branch}` (if exists)
3. `git branch -D {branch}` (if exists)
4. Show: "Task T{id} returned to pending. Worktree cleaned up."

## Step 6: Summary

Show:
- Path taken (merge conflict / implementation / abandon)
- Task final status
- Downstream tasks restored (if any)
- Next: `/mysd:apply` to continue execution
```

Copy identical content to `plugin/commands/mysd-fix.md`.
  </action>
  <verify>
    <automated>bash -c "test -f .claude/commands/mysd-fix.md && echo 'fix exists'; grep -c 'conflict' .claude/commands/mysd-fix.md; grep -c 'mysd-executor' .claude/commands/mysd-fix.md; grep -c 'mysd-researcher' .claude/commands/mysd-fix.md; grep -c 'task-update' .claude/commands/mysd-fix.md; grep -c 'worktree remove' .claude/commands/mysd-fix.md; grep -c 'Abandon' .claude/commands/mysd-fix.md"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/commands/mysd-fix.md` exists
    - Contains `conflict` at least 3 times (detection, path name, resolution)
    - Contains `mysd-executor` (re-execution spawn)
    - Contains `mysd-researcher` (optional research)
    - Contains `task-update` at least 3 times (done after merge, pending for re-execute, pending for abandon)
    - Contains `worktree remove` at least 2 times (merge cleanup, implementation cleanup)
    - Contains `Abandon` or `abandon` (abandon path)
    - Contains `downstream` or `skipped` (transitive task recovery)
    - `diff .claude/commands/mysd-fix.md plugin/commands/mysd-fix.md` returns empty
  </acceptance_criteria>
  <done>/mysd:fix orchestrator created with dual-path detection, merge conflict resolution, implementation re-execution, abandon path, and transitive downstream task recovery</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite /mysd:ff and /mysd:ffe for direct pipeline model</name>
  <files>
    .claude/commands/mysd-ff.md
    .claude/commands/mysd-ffe.md
    plugin/commands/mysd-ff.md
    plugin/commands/mysd-ffe.md
  </files>
  <read_first>
    .claude/commands/mysd-ff.md
    .claude/commands/mysd-ffe.md
    plugin/commands/mysd-ff.md
    plugin/commands/mysd-ffe.md
    .claude/commands/mysd-plan.md
    .claude/commands/mysd-apply.md
    .planning/phases/08-skill-md-orchestrators-agent-definitions/08-CONTEXT.md
  </read_first>
  <action>
**A. Rewrite `.claude/commands/mysd-ff.md` (D-24, FAUTO-03, FAUTO-04):**

Frontmatter:
```yaml
---
model: claude-sonnet-4-5
description: Fast-forward through plan + apply + archive. Assumes spec is ready. No research. Implies --auto. Usage: /mysd:ff [change-name]
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Task
---
```

Body — `/mysd:ff`:

```markdown
# /mysd:ff -- Fast-Forward (Plan + Apply + Archive)

You are the mysd fast-forward orchestrator. Run the pipeline: plan (no research) -> apply -> archive. Auto mode is always on.

## Step 1: Get Change Name

Get change name from `$ARGUMENTS`. If not provided, check `mysd status` for active change. If none, ask user.

Set `auto_mode = true` (always, per D-19/FAUTO-03).

## Step 2: Plan Phase (no research, per FAUTO-04)

Run: `mysd plan --context-only`
Parse JSON.

Spawn designer:
  Task: Create design for {change_name} (ff mode)
  Agent: mysd-designer
  Context: { "change_name": "...", "specs": [...], "research_findings": [], "auto_mode": true }

Run: `mysd design`

Spawn planner:
  Task: Create task list for {change_name} (ff mode)
  Agent: mysd-planner
  Context: { full context JSON, "auto_mode": true }

Run: `mysd plan`

## Step 3: Apply Phase

Run: `mysd execute --context-only`
Parse JSON (tasks, pending_tasks, wave_groups, etc.).

Execute tasks using the same logic as /mysd:apply Step 3:
- Single mode: sequential per-task spawn of mysd-executor with auto_mode: true
- Wave mode: parallel per-task spawn with worktree isolation, auto_mode: true

Run: `mysd execute` (state transition)

## Step 4: Archive

Run: `mysd archive`

## Step 5: Confirm

Show: "Fast-forward complete. Change `{change_name}` has been planned, executed, and archived."
```

NOTE: ff does NOT spawn `mysd-fast-forward` agent. It does NOT include research step. It hardcodes `auto_mode: true`.

Copy to `plugin/commands/mysd-ff.md`.

**B. Rewrite `.claude/commands/mysd-ffe.md` (D-25, FAUTO-03, FAUTO-04):**

Frontmatter:
```yaml
---
model: claude-sonnet-4-5
description: Full fast-forward with research + plan + apply + archive. Implies --auto. Usage: /mysd:ffe [change-name]
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Task
---
```

Body — `/mysd:ffe`:

```markdown
# /mysd:ffe -- Full Fast-Forward (Research + Plan + Apply + Archive)

You are the mysd full fast-forward orchestrator. Run the pipeline: 4-dimension research -> plan -> apply -> archive. Auto mode is always on.

## Step 1: Get Change Name

Get change name from `$ARGUMENTS`. If not provided, check `mysd status` for active change. If none, ask user.

Set `auto_mode = true` (always, per D-19/FAUTO-03).

## Step 2: Research Phase

Spawn 4 `mysd-researcher` agents in parallel:

For each dimension in ["codebase", "domain", "architecture", "pitfalls"]:
  Task: Research {dimension} for {change_name} (ffe mode)
  Agent: mysd-researcher
  Context: {
    "change_name": "{change_name}",
    "dimension": "{dimension}",
    "topic": "{change description from spec}",
    "spec_files": [{spec file paths}],
    "auto_mode": true
  }

Collect all 4 research outputs.

## Step 3: Plan Phase (with research findings)

Run: `mysd plan --context-only`
Parse JSON.

Spawn designer with research:
  Task: Create design for {change_name} (ffe mode)
  Agent: mysd-designer
  Context: { "change_name": "...", "specs": [...], "research_findings": [{from Step 2}], "auto_mode": true }

Run: `mysd design`

Spawn planner:
  Task: Create task list for {change_name} (ffe mode)
  Agent: mysd-planner
  Context: { full context JSON, "auto_mode": true }

Run: `mysd plan`

## Step 4: Apply Phase

Same as /mysd:ff Step 3 — execute all tasks with auto_mode: true.

## Step 5: Archive

Run: `mysd archive`

## Step 6: Confirm

Show: "Full fast-forward complete. Change `{change_name}` has been researched, planned, executed, and archived."
```

NOTE: ffe DOES include research (D-25). It does NOT spawn `mysd-fast-forward` agent. It hardcodes `auto_mode: true`.

Copy to `plugin/commands/mysd-ffe.md`.
  </action>
  <verify>
    <automated>bash -c "echo '--- ff checks ---'; grep -c 'auto_mode.*true' .claude/commands/mysd-ff.md; grep -c 'mysd-fast-forward' .claude/commands/mysd-ff.md; grep -c 'research' .claude/commands/mysd-ff.md; echo '--- ffe checks ---'; grep -c 'auto_mode.*true' .claude/commands/mysd-ffe.md; grep -c 'mysd-fast-forward' .claude/commands/mysd-ffe.md; grep -c 'mysd-researcher' .claude/commands/mysd-ffe.md"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/commands/mysd-ff.md` contains `auto_mode` set to `true` (hardcoded, not parsed)
    - `.claude/commands/mysd-ff.md` does NOT contain `mysd-fast-forward` (no longer uses that agent)
    - `.claude/commands/mysd-ff.md` does NOT contain `mysd-researcher` (no research in ff, FAUTO-04)
    - `.claude/commands/mysd-ff.md` contains `mysd-designer` and `mysd-planner` (direct pipeline)
    - `.claude/commands/mysd-ff.md` contains `mysd-executor` (apply step)
    - `.claude/commands/mysd-ff.md` contains `mysd archive` (archive step)
    - `.claude/commands/mysd-ffe.md` contains `auto_mode` set to `true`
    - `.claude/commands/mysd-ffe.md` does NOT contain `mysd-fast-forward`
    - `.claude/commands/mysd-ffe.md` DOES contain `mysd-researcher` (ffe includes research)
    - `.claude/commands/mysd-ffe.md` contains `mysd-designer`, `mysd-planner`, `mysd-executor`
    - `diff .claude/commands/mysd-ff.md plugin/commands/mysd-ff.md` returns empty
    - `diff .claude/commands/mysd-ffe.md plugin/commands/mysd-ffe.md` returns empty
  </acceptance_criteria>
  <done>ff rewritten as plan+apply+archive pipeline (no research, auto mode); ffe rewritten as research+plan+apply+archive pipeline (with research, auto mode); neither uses mysd-fast-forward agent</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human audit of all agent definitions and SKILL.md commands</name>
  <what-built>
All Phase 8 deliverables: 3 new agent definitions, 2 agent rewrites, 2 new SKILL.md commands, 6 SKILL.md rewrites, --auto flag support, agent Task tool audit
  </what-built>
  <how-to-verify>
**Agent Task tool audit (FAGENT-05):**
1. Run: `grep -n '  - Task' .claude/agents/*.md`
2. Expected: ZERO results. No agent definition should have Task in allowed-tools.
3. Run: `grep -rn 'Task tool\|spawn.*agent\|invoke.*agent' .claude/agents/*.md`
4. Expected: ZERO results. No agent should describe spawning other agents.

**File completeness check:**
5. Run: `ls .claude/agents/mysd-researcher.md .claude/agents/mysd-advisor.md .claude/agents/mysd-proposal-writer.md .claude/agents/mysd-plan-checker.md`
6. Run: `ls .claude/commands/mysd-discuss.md .claude/commands/mysd-fix.md .claude/commands/mysd-apply.md`
7. All files should exist.

**ff/ffe behavior verification:**
8. Confirm `.claude/commands/mysd-ff.md` does NOT reference `mysd-fast-forward` agent
9. Confirm `.claude/commands/mysd-ff.md` does NOT contain research step
10. Confirm `.claude/commands/mysd-ffe.md` DOES contain research step

**Sync verification:**
11. Run: `diff -rq .claude/agents/ plugin/agents/ --exclude=CLAUDE.md`
12. Run: `diff -rq .claude/commands/ plugin/commands/ --exclude=CLAUDE.md --exclude=spectra`
13. All matching files should be identical.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn '  - Task' .claude/agents/*.md` returns 0 results (FAGENT-05)
- `/mysd:fix` exists with dual-path detection
- `/mysd:ff` is plan+apply+archive (no research, auto mode)
- `/mysd:ffe` is research+plan+apply+archive (with research, auto mode)
- All .claude/ files synced with plugin/ counterparts
- No agent spawns subagents (FAGENT-05 audit passed)
</verification>

<success_criteria>
- fix orchestrator handles merge conflicts, implementation failures, and abandon
- ff/ffe use direct pipeline model without mysd-fast-forward agent
- Human audit confirms zero Task tool violations in all 9+ agent definitions
- All files synced between .claude/ and plugin/
</success_criteria>

<output>
After completion, create `.planning/phases/08-skill-md-orchestrators-agent-definitions/08-05-SUMMARY.md`
</output>
