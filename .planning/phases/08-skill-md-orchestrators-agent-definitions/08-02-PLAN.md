---
phase: 08-skill-md-orchestrators-agent-definitions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/mysd-executor.md
  - .claude/agents/mysd-spec-writer.md
  - plugin/agents/mysd-executor.md
  - plugin/agents/mysd-spec-writer.md
autonomous: true
requirements:
  - FAGENT-05
  - FAGENT-06
  - FAGENT-07

must_haves:
  truths:
    - "mysd-executor handles only a single assigned_task, no sequential loop over pending_tasks"
    - "mysd-spec-writer handles only a single capability area per invocation, not all specs at once"
    - "All 9 agent definitions (executor, spec-writer, planner, designer, verifier, scanner, uat-guide, fast-forward, plan-checker) have zero Task tool in allowed-tools"
  artifacts:
    - path: ".claude/agents/mysd-executor.md"
      provides: "Per-task executor agent"
      contains: "assigned_task"
    - path: ".claude/agents/mysd-spec-writer.md"
      provides: "Per-spec-file spec writer agent"
      contains: "capability_area"
    - path: "plugin/agents/mysd-executor.md"
      provides: "Per-task executor (plugin)"
      contains: "assigned_task"
    - path: "plugin/agents/mysd-spec-writer.md"
      provides: "Per-spec-file spec writer (plugin)"
      contains: "capability_area"
  key_links:
    - from: ".claude/agents/mysd-executor.md"
      to: "plugin/agents/mysd-executor.md"
      via: "identical content"
      pattern: "assigned_task"
---

<objective>
Rewrite mysd-executor to per-task spawn model and mysd-spec-writer to per-spec-file model. Audit all 9 existing agents for Task tool violations.

Purpose: FAGENT-07 requires executor to handle one task per invocation (SKILL.md orchestrator does the loop). FAGENT-06 requires spec-writer to handle one capability area per invocation. FAGENT-05 requires no agent to contain Task tool references.

Output: 2 rewritten agents + audit confirmation of all 9 agents
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-skill-md-orchestrators-agent-definitions/08-CONTEXT.md
@.planning/phases/08-skill-md-orchestrators-agent-definitions/08-RESEARCH.md

<interfaces>
<!-- Current executor agent input format (must change) -->
Current mysd-executor.md accepts:
- `pending_tasks`: Array of all pending tasks (REMOVE this for single mode)
- `assigned_task`: Single task in wave mode (KEEP this, make it the ONLY mode)
- `execution_mode`: "single" or "wave" (REMOVE — always single-task now)

<!-- Current spec-writer pattern (must change) -->
Current mysd-spec-writer.md:
- Step 3: "Write Spec Files" — writes ALL capability areas in one invocation
- Must change to: receive ONE `capability_area` and write ONE spec file

<!-- Agent audit targets (all must have NO Task in allowed-tools) -->
.claude/agents/mysd-planner.md — verify no Task
.claude/agents/mysd-designer.md — verify no Task
.claude/agents/mysd-verifier.md — verify no Task
.claude/agents/mysd-scanner.md — verify no Task
.claude/agents/mysd-uat-guide.md — verify no Task
.claude/agents/mysd-fast-forward.md — verify no Task
plugin/agents/mysd-plan-checker.md — verify no Task (already confirmed compliant)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite mysd-executor for per-task spawn model</name>
  <files>
    .claude/agents/mysd-executor.md
    plugin/agents/mysd-executor.md
  </files>
  <read_first>
    .claude/agents/mysd-executor.md
    plugin/agents/mysd-executor.md
    .planning/phases/08-skill-md-orchestrators-agent-definitions/08-CONTEXT.md
  </read_first>
  <action>
Rewrite `.claude/agents/mysd-executor.md` per D-15:

**Frontmatter** (keep existing):
```yaml
---
model: claude-sonnet-4-5
description: Executor agent. Implements a single spec task with mandatory alignment gate before any code is written.
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
---
```
Note: description changed from "spec tasks" (plural) to "a single spec task" (singular). No Task tool.

**Input section changes:**
- REMOVE `pending_tasks` field — agent no longer receives a list of tasks
- REMOVE `execution_mode` field — there is no "single vs wave" distinction at agent level
- KEEP `assigned_task` — this is now the ONLY task input, always present
- KEEP all other fields: `change_name`, `must_items`, `should_items`, `may_items`, `tasks` (all tasks for reference), `tdd_mode`, `atomic_commits`
- ADD `worktree_path` (optional, present in wave mode — agent works in this directory)
- ADD `branch` (optional, present in wave mode)
- ADD `isolation` (optional, "worktree" when in wave mode)
- ADD `auto_mode` boolean

**Alignment Gate** — Keep the full 4-step alignment gate (Steps 1-4). This runs ONCE for the single assigned task.

**Task Execution changes:**
- REMOVE the "For each task in `pending_tasks`" loop
- Change to: "Execute the `assigned_task`"
- Step 1: Mark In Progress — `mysd task-update {assigned_task.id} in_progress`
- Step 2: TDD Mode (if tdd_mode) — same RED/GREEN/REFACTOR cycle
- Step 3: Implement — same implementation logic but only for assigned_task
- Step 4: Mark Done — `mysd task-update {assigned_task.id} done`
- Step 5: Atomic Commit (if atomic_commits) — same
- REMOVE "Repeat Steps 1-5 for each pending task" line

**Post-Execution Test Generation** — Keep as-is (only runs for files modified by this single task)

**Completion Summary** — Change from plural "Tasks completed" to singular "Task completed: {task name}"

Copy identical content to `plugin/agents/mysd-executor.md`.
  </action>
  <verify>
    <automated>bash -c "grep -c 'pending_tasks' .claude/agents/mysd-executor.md; grep -c 'assigned_task' .claude/agents/mysd-executor.md; grep -c 'For each task' .claude/agents/mysd-executor.md; grep -c 'execution_mode' .claude/agents/mysd-executor.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pending_tasks' .claude/agents/mysd-executor.md` returns 0
    - `grep -c 'execution_mode' .claude/agents/mysd-executor.md` returns 0
    - `grep 'assigned_task' .claude/agents/mysd-executor.md` returns at least 3 matches (Input, Step 1, Step 3)
    - `grep 'For each task' .claude/agents/mysd-executor.md` returns 0 (no loop)
    - `grep 'Task' .claude/agents/mysd-executor.md` does NOT appear in allowed-tools section
    - `diff .claude/agents/mysd-executor.md plugin/agents/mysd-executor.md` returns empty (identical)
    - `.claude/agents/mysd-executor.md` contains `auto_mode` in Input section
    - `.claude/agents/mysd-executor.md` contains `worktree_path` in Input section
  </acceptance_criteria>
  <done>Executor agent handles exactly one assigned_task per invocation; no sequential loop; no Task tool; .claude/ and plugin/ copies identical</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite mysd-spec-writer for per-spec-file model + audit all 9 agents</name>
  <files>
    .claude/agents/mysd-spec-writer.md
    plugin/agents/mysd-spec-writer.md
  </files>
  <read_first>
    .claude/agents/mysd-spec-writer.md
    plugin/agents/mysd-spec-writer.md
    .claude/agents/mysd-planner.md
    .claude/agents/mysd-designer.md
    .claude/agents/mysd-verifier.md
    .claude/agents/mysd-scanner.md
    .claude/agents/mysd-uat-guide.md
    .claude/agents/mysd-fast-forward.md
    plugin/agents/mysd-plan-checker.md
  </read_first>
  <action>
**A. Rewrite `.claude/agents/mysd-spec-writer.md` per D-16:**

**Frontmatter:**
```yaml
---
model: claude-sonnet-4-5
description: Spec writer agent. Receives a single capability area and writes one RFC 2119 requirement spec file.
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
---
```
Note: description changed to "single capability area" and "one ... spec file". No Task tool.

**Input section changes:**
- KEEP `change_name`, `phase`, `proposal` (proposal body text), `model`
- ADD `capability_area`: string — the specific capability area to write a spec for (e.g., "authentication", "data-validation")
- ADD `auto_mode`: boolean
- ADD `existing_spec_body`: optional string — if updating an existing spec file, the current content

**Step changes:**
- Step 1: Read the Proposal — Keep as-is
- Step 2: REMOVE "Discuss Capability Priorities" — This is now the SKILL.md orchestrator's job. The agent receives a pre-determined capability_area.
- Step 3: Write ONE Spec File — Change from "Create spec files" (plural) to "Create spec file for `{capability_area}`". Write ONE file: `.specs/changes/{change_name}/specs/{capability-slug}.md`. Keep the frontmatter + content format exactly as-is. REMOVE "Write at least one spec file. Large changes may need 2-4 spec files" — this agent writes exactly one.
- Step 4: Verify Spec File — `ls .specs/changes/{change_name}/specs/{capability-slug}.md`
- Step 5: REMOVE "Transition State" — State transition is now the SKILL.md orchestrator's responsibility (called after ALL spec-writers complete)
- Step 6: Confirm — "Spec file `{capability-slug}.md` written with {N} MUST, {M} SHOULD, {K} MAY requirements."

**auto_mode handling:** If true, skip any clarification questions and write based on proposal content directly.

Copy identical content to `plugin/agents/mysd-spec-writer.md`.

**B. Audit all 9 existing agents for Task tool violations (FAGENT-05):**

Run `grep -n 'Task' .claude/agents/*.md` and check each result:
1. `mysd-planner.md` — Check allowed-tools frontmatter for `Task`. If found, REMOVE it. Check body for "Use the Task tool" or "spawn" language. If found, REMOVE those instructions.
2. `mysd-designer.md` — Same check
3. `mysd-verifier.md` — Same check
4. `mysd-scanner.md` — Same check
5. `mysd-uat-guide.md` — Same check
6. `mysd-fast-forward.md` — Same check. NOTE: fast-forward agent currently describes spawning executor — this must be removed since ff/ffe SKILL.md orchestrator will handle spawning directly.
7. `mysd-plan-checker.md` (in .claude/agents/ after Plan 01 syncs it) — Already confirmed compliant
8. `mysd-executor.md` — Already rewritten in Task 1
9. `mysd-spec-writer.md` — Already rewritten above

For any agent that has Task tool violations:
- Remove `Task` from allowed-tools frontmatter
- Remove any "Use the Task tool to invoke" instructions from body
- Remove any "spawn" or "delegate to another agent" language
- Sync changes to corresponding `plugin/agents/` file

Document audit results as a comment block at the end of this task's summary.
  </action>
  <verify>
    <automated>bash -c "echo '--- Spec-writer checks ---'; grep -c 'capability_area' .claude/agents/mysd-spec-writer.md; grep -c 'Discuss Capability' .claude/agents/mysd-spec-writer.md; echo '--- Task tool audit ---'; for f in .claude/agents/mysd-planner.md .claude/agents/mysd-designer.md .claude/agents/mysd-verifier.md .claude/agents/mysd-scanner.md .claude/agents/mysd-uat-guide.md .claude/agents/mysd-fast-forward.md .claude/agents/mysd-executor.md .claude/agents/mysd-spec-writer.md; do echo -n \"$f: \"; grep -c '  - Task' $f 2>/dev/null || echo 0; done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'capability_area' .claude/agents/mysd-spec-writer.md` returns at least 2
    - `grep -c 'Discuss Capability' .claude/agents/mysd-spec-writer.md` returns 0 (removed)
    - `grep 'mysd spec' .claude/agents/mysd-spec-writer.md` returns 0 (state transition removed from agent)
    - `diff .claude/agents/mysd-spec-writer.md plugin/agents/mysd-spec-writer.md` returns empty
    - For ALL 8 agents in .claude/agents/ (planner, designer, verifier, scanner, uat-guide, fast-forward, executor, spec-writer): `grep -c '  - Task' {file}` returns 0
    - `mysd-fast-forward.md` does NOT contain "Use the Task tool" or "spawn" language
  </acceptance_criteria>
  <done>Spec-writer handles one capability area per invocation; all 9 agents (8 existing + plan-checker) pass Task tool audit with zero violations</done>
</task>

</tasks>

<verification>
- `grep -rn '  - Task' .claude/agents/*.md` returns zero results
- mysd-executor.md contains `assigned_task` but not `pending_tasks` loop
- mysd-spec-writer.md contains `capability_area` but not multi-spec loop
- All .claude/agents/ files are synced with plugin/agents/ counterparts
</verification>

<success_criteria>
- Executor rewritten for per-task model, spec-writer for per-spec model
- All 9 agent definitions pass Task tool audit (zero violations)
- .claude/agents/ and plugin/agents/ in sync for modified files
</success_criteria>

<output>
After completion, create `.planning/phases/08-skill-md-orchestrators-agent-definitions/08-02-SUMMARY.md`
</output>
