---
phase: 04-plugin-layer-distribution
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - main.go
  - cmd/propose.go
  - cmd/spec.go
  - cmd/design.go
  - cmd/plan.go
  - cmd/execute.go
  - cmd/verify.go
  - cmd/archive.go
  - cmd/ff.go
  - cmd/ffe.go
  - cmd/capture.go
  - cmd/status.go
  - cmd/task_update.go
  - cmd/scan.go
  - cmd/init_cmd.go
  - cmd/scan_test.go
  - cmd/execute_test.go
  - cmd/verify_test.go
  - cmd/archive_test.go
  - cmd/ff_test.go
  - cmd/status_test.go
  - cmd/task_update_test.go
  - cmd/integration_test.go
  - internal/scanner/scanner.go
  - internal/scanner/scanner_test.go
  - internal/roadmap/roadmap.go
  - internal/roadmap/roadmap_test.go
  - internal/roadmap/mermaid_test.go
  - internal/executor/context.go
  - internal/executor/context_test.go
  - internal/executor/status.go
  - internal/executor/status_test.go
  - internal/executor/progress.go
  - internal/executor/progress_test.go
  - internal/verifier/context.go
  - internal/verifier/context_test.go
  - internal/verifier/report_test.go
  - internal/verifier/integration_test.go
  - internal/uat/checklist_test.go
  - internal/uat/integration_test.go
  - plugin/hooks/hooks.json
  - plugin/.claude-plugin/plugin.json
  - .github/workflows/release.yml
autonomous: false
gap_closure: true
requirements:
  - DIST-03
  - DIST-04
  - WCMD-09
  - RMAP-01
  - RMAP-02
  - RMAP-03

must_haves:
  truths:
    - "go.mod module path is a real GitHub repository URL (not placeholder github.com/mysd)"
    - "All internal import paths use the real module path consistently"
    - "go build succeeds with the updated module path"
    - "go test ./... passes with updated import paths"
    - "plugin/hooks/hooks.json install command uses the real repository URL"
    - "plugin/.claude-plugin/plugin.json repository field uses the real repository URL"
    - ".github/workflows/release.yml exists and triggers GoReleaser on tag push"
  artifacts:
    - path: "go.mod"
      provides: "Real module path"
      contains: "module github.com/"
    - path: ".github/workflows/release.yml"
      provides: "CI release pipeline"
      contains: "goreleaser"
    - path: "plugin/hooks/hooks.json"
      provides: "Real install command"
    - path: "plugin/.claude-plugin/plugin.json"
      provides: "Real repository URL"
  key_links:
    - from: "go.mod"
      to: "all *.go files"
      via: "import paths"
      pattern: "github\\.com/.+/mysd"
    - from: ".github/workflows/release.yml"
      to: ".goreleaser.yaml"
      via: "goreleaser-action"
      pattern: "goreleaser/goreleaser-action"
---

<objective>
Close 2 verification gaps from Phase 4: (1) replace placeholder module path `github.com/mysd` with the user's real GitHub repository URL across go.mod, all import paths, and plugin metadata files; (2) create GitHub Actions release workflow to trigger GoReleaser on tag push.

Purpose: Without a real module path, `go install` cannot work (DIST-03 blocker). Without a CI workflow, GitHub Releases cannot be published automatically.
Output: Updated go.mod + all imports, updated plugin metadata, new .github/workflows/release.yml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugin-layer-distribution/04-VERIFICATION.md

@go.mod
@plugin/hooks/hooks.json
@plugin/.claude-plugin/plugin.json
@.goreleaser.yaml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide GitHub repository owner/name for module path</name>
  <decision>The go.mod module path must be a real GitHub repository URL for `go install` to work. The current placeholder is `github.com/mysd`. No git remote is configured.</decision>
  <context>
This affects:
- go.mod module declaration (line 1)
- All ~40 .go files with `import "github.com/mysd/..."` paths
- plugin/hooks/hooks.json install command
- plugin/.claude-plugin/plugin.json repository field
- .goreleaser.yaml release.github section (currently auto-detected from remote)

The module path format must be: `github.com/{owner}/{repo}`
Example: `github.com/anthropics/mysd` or `github.com/user123/mysd`
  </context>
  <options>
    <option id="option-a">
      <name>Provide your GitHub username/org and repo name now</name>
      <pros>go install works immediately after update; all placeholders resolved</pros>
      <cons>Must have a GitHub repository created (or planned)</cons>
    </option>
    <option id="option-b">
      <name>Use a temporary but valid path (e.g., github.com/mysd-dev/mysd) and update later</name>
      <pros>Unblocks development; can be changed before first release</pros>
      <cons>Requires another round of import path updates later</cons>
    </option>
  </options>
  <resume-signal>Provide: "owner/repo" (e.g., "myuser/mysd") or select option-b</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update module path across entire codebase + create CI release workflow</name>
  <read_first>
    - go.mod (current module declaration)
    - main.go (import paths)
    - cmd/propose.go (representative cmd file for import pattern)
    - internal/scanner/scanner.go (representative internal file for import pattern)
    - plugin/hooks/hooks.json (placeholder install URL)
    - plugin/.claude-plugin/plugin.json (placeholder repo URL)
    - .goreleaser.yaml (ldflags and release config)
  </read_first>
  <files>
    go.mod, go.sum,
    main.go,
    cmd/propose.go, cmd/spec.go, cmd/design.go, cmd/plan.go, cmd/execute.go,
    cmd/verify.go, cmd/archive.go, cmd/ff.go, cmd/ffe.go, cmd/capture.go,
    cmd/status.go, cmd/task_update.go, cmd/scan.go, cmd/init_cmd.go,
    cmd/scan_test.go, cmd/execute_test.go, cmd/verify_test.go, cmd/archive_test.go,
    cmd/ff_test.go, cmd/status_test.go, cmd/task_update_test.go, cmd/integration_test.go,
    internal/scanner/scanner.go, internal/scanner/scanner_test.go,
    internal/roadmap/roadmap.go, internal/roadmap/roadmap_test.go, internal/roadmap/mermaid_test.go,
    internal/executor/context.go, internal/executor/context_test.go,
    internal/executor/status.go, internal/executor/status_test.go,
    internal/executor/progress.go, internal/executor/progress_test.go,
    internal/verifier/context.go, internal/verifier/context_test.go,
    internal/verifier/report_test.go, internal/verifier/integration_test.go,
    internal/uat/checklist_test.go, internal/uat/integration_test.go,
    plugin/hooks/hooks.json, plugin/.claude-plugin/plugin.json,
    .github/workflows/release.yml
  </files>
  <action>
Use the owner/repo value from Task 1 decision (referred to as OWNER/REPO below).

**Part A: Update go.mod module path**
1. In `go.mod` line 1, replace `module github.com/mysd` with `module github.com/OWNER/REPO`
2. Run `go mod tidy` to update go.sum

**Part B: Update all Go import paths**
Find all .go files (excluding .claude/worktrees/) containing `"github.com/mysd/` and replace with `"github.com/OWNER/REPO/`. There are ~40 files. Use a find + sed approach or manually update each file:
- Pattern to find: `"github.com/mysd/`
- Replace with: `"github.com/OWNER/REPO/`

Key files to update (each has 1-5 occurrences):
- main.go: `import "github.com/mysd/cmd"` -> `import "github.com/OWNER/REPO/cmd"`
- cmd/*.go: imports of `github.com/mysd/internal/*`
- internal/*/*.go: imports of `github.com/mysd/internal/*`
- cmd/*_test.go: imports of `github.com/mysd/internal/*`

**Part C: Update plugin metadata**
1. In `plugin/hooks/hooks.json`: replace `go install github.com/[owner]/mysd@latest` with `go install github.com/OWNER/REPO@latest`
2. In `plugin/.claude-plugin/plugin.json`: replace `"repository": "https://github.com/[owner]/mysd"` with `"repository": "https://github.com/OWNER/REPO"`

**Part D: Create GitHub Actions release workflow**
Create `.github/workflows/release.yml`:

```yaml
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

**Part E: Verify**
Run `go build -o mysd_test_build .` to confirm compilation succeeds with new module path.
Run `go test ./... -count=1` to confirm all tests pass.
Clean up: `rm mysd_test_build` (or `mysd_test_build.exe` on Windows).
  </action>
  <verify>
    <automated>go build -o /dev/null . &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` line 1 contains `module github.com/OWNER/REPO` (not `github.com/mysd`)
    - `grep -r "github.com/mysd" --include="*.go" . | grep -v ".claude/worktrees"` returns zero results
    - `grep "github.com/\[owner\]" plugin/hooks/hooks.json` returns zero results
    - `grep "github.com/\[owner\]" plugin/.claude-plugin/plugin.json` returns zero results
    - `plugin/hooks/hooks.json` contains `go install github.com/OWNER/REPO@latest`
    - `plugin/.claude-plugin/plugin.json` contains `"repository": "https://github.com/OWNER/REPO"`
    - `.github/workflows/release.yml` exists and contains `goreleaser/goreleaser-action@v6`
    - `.github/workflows/release.yml` contains `on: push: tags: - "v*"`
    - `go build -o /dev/null .` exits 0
    - `go test ./... -count=1` exits 0
  </acceptance_criteria>
  <done>
    All placeholder module paths replaced with real GitHub repository URL. All Go files compile and tests pass. Plugin metadata references real repo. GitHub Actions release workflow triggers GoReleaser on tag push.
  </done>
</task>

</tasks>

<verification>
1. `go.mod` first line is `module github.com/OWNER/REPO`
2. No occurrences of `github.com/mysd` remain in any .go file (outside .claude/worktrees/)
3. No `[owner]` placeholder in plugin/hooks/hooks.json or plugin/.claude-plugin/plugin.json
4. `.github/workflows/release.yml` exists with GoReleaser action on tag push
5. `go build` and `go test ./...` both pass
</verification>

<success_criteria>
- DIST-03 gap closed: go install path is a real, resolvable GitHub module URL; CI workflow exists for tag-triggered releases
- DIST-04 gap closed: plugin metadata references real repository
- All existing tests continue to pass after import path migration
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-layer-distribution/04-04-SUMMARY.md`
</output>
